# --- Node / 前端 ---
node_modules
dist
*.local
npm-debug.log*
yarn-error.log*

# --- Python 后端 ---
.venv/
# TTS 独立环境（Python 3.10 + torch，数 GB，由 install_tts_env.ps1 重建）
.venv-tts/
__pycache__/
*.py[cod]
.pytest_cache/
# TTS 环境安装脚本的运行日志（每次运行重写）
install_tts_env.log

# --- 运行时配置 / 工作空间指针（含本机路径与密钥，不入库）---
# 根目录 app.json = 通用配置模板 + 当前工作空间指针；产物 / 日志 / 工程配置都在用户工作空间内
/app.json

# --- 系统 / 编辑器 ---
.DS_Store
