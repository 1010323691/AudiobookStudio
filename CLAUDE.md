# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目是什么

AudiobookStudio 是一个 Web 应用，在一个地方完成中文有声书的制作流程：
**开始（工作空间）→ 排版与分册（排版 → 自动章节分析 → 每章一册）→ 文本解析（LLM→JSON，内含角色匹配检查 / 断句失败校验 / 纯归属标签删除 / 超长段落检查〔LLM 重切 + 机械分段兜底〕/ 归属抽样 / 纯标点条目吸收 / 同人段落合并 / 超长机械分段〔硬保证终态〕）→ 角色配音（LLM 语音推理 + TTS 克隆两阶段）→ 音频合成（批量）→ 音频合并（两阶段）→ 音频分集 → 背景音乐（章节气氛分析 → 标签匹配 → 最终混音）**，
外加设置与全局音乐库（项目根 `music_library/`，跨工程、工作空间外）。它是三层结构的应用：

- **`backend/`** — Python **FastAPI** 服务（端口 `127.0.0.1:8642`，CPython 3.14）。**所有真正的逻辑都在这里。**
- **`src/`** — **Vue 3** 前端（TypeScript、Vite、Tailwind、Pinia、vue-router）。*瘦客户端*：只渲染 UI 并调 HTTP，不含处理逻辑。
- **`tts-engine/` + `.venv-tts/`** — 本地 Qwen3-TTS 引擎。独立虚拟环境（**Python 3.10** + torch(CUDA) + qwen-tts），以**一次性子进程**运行；3.14 后端**永不 import torch**，ML 依赖与应用包完全隔离。

开发和运行本应用**不需要任何原生外壳或额外工具链**。

## 命令

### 后端（Python）

虚拟环境是 `.venv/`（Python 3.14）。依赖清单是 **`backend/requirements.txt`**（不在根目录），刻意精简（fastapi、uvicorn、pydantic、python-multipart）以兼容 CPython 3.14 —— 没有锁定 pytest，但 pytest 已装进 `.venv`。

```
# 启动 API（Windows）
.venv\Scripts\python -m backend.main        # → http://127.0.0.1:8642
# 启动 API（macOS / Linux）
python -m backend.main

# 运行整个测试套件（827 个测试）—— 在项目根目录运行
.venv/Scripts/python -m pytest backend/tests/ -v
# 单个文件 / 单个测试
.venv/Scripts/python -m pytest backend/tests/test_book.py -v
.venv/Scripts/python -m pytest backend/tests/test_book.py::test_range_char_count_excludes_newlines
```

`backend/tests/conftest.py` 把项目根目录加进 `sys.path`，测试以 `backend.engines.*` 方式导入，可从任意目录启动。`tts_worker.py` 的测试用 `importlib` 单独加载其**纯函数**（worker 顶层保持 stdlib-only，3.14 环境可 import），不碰 torch。

### 前端（npm，项目根目录）

```
npm run dev         # Vite 开发服务器，:5173（strictPort）
npm run build       # vue-tsc --noEmit（类型检查）+ vite build → dist/
npm run typecheck   # 仅 vue-tsc --noEmit
npm run preview     # 预览构建产物
```

前端没有 ESLint/linter；`npm run typecheck`（vue-tsc，`strict: true`）就是类型门槛。Python 侧同样没有 linter。

### TTS 独立环境（可选，仅「音频合成 / 音频合并 / 角色配音·克隆」需要）

```
powershell -ExecutionPolicy Bypass -File install_tts_env.ps1
```

重建 `.venv-tts`（幂等：先删后建）：Python 3.10 + torch/torchaudio 2.11.0（**PyTorch cu128 索引 = CUDA 构建**）+ qwen-tts 0.1.1 + transformers 4.57.3 + accelerate + soundfile/pydub/numpy/huggingface_hub。**安装顺序是承重墙**：先装 PyPI 其余依赖（Windows 上会顺带装进 CPU 版 torch——预期内），**最后**用 `--no-deps` 从 cu128 索引强制重装 CUDA torch 三元组，否则 CPU 轮子会静默盖掉 CUDA 构建；脚本第 4 步**断言 `cuda_available=True`**（不能只查退出码——python 一行打印 False 也退出 0）。脚本刻意 ASCII-only（PS 5.1 按系统 ANSI 码页读 `.ps1`，非 ASCII 会坏）。

### 运行应用

1. **开发态** —— 启动后端 + `npm run dev` → `http://localhost:5173`（`start.bat` 一键做这三件事）。
2. **后端托管（最简）** —— `npm run build` 后只启动后端 → `http://127.0.0.1:8642`。`main.py` 的兜底 SPA 路由托管 `dist/`（**注册在所有 API 路由之后**；未知 `/api/*` 一律真 404——GET 由 SPA 路由内判断、非 GET 由专门的 `api_not_found` 路由兜住，不会误返 SPA 外壳；`dist/` 不存在时返 503 提示先 build）。

## 目录结构

```
项目根/
├── backend/                 # Python 后端（所有处理逻辑）
│   ├── main.py              # FastAPI 入口：路由注册、CORS、/api/health、SPA 兜底托管
│   ├── api/                 # 瘦路由：text / book / audio / tts / script + 横切 tasks / config / files / workspace；_common.py 共享助手
│   ├── core/                # 基础设施：paths / pathio / config / tasks / concurrency / logging_setup
│   ├── engines/             # 真正的算法（见「流水线逻辑链」）
│   ├── resources/           # 捆绑默认提示词：default_prompts.txt（解析）、default_check_prompts.txt（解析内重判，不可配置）
│   ├── tests/               # pytest 套件（827 个测试）
│   └── requirements.txt     # 精简依赖（fastapi / uvicorn / pydantic / python-multipart）
├── src/                     # Vue 3 前端（瘦客户端）
│   ├── api/                 # 唯一 HTTP 客户端 client.ts + 每模块一个封装
│   ├── views/               # 8 个页面（每模块一个）
│   ├── stores/              # Pinia：app / project / settings / task
│   ├── components/          # DirPicker + components/ui/ 原子组件（shadcn-vue 风格）
│   ├── composables/         # useAudioBus / useWorkspaceGate
│   └── utils/               # fileops / format / log-follow
├── tts-engine/tts_worker.py # TTS 工作进程（跑在 .venv-tts，一次性子进程；6 种 --mode）
├── music_library/           # 音乐库（全局、跨工程、.gitignore；music_index.json + *.mp3/*.wav，引擎自管，永不进 pathio 链）
├── install_tts_env.ps1      # 重建 .venv-tts
├── start.bat                # 一键启动（后端 + npm dev + 开浏览器）
├── .venv/                   # 3.14 应用环境（gitignore）
├── .venv-tts/               # 3.10 ML 环境（gitignore）
├── app.json                 # 根配置模板 + 工作空间指针（gitignore；唯一可写根级字段是 paths.working_dir）
└── dist/                    # 前端构建产物（gitignore），后端直接托管
```

**工作空间**（用户在「开始」页选的文件夹，一切产物所在）固定子目录：

```
<workspace>/
├── 00_temp/        # 引擎子进程暂存（段表 / 段清单 / part WAV，用完即清）
├── 01_input/       # 原始上传 + 排版结果（<原stem>_排版<ext>）
├── 02_split_text/  # 章节文件（第 NNN 章 标题.txt，每章一个——智能识别或点「分册（智能识别结果）」的产物，两者字节相同）+ 零章节时单个 <base> 全书.txt；旧工程或有遗留 <base> 分册NN 第XXX章.txt / .zip（不读取、不删除）
├── 03_parsed_json/ # 解析 JSON（<stem>.json 基文件 = 唯一产物；旧工程或有惰性 <stem>_checked.json 孤儿——不再读取/生成，留盘不删）
├── 04_voice_profiles/  # voice_config.json + designed_voices/*_c{k}.wav 克隆候选试听
├── 05_audio_chunk/     # 每包一子目录 <源JSON stem>/：逐行 mp3 + manifest.json
├── 06_audio_merge/     # <包名>.mp3（编码失败兜底 <包名>.wav）
├── 07_output/          # 每源一子目录 <源stem>/：分集文件 + <base>.zip
├── 08_bgm/             # 背景音乐成品 <章stem>.mp3（无 BGM 章 = 06 原声 copy2）+ chapter_music_analysis.json / bgm_assignments.json（引擎自管）
├── logs/           # app.log（轮转）+ tts_batch_*.log / tts_clone_*.log（合成/克隆排障镜像）
└── config/         # app.json 当前工程独立配置 + spot_check_history.json（归属抽样读数历史；引擎自管，不经文件 API）
```

## 核心约定（承重墙 —— 改代码前必读）

1. **字数统计 = 去除换行后的 Unicode 码点数**。分册的 `range_char_count` 对换行位置表做二分（非 BMP 计 1）；排版 stats 的 `chars` 是去空白长度。不要改成字节长度，不要计入换行。
2. **写文件用 `write_bytes` / `.encode("utf-8")`，绝不用 `write_text`** —— Windows 上 `write_text` 把 `\n` 翻译成 `\r\n`，破坏往返/无损保证（分册拼接复原原文、zip 字节还原都依赖它）。
3. **路径模型（`core/pathio.py`）= 工程可整体搬家的承重墙**。工作空间**内部**路径在成果物 JSON 里一律存**工作空间相对**的正斜杠串（`05_audio_chunk/s/0001.mp3`、`voice_config.json[].ref_audio`）；运行时读取一律 `pathio.resolve_path(值, 当前工作空间根)`。明确外部资源（`ffmpeg_path`/`ffprobe_path`、工作空间指针）**保持绝对、永不转换**（`to_workspace_relative` 对外部值返回 `None` 即此约定）。旧绝对路径透明兼容：读取按原值解析，加载/保存时经 `migrate_entries_in` **幂等**迁移为相对形式；工程搬家后失效路径按「原目录结构尾部（首个 `01_input/`…`07_output/`、`logs/`、`config/` 之后重锚）→ 全目录唯一文件名」两级恢复；恢复不了才抛**清晰**的 `PathNotFoundError`（提示重新选择工作目录），**绝不静默失败**。相对值经 `..` 越出工作空间 → `PathOutsideWorkspace`（绝不持久化）。API 响应给前端的路径仍是**绝对**路径（瘦客户端直接回传/拼下载 URL）；给 worker 的临时清单（`00_temp/merge_segments_*.json` 等）也带绝对路径（一次性，不持久化）——worker 另经 `--workspace` 参数解析 voice_config 里的相对 `ref_audio`。
4. **绝不删除用户数据**：`Layout.ensure()` 只 `mkdir(exist_ok)`；切换/清除工作空间不移动、不删除任何旧文件；引擎只清理自己在 `00_temp/` 的暂存物。
5. **LLM 调用走 stdlib `urllib.request`**（OpenAI 兼容 `chat/completions`，body/headers 与 openai SDK 字节级一致），**不装 openai SDK**（保持 3.14 依赖精简）。入口都在 `engines/script.py` 的 `_llm_chat_completion(_stream)`，`voices` 复用它。
6. **TTS 隔离**：3.14 后端永不 import torch。一切 ML 工作 = 一次性 `.venv-tts` 子进程（`engines/tts.py::run_worker` 统一编排）。子进程强制 `PYTHONUTF8=1`+`PYTHONIOENCODING=utf-8`（防 GBK 码页搞乱 `[progress]`/`[result]`/`[segment]` 行里的中文与路径）；取消时 Windows 用 `taskkill /F /T` 杀**进程树**（否则 worker 的 ffmpeg 孙进程成孤儿）。
7. **任务隔离**：长时任务任何异常 → 该任务 `failed`（`task.error = str(exc)`），**绝不把控制台带崩**；取消 → `cancelled`（不算失败）。
8. **确定性三引擎的不变量**（源自 JS 工具 1:1 移植，测试固化，用户依赖）：
   - `text.py`（←TextFormatter）：确定性空白/段落/标点/章节规则；**内容保持 + 幂等**（对已排版文本再排版是空操作）。
   - `book.py`（←BookChunker）：章节**铺满整段文本**（首章 start=0、末章 end=len、`ch[i].end==ch[i+1].start`）；切分**只落章节边界**（**每章恰好一个文件**，`分册NN` 只是位置顺序号）；**从不重编号**（缺口保留原号，如 1,2,5 → `第001章`/`第002章`/`第005章` 三个文件——引擎级行为，`make_chapter_filenames` 保留但 API 已不再调用）；**无章节 → 停止**（`/split` 返 400；普通分册路径已移除，API 只接受 `smart=true` 或 `whole_book=true`——整本例外写单个 `<base> 全书.txt`）；**所有逐章文件拼接 == 原文**。智能识别（`smart_repair`）是**产物级例外**：它**会**重编号（物理序 1..N）并**删除**同号且正文指纹相同的重复章——但只作用于 `02_split_text/` 的写出产物，**原文永不改写、源文件永不删**（「绝不删除用户数据」不受影响）；其不变量是**保留章 + 已删 span 精确铺满 [0, len]**（往返 = 原文按已删 span 逆序挖除后的文本，`test_book.py` 固化）。
   - `audio.py`（←mp3-cue）：均分后**每段 ≤ 目标时长**（+1e-6 容差）且铺满 `[0,total]`；停顿对齐只移动**内部**边界、保持**严格单调**（棘轮防交叉）、**从不改变段数**（窗口无停顿则留在原均分位，计 fallback）；切割是**无损 `-c copy`** 流拷贝（不重编码；ffmpeg/ffprobe 从 `config.ffmpeg` 解析否则取 PATH）。
9. **解析内检查的不变量**（解析任务内各阶段，按序：角色匹配检查 → 断句失败校验 → 纯归属标签删除 → 超长段落检查〔LLM 重切〕→ 归属抽样 → 纯标点条目吸收 → **同人段落合并** → 超长段落机械分段〔硬保证终态〕，见阶段 2）：基文件 `03_parsed_json/<stem>.json` 是**唯一产物**，只由解析任务自己写出——检查阶段**永不改写**它（归属抽样 / 角色匹配检查 / 超长段落重切的修正由解析任务随基文件一并写出）。各阶段项目级开关：`generation.check_boundary_speakers` / `revalidate_splits` / `delete_saying_tags` / `check_long_paragraphs`（覆盖超长段落检查两半——LLM 重切与机械分段兜底）/ `absorb_punct_entries`（**默认开** = 现有行为不变）/ `merge_same_speaker`（**默认开** = 同人段落合并，关闭 = 跳过 + 日志、`merged_same_speaker` 保持 0），归属抽样由 `spot_check_rate` 门控（0 = 关）；关闭 = **整体跳过该阶段并留一行「…已关闭（配置）」日志**（防静默被误读为阶段缺失），对应结果字段（`boundary_checked` / `boundary_fixed` / `suspicious` / `suspicious_fixed` / `tags_deleted` / `long_checked` / `long_fixed` / `long_split` / `punct_absorbed` / `punct_deleted` / `merged_same_speaker`）保持存在、值为 0。**超长段落硬保证**：`check_long_paragraphs` 开时最终基文件**绝不出现 strip 后 > `max_paragraph_chars`（默认 200）字的条目**——LLM 重切（同断句校验的重判协议：忠实性门要求多段需 ≥2 个不同 speaker → 同一说话人长独白的「拆分」无票、原样保留）之后仍超长的条目**以及同人段落合并的 ≤10 强制合并造出的超限块**，一律由确定性 `split_long_entries` 切开（三级边界放宽：句末 > 子句界 > 定宽硬切，各段去空白拼接骨架 == 原文骨架）；**机械分段排在纯标点吸收与同人段落合并**之后（吸收可把邻接 NARRATOR 推过上限、同人强制合并可造 >200 块，均须由兜底再切），且是**检查段最后一步**——切出的段其后无合并、绝不被回粘。归属抽样 / 角色匹配检查 / 超长段落重切**恒用**捆绑重判提示词（`check_prompts` + `resources/default_check_prompts.txt`，**不再用户可配**）与共享重判协议（断句校验 / 超长段落重切经 `revalidate_entry` 逐条多者胜，归属抽样 / 角色匹配检查经 `_run_rejudge_groups` 批协议），几何取 `generation.check_batch_size` / `check_context_window`（**UI 不露出**）。重判协议**严格区分 target 与 context**（context 条目仅提供判断依据、**永不被修改**，只有 target 条目可被改写——提示词明令 + 应用代码只写 target 下标 + 窗口恒按原始列表预建，三层保证）：仅**严格多数**（≥2 票且唯一领先）且 ≠ 原值才改；无共识 / 解析失败 → 保留原值，**从不猜**；**只改 `speaker`** + 台词「仅去外层引号」的机械计算值严格采纳（NARRATOR 终值不剥）；角色名**禁止翻译/音译/本地化/改写**（提示词层强约束：必须逐字复制窗口/花名册内既有 speaker，中文角色名保持原汉字）。旧工作空间遗留的 `_checked.json` 是**惰性孤儿**：resolver 永不读取（自动选取 / `__all__` 只取基文件），文件列表可见、可下载、显式指名可直读，**绝不删除**（「绝不删除用户数据」约定）。
10. **合并不变量**：输入是 batch 写好的逐行 mp3（**不重解码**，pydub 拼接）；两阶段（每 100 段一批 part → 整书）的**批间间隙必须与单遍合并逐一样本一致**（`boundary_gap_ms`：段级 `pause_after` override 优先 > 同人 `same_ms` > 换人 `pause_ms`）；全链路只有一次 ffmpeg 编码（`-c:a libmp3lame`，刻意**不带 `-b:a`**，与 pydub 默认导出一致）；合并顺序恒等于解析 JSON 行序（按 `index` 重排）。

## 后端架构

### `backend/main.py`

`ROUTERS`（注册顺序）：tasks、config、files、text、book、audio、tts、script、workspace。CORS 全开放（仅面向本机回环客户端）。`GET /api/health`。SPA 兜底见「运行应用」。

### `backend/core/`

- **`paths.py`** — `Layout`（单根）：跟随工作空间根（根 `app.json` 的 `paths.working_dir` 指针），持有 8 个管线目录 + `logs/` + `config/`。`get_layout()`：未设置 → 惰性 `Layout(None)`（一切属性 None，`ensure`/`dirs` 空转）；已设置但文件夹已不在（工程被移动/删除）→ **同样保持惰性，不在旧位置重建空骨架**（由 `GET /api/workspace` 的 `exists: false` 与写端点 409 告知用户重选）。`resolve_parsed_json(script=None)`：解析下游该读哪个脚本 JSON——给定文件名则**原样直读**（显式基名与显式 `_checked` 名都不再做任何升级/变换），省略则取 mtime 最新的**基文件**（孤儿 `_checked` 永不遮蔽基文件），最后兜底遗留名 `annotated_script.json`。`resolve_parsed_json_all()`：「全部文件」（整书聚合）= 所有**基文件**按 `(mtime, name)` 排序（mtime ≈ 分册生成序 ≈ 阅读顺序，抗中文数字乱序）；孤儿 `_checked` 文件**永不读取**（列表可见、可下载、显式指名可直读、绝不删除）。哨兵 **`ALL_PARSED_JSON = "__all__"`**（前后端同一字面量；**只用于角色配音**，音频合成收到它直接 400）。
- **`pathio.py`** — 路径序列化/解析统一入口（详见核心约定 #3）。异常：`PathOutsideWorkspace`、`PathNotFoundError`（用户可读文案）。
- **`config.py`** — 持久化配置，**两个文件一个指针**：根 `app.json` = 通用配置模板 + 工作空间指针（`paths.working_dir` 是唯一允许在根级写入的字段，其余只读，作为新工作空间的种子）；`<workspace>/config/app.json` = 当前工程独立配置（设置工作空间时从根模板复制、**已存在则绝不覆盖**；此后所有读写只针对它，`update_config` 深合并 patch 后强制 `working_dir` 为该工作空间）。Pydantic `AppConfig`，线程安全（`RLock`）；根文件缺失由代码默认值自动种子。`get_config()` 每次读时把内存里的 `working_dir` 自愈为根指针当前值（工作空间被移动重选后 UI 不显示陈旧指针）；`reset_config_cache()` 在指针变化后调用。读取链：工作空间配置 → 根模板 → 纯代码默认（**读永不写**；文件损坏/缺失静默降级）。
- **`tasks.py`** — 任务系统（详见下文专节）。
- **`concurrency.py`** — 进程级 `ConcurrencyGate`（`threading.Condition` 而非 Semaphore，**上限可增可减**；恒 clamp ≥1，**没有"无限"模式**，保证 acquire/release 严格配平）。`set_concurrency(n)` 在每个解析批次开始时按 `config.generation.max_concurrency` 调。语义：**文件间并行、文件内 LLM 调用串行**。**槽位作用域 = 仅 LLM 分段解析阶段**（2026-09 解析页改造）：读文件/解码/分 chunk 在**占槽前**（预备），chunk 循环一结束即 `release`，各机械检查/后处理阶段（含同人段落合并 + 超长机械分段）+ 写基文件**在槽外**（检查期的重判 LLM 调用（边界重判 / 断句重判 / 超长段落重切 / 归属抽样）不计入并发上限——用户拍板的有意语义）。`acquire(stop_check=…) -> bool`：提供 `stop_check` 时等待改为协作式（**0.2s 轮询**谓词；返回 True = 取槽 / False = 等待中被取消、**未取槽**、调用方必须放弃且不 `release`）；`stop_check=None` = 原 notify 驱动阻塞语义（也返回 True）。「**有序投放 + 预取深度 4**」由 `api/script.py` 的每批协调者线程实现（0.2s 轮询 `gate().active` + `task.phase`，按勾选序 `start` 待投壳；不变量 = 「等待槽的任务数 ≤ 4」，检查期任务既不持槽也不占预取位）。**第二闸 `merge_gate`**（`set_merge_concurrency(n)` / `merge_gate()`）= 批量合并的独立进程级闸（2026-09 合并页改造）：与 LLM 解析闸**完全分离**——合并是 CPU/ffmpeg/磁盘密集型（无 LLM、无 GPU），解析批与批量合并可并行跑；上限 = `engines/merge.py::concurrency_limit()`（逻辑处理器数 ÷ 2，钳 [1, 4]，**常量策略、无配置项**），每个合并批次开始时重置。合并 `run` 的**槽位作用域 = 引擎运行全程**（全部快速失败校验之后占槽、`finally` release——区别于解析闸的「仅 LLM 分段阶段」；合并任务全程持槽、无「检查期」）；其「**有序投放 + 预取深度 4**」由 `api/tts.py::_run_merge_coordinator` 实现（0.2s 轮询 `merge_gate().active`，按提交序 `start` 待投壳；**无 `phase` 判定/无注册表/无 stop 标志**——取消 = 前端逐任务 `control`，见阶段 5 与「已知陷阱」）。
- **`logging_setup.py`** — 轮转文件 handler 跟随工作空间（`<workspace>/logs/app.log`，2MB×5 份），设置/清除工作空间时重定向；未设置 → 仅控制台；指针指向的文件夹已不在 → **降级控制台，不复活幽灵 `logs/` 树**。

### `backend/api/`（瘦路由 → engines）

| 模块 | 端点（全部前缀 `/api/...`） |
|---|---|
| `_common.py` | `require_workspace()`（写端点守卫：未设置 409；目录已不存在 409）、`resolve_inbound_path()`（400 系）、`read_decoded_file()`（自动编码探测，400 系）、`partial_copy()`（只应用已知字段，防客户端脏键） |
| `files.py` | `GET /files/list/{module}`（module = **磁盘目录名**如 `02_split_text`，7 个可列目录，`00_temp` 不可寻址）、`GET /files/download/{module}/{name}`（FileResponse，防穿越）、`POST /files/upload`（FormData → `01_input/`，返回绝对路径） |
| `workspace.py` | `GET /workspace`（`{set, path, exists, is_default, dirs}`）、`PUT /workspace`（path 空 = 清除并重新锁定；先 mkdir 校验再落指针 → 400 不会写坏指针） |
| `config.py` | `GET /config`（空 prompts 在**响应里**种子捆绑默认值，不落盘）、`PUT /config`（409 守卫；深合并 patch 持久化到工作空间配置） |
| `tasks.py` | `GET /tasks`、`GET /tasks/stream`（**多路复用 SSE：一条连接推所有任务**，每事件带 `task_id`，连接先发 `snapshot_all`；**必须注册在 `/{id}` 之前**）、`GET /tasks/{id}`、`POST /tasks/{id}/{action}`（cancel/pause/resume/retry）、`GET /tasks/{id}/stream`（单任务 SSE，保留） |
| `music.py` | 音乐库（**全局，全部端点无 `require_workspace`**）：`GET /music/library`（= 索引 + `suggestions`〔批量 AI 候选，**只列现存曲目**——孤儿隐藏不删除〕）、`POST /music/upload`（multipart；同名 409 不覆盖 / 非 mp3·wav 400 / 空文件 400）、`GET /music/preview/{name}`（FileResponse 防穿越）、`PUT /music/tracks/{name}`（tags/enabled/description；词表外标签自动进 custom；**带 tags = 用户已做标签决定 → 消费该曲 AI 候选**〔`clear_suggestion`〕）、`DELETE /music/tracks/{name}`（被 locked 章引用 → skip + reason；未锁定引用不阻挡）、`POST /music/tracks/batch-enable|batch-tags|batch-delete`、`POST /music/tracks/apply-suggestions {names}`（**AI 推荐采用**：只作用于「有候选 且 四类全空」的曲目，**已手动打标绝不覆盖**〔skipped_manual + 候选保留〕；采用后消费候选；空选择/穿越 400、缺失静默报告）、`POST /music/tags` / `POST /music/tags/rename` / `DELETE /music/tags/{category}/{name}`（波及注册表 + 全 track + `08_bgm` 分析缓存；assignments 快照不改写；删除标签不删音乐文件）、`POST /music/suggest-tags`（**同步 LLM 不经任务系统**；prompt = 文件名 + 描述 + 词表；词表外过滤 + 钳上限；全败 502）、**`POST /music/suggest-tags-batch {names}`**（**每曲一 Task**，module `music-ai-tags` / label `AI 推荐标签：{name}`〔承重契约，三处钉死〕；守卫 = 逐名 穿越 400 / 缺失 404 → 空选择 400 → model 空 400 → **同曲在途 409**〔追加不冲突放行〕；`set_concurrency(generation.max_concurrency)` → PENDING 壳 + 复用 `api/bgm.py::_run_bgm_coordinator`〔真共享 LLM 闸 `gate()`、预取深度 4〕；候选只写 `music_tag_suggestions.json`、索引不动——用户确认前标签零变化） |
| `bgm.py` | 背景音乐：`GET /bgm/chapters`（只读无 409；无工作空间空行降级；行基准 = 02 stem；含 `narration_exists` / `mix_exists` / `music_missing`）、`POST /bgm/analyze {chapters}`（每章一 Task，共享 LLM 闸）、`POST /bgm/match {chapters?, mode}`（同步；`segment` → 400）、`PUT /bgm/chapters/{stem} {tags?, music?, locked?}`（同步写）、`POST /bgm/mix {chapters}`（每章一 Task，`merge_gate`） |
| `text.py` / `book.py` / `audio.py` / `tts.py` / `script.py` | 见下「流水线逻辑链」各阶段 |

### `backend/engines/`

| 文件 | 职责 / 入口 |
|---|---|
| `text.py` | 确定性排版。`format_text(text, cfg) → {text, stats}` |
| `book.py` | 分册（每章一册）+ 智能识别（章节结构机械修复）。`decode_buffer`（编码探测，**全后端共用**——`script.py` / `_common.py` 复用）、`analyze_text`、`make_chapter_filenames`（`<base> 分册NN 第XXX章.txt`，NN 与章号双宽独立；**遗留命名——普通分册移除后 API 不再调用**）、`make_whole_book_filename`（`<base> 全书.txt`）、`chapter_content`（单章原文切片）、`check_chapter_sequence`、`EXPECTED_CHAPTER_FORMAT`（用户可见的章节格式文案）、`build_zip`（**API 不再调用**，测试固化）、**`smart_repair`**（智能识别纯函数：重编号 1..N + 异常长章推断拆分 + 同号同内容章删除 + 自检，见阶段 1；`LONG_CHAPTER_RATIO=2.0` / `MIN_LENGTH_SAMPLES=5` / `MIN_BASELINE_CHARS=200` / `INFER_SNAP_TOLERANCE=0.5`）、`make_smart_filenames`（`第 NNN 章 标题.txt`，N≤1000 三位宽 / 否则四位宽，sanitize 后判重加 ` 2`/` 3` 后缀）、`_normalized_fingerprint`（切片去全部空白 sha256，重复章判定） |
| `audio.py` | 分集。`probe_duration`、`detect_silences`（流式 stderr）、`parse_silence_log`、`build_plan`、`snap_boundaries`、`build_aligned_plan`、`cut_segments`（`-c copy`）、`output_name` |
| `script.py` | LLM→JSON 解析。`split_into_chunks`（**2026-09 重构**：先定段数再均分切——段数 `ceil(total/size)`〔过短尾段 `total−(count−1)·size < size/2` → 减一重新平均；再钳到 内部边界数+1〕→ 逐目标位 `total·k/count` 在统一边界表（段落 `\n\s*\n` > 换行 > 句末：CJK `(?<=[。！？!?…])` 零宽 / ASCII `(?<=[.!?])` 须后随空白）取**最近合法边界**（前驱/后继、严格单调、悬题改判；无边界 → 不切，允许超 `chunk_size`，**绝不拦腰截断**）→ 收尾过短块（<`size/2`）**只删已有切点**与较短邻边合并（不跨章标题行；不可并 → 保留短块）；无损口径 = 去空白后拼接相等；章标题防丢 = 搜索期切点改判 + `_evict_trailing_title` 兜底 + carry）、`check_chunk_fidelity`（逐 chunk 忠实性：引语段骨架比对）、`split_chunk_balanced`（近中点安全边界对半切）、`process_chunk`（重试/修复/抢救 + 忠实性恢复：翻倍 max_tokens → 一级对半切 两级阶梯）、`is_suspicious_entry_text` / `suspicious_entry_indices`（断句失败检测：外层双引号包裹 + 引号内「…道 + 冒号」，`rfind` 取最宽跨度）、`_strip_leading_saying_tag`（剥开头「…道：」纯标签——忠实性门的第二允许骨架）、`_parse_entries_reply`（重判回复的清理/修复/抢救阶梯）、`_reparse_vote`（重判忠实性门 → 投票值）、`revalidate_entry`（单条多者胜：1+2 次早停、无共识第 4 次）、`validate_sentence_splits`（解析后断句校验：窗口/花名册预建 + 降序应用）、`delete_pure_saying_tags` / `_is_pure_saying_tag`（纯归属标签条确定性清理：NARRATOR + 无引号 + ≤10 字 + 五动词收尾 + 非标题 + 紧邻对白 → 删除；非级联、零 LLM 成本）、`long_entry_indices`（超长条目纯函数：strip 后 > `max_chars` 字）、`long_paragraph_resplit`（超长段落 LLM 重切 = 阶段 A 前半，与断句校验同形：零命中一行日志零调用、context 首行注记「LONGER than N characters」+ 花名册 + 窗口、`revalidate_entry(stage="超长段落重切")` 逐条多者胜、降序应用；progress 带 [0.96, 0.98) 与断句校验**时间互斥**）、`split_long_text` / `split_long_entries`（超长段落机械分段 = 阶段 A 后半兜底：三级边界逐级放宽 ①句末（与切分同口径，保护 `3.14`/`e.g.`）②子句界 ③定宽硬切（切点先查 ±20 字窗引号安全位，找不到才裸切）；①② 切点须**引号安全**（`_quote_parity` 前缀开−闭奇偶与起点一致）；tier 内取**最接近目标宽度**候选（平手取较小偏移）；不变量 = 各段去空白拼接骨架 == 原文骨架 + 每段 ≤ `max_chars` + 切点严格前进；条目级 = speaker 继承、instruct 只给首段、非级联单遍）、`absorb_punct_entries`（纯标点/无词字符条目吸收 = 阶段 B：`_skeleton` 为空的条目 → 并入相邻 NARRATOR（前邻优先、章标题守卫、`consumed` 集防链式丢文本），无 NARRATOR 邻接 → 删除；确定性、零 LLM 成本）、`spot_check_speakers`（解析后归属抽样：两桶抽样〔1/3 纯随机仪表 + 2/3 风险级联〕+ 复用捆绑重判提示词（`check_prompts`）/共享重判批协议重判、内存修正随基文件写出；`rng` 可注入供测试）、`spot_budget`/`select_spot_targets`/`_risk_tier`/`_has_attribution_tag`/`_tag_in`（抽样纯函数：预算两桶 / 级联选样 / 特征计数 / 归属标签检测〔知道/难道 形态守卫〕）、`_load_spot_history`/`_append_spot_history`（`config/spot_check_history.json` 读数历史：模块锁读-改-写、保留最近 50、`write_bytes`）、`select_boundary_targets`（角色匹配检查目标纯函数：内部 chunk 边界两侧各 n 条、全局去重排序、头/尾边界不查）、`boundary_check_speakers`（解析后角色匹配检查：跨 chunk 窗口重判边界两侧条目，复用捆绑重判提示词/共享重判批协议；单 chunk → 静默跳过，零目标 → 一行日志、均零 LLM 调用；context 永不被修改）、`_run_rejudge_groups`（共享重判执行器：逐组窗口 → 首判 → 分歧条目动态投票 → 严格多数 → **只写 target 下标**；归属抽样 / 角色匹配检查共用）、`merge_adjacent_same_speaker`（同人段落合并：连续**同 speaker** 条目按词字符数〔`_skeleton` 骨架长度，非原文长度〕合并——块+段 ≤100 或较短一方 ≤10 **强制**（即使 >100）；≥3 段左→右贪心（运行块，满足任一条件并入、否则封块开新块）；text 首尾相接、边界前段尾无收尾标点（`ends_sentence`）补「。」；instruct 取词字符最多成员（tie → 最左）；章标题守卫**恒判无豁免**（每次吸收前对运行块重跑 `title_test`）；返回 (新列表, 吸收步数)；输入不动；确定性零 LLM 成本）、`generate_file`（Task worker，可选 `rng` 参数；**槽位只覆盖 LLM 分段解析阶段**：读/解码/分 chunk 在占槽前，chunk 循环结束 `gate().release()` + `handle.phase("check")`，六检查 + 写基文件在槽外——配平由 `finally: if not slot_released: release()` 保证，排队中被取消（`acquire(stop_check)` 返回 False）的路径从未取槽不 release；解析内检查/后处理各阶段带开关门：`check_boundary_speakers` 角色匹配检查 / `revalidate_splits` 断句校验 / `delete_saying_tags` 标签删除 / `check_long_paragraphs` 超长段落检查（含 LLM 重切 + 机械分段兜底两半）/ `spot_check_rate` 归属抽样（0 = 关）/ `absorb_punct_entries` 纯标点条目吸收 / `merge_same_speaker` 同人段落合并——关 = 跳过 + 一行日志 + 结果字段 0；**阶段顺序** = 角色匹配检查 → 断句校验 → 标签删除 → 超长重切 → 归属抽样 → 纯标点吸收 → **同人段落合并** → 机械分段（硬保证终态，同人在其**前**——≤10 强制合并可造 >200 块、由机械分段切回）；**progress 重标度**：100% = 仅完成态、解析阶段 [0, 0.9)、角色匹配检查 [0.9, 0.96)、断句校验 / 超长重切共用 [0.96, 0.98)（时间互斥）、归属抽样 [0.98, 0.998)）；`_llm_chat_completion(_stream)`（**全后端 LLM 传输的唯一实现**）；`clean_json_string`/`repair_json_array`/`salvage_json_entries`/`fix_mojibake`；**重判批协议**（自已退役的独立检查模块整体搬入，断句失败校验 / 归属抽样 / 角色匹配检查共用）：`build_roster`（全书角色花名册）、`build_batch_window`（±window 上下文窗口；可选 `skip` 参数 = 组内非目标条不标 target）、`strip_outer_quotes`（协议唯一允许的 text 编辑 = 去外层引号）、`parse_speaker`（单条回复多形态解析）、`parse_speaker_map_full`（批回复解析，另捕获可选 `text` 键）、`_pick_majority`（严格多数投票）、`group_retry_indices`（按 ≤batch 分组 + 间距 ≤window 的重试分组）、`_llm_call`（重判 LLM 调用封装 = 解析传输的薄包装） |
| `script_prompts.py` / `check_prompts.py` / `persona_prompts.py` | 捆绑默认提示词（`load_default_prompts` / `load_default_check_prompts` 在 **import 时**加载 `resources/*.txt`，分隔符不恰有一个 `---SEPARATOR---` 会 RuntimeError）；`check_prompts` = 解析内重判阶段的**唯一**捆绑默认（不可配置）；`PERSONA_SYSTEM_PROMPT`/`PERSONA_USER_PROMPT` |
| `voices.py` | 角色配音两阶段。`prepare_foundations`（阶段 1，纯 LLM）、`make_clones`（阶段 2，纯 TTS，(角色,k) 候选作业池 → 单一长驻 design-batch 子进程 + 看门狗缩批/隔离重启）、`_fold_aliases`、纯函数族（`extract_json_object`/`_select_target_bands`/`pick_ref_text`/`_sanitize`/`auto_candidate_count`（自动备选数对数分档）/`effective_candidates`/`_clone_have`/`_effective_line_counts`（别名台词归并）/`_canonical_of`…） |
| `tts.py` | TTS 族**基础模块**（不再是"单条合成"）：`resolve_engine()`（`.venv-tts` 解释器 + worker 脚本；env 覆盖 `AUDIOTTS_PYTHON`/`AUDIOTTS_WORKER` 供测试桩用）、`run_worker()`（一次性子进程编排器：pump 线程、`[progress]`/其余行分流、协作取消/暂停、`log_file` 镜像、进程树 kill、temp 清理）、`WorkerWatchdogTimeout` |
| `tts_batch.py` | 批量合成编排。`synthesize`（单文件入口，同签名薄封装 → `_synthesize_one`，逐字节不变）、`_synthesize_one`（单文件入口背后的每文件主体，含看门狗缩批/隔离重启循环）、`synthesize_multi`（多文件 Task worker：**池化**——所有章节待合成段读入同一 TTS 任务池、单一引擎子进程全池统一调度；池行经 seg JSON 携带 `out_dir`/`file_index`（worker 命令形状不变）；准备阶段 fatal 隔离 + 包冲突守卫；看门狗 strike/隔离经池全局 index 映射回各章 manifest；多 manifest 共享 2s 节流）、`_PooledFile`/`_build_pool_rows`/`_handle_segment_pool`/`_settle_pool`（池簿记纯函数）、`_build_cmd`、`_build_segments`、`package_for`、`load_manifest`/`build_manifest`（增量写）、`is_done`、`plan_to_synthesize`、`count_completion`、`clamp_concurrency`（[1,64]）、`encode_restore_stack`（降档记录栈 → `--restore-stack` 值，空 → `""` = flag 整体省略）、`_parse_restore_cap`（`[restore]` 行解析）、**降档恢复簿记**（`_synthesize_one` / `synthesize_multi` 各持运行内存 `restore_stack`：workers>1 降档时压 `(超时子批总字数, 降档前上限)`、记罚/未知索引不压；`on_line` 的 `[restore]` 分支弹栈顶 + 同步 `workers`）、**进度指标**（`STATS_FLUSH_INTERVAL=1.0`；`_synthesize_one` / `synthesize_multi` 各持 `_report_stats`：每段完成上报**全量累计** = 运行前 manifest done 集 ∪ 本次运行 ok 段（union 口径，失败段不计）对全部可合成段总数〔池化 = 全池口径，prep fatal 文件不进总数〕；每段完成触发、节流至最多每 1s 一次，基线 / 收尾 / 异常各强制推一次，经 `handle.segment_stats` → SSE `segments` 事件——合成页「开始音频合成」按钮下方「已合成/总段数 · 已合成/总字数」卡的唯一数据源） |
| `merge.py` | 两阶段合并编排（批量合并 = 每包一任务并行，2026-09 改造）。`concurrency_limit`（并行度 = 逻辑处理器数 ÷ 2，钳 [1,4]，常量策略无配置项）、`run`（Task worker；**`merge_gate` 闸作用域 = 全部快速失败校验之后占槽 → `finally` release**，排队中取消 → `TaskCancelled` 零残留）、`_find_manifest`、`collect_segments`、`_output_name`（恒 `.mp3`，编码失败兜底 `.wav`）、`MERGE_BATCH_SIZE=100` |
| `music.py` | 音乐库引擎（全局 `music_library/music_index.json` 单索引）。常量 `TAG_CATEGORIES`（scene/mood/emotion/custom 四桶）/ `TAG_WEIGHTS`（mood 3 / scene 2 / emotion 1 / custom 1）/ `DEFAULT_TAGS` 内置词表 / `ALLOWED_EXTS={.mp3,.wav}`；纯函数 `normalize_track_tags`（每桶只留词表内名字、词表外 + 客户端 custom 折叠进 custom）、`is_generic_track`（enabled 且四类全空 = 「通用音乐」）、`find_tag_category`、`apply_tag_rename` / `apply_tag_delete`（注册表 + 全部 track.tags 波及，返回波及数）、`validate_music_name`（`name == Path(name).name` + 扩展名）；IO = 模块 `_INDEX_LOCK`，`load_index`（缺失 → 默认词表播种**不落盘** / 损坏 → 降级 WARNING）、`save_index`（`write_bytes` + `os.replace`）、`update_index(mutator)`（锁内 读→改→写 原子事务——上传全程锁内，同名检查 + 写文件 + 写索引防并发同名牌）；**AI 标签候选缓存**（`music_tag_suggestions.json`，**只存候选、永不写索引**）：`load_suggestions`（缺失 → 空缓存**不落盘** / 损坏 → 降级 WARNING）、`save_suggestions`（`write_bytes` + mkstemp + `os.replace`）、`update_suggestions(mutator)`（模块 `_SUGGESTIONS_LOCK` 内 读→改→写；并行任务每完成一首整体重写——`voice_config.json` 先例）、`clear_suggestion`（用户确认标签时消费；条目缺失 = no-op 不写盘）；**AI 推荐提示词/解析**（同步单曲端点与批量 worker 共用）：`build_suggestion_prompts(stem, description, registry)`（文件名 stem + 描述〔空 =「（无）」〕+ 三桶词表，「只能从词表中选，禁止创造新词」）、`parse_suggestion_reply`（`extract_json_object`；非对象 → None 重试；逐桶只留**词表内**名字 strip/去重 + 钳上限 `SUGGESTION_CAPS` scene 2 / mood 3 / emotion 2）、**Task worker `suggest_track_tags`**（module `music-ai-tags`；**槽位 = 全任务**——文件/model 快速失败在占槽前、`gate().acquire(stop_check=…)` 后 2 次 LLM（temperature 0.2 / top_p 0.9 / max_tokens 300，`TaskCancelled` 永不重试）、**2 败 → 任务 failed**（UI 提供【重试】——与 `analyze_chapter`「3 败空标签不卡章」有意不同：候选失败不卡任何下游）；成功**只写本曲候选条目**（+ `suggested_at`/`model`）+ progress 1.0；`finally` release——排队取消路径未取槽不到 try） |
| `bgm.py` | 背景音乐引擎（分析 / 匹配 / 混音）。纯函数（rng 可注入）：`sample_chapter_text`（len≤n 原文；否则头/中/尾三窗各 `n//3`、`\n……\n` 连接、首见去重）、`parse_analysis_reply`（复用 `voices.extract_json_object`；每类钳上限 scene 2/mood 3/emotion 2/custom 2；不可解析 → None 重试；**词表外标签保留**——章节标签是自由分析可编辑，与 suggest-tags 严格词表策略有意不同）、`score_track`（同类交集 × 权重；reason 钉死 `"mood 命中 紧张, 热血(+6)；scene 命中 战斗(+2)"` 最重类在前）、`match_chapter`（最高分平分 `rng` 随机；候选排除 prev/next——**同顶层内**排除，全被挡 → 放宽选并注「（相邻章去重放宽）」；无候选 → 通用池同去重；再无 → `music=None`；random 模式忽略标签、score 0、「全章节随机」仍相邻去重）、`build_mix_cmd`（`ffmpeg -y -i 旁白 -stream_loop -1|-0 -i 音乐 -filter_complex [双侧 aresample=44100 + atrim 到旁白时长 + afade（fade 双侧钳 `min(fade, duration/2)`）+ volume] amix=duration=first -c:a libmp3lame`，逐元素可测）、`list_chapter_stems`（02 `*.txt` stem 排序）；JSON 缓存 `load_analysis`/`save_analysis`/`load_assignments`/`save_assignments`（模块 `_BGMS_LOCK` + mkstemp + `os.replace`；缺失不写、损坏降级）；Task worker `analyze_chapter`（module `bgm-analysis`；**槽位 = 全任务**——读文件在占槽前、`gate().acquire(stop_check=…)` 后 3 次 LLM（temperature 0.2 / max_tokens 512，`TaskCancelled` 永不重试）、3 败 → 空标签记录 + WARNING **绝不卡章**、成功只改本 stem 全量重写 + `data["model"]`；`finally` release——排队取消路径未取槽不到 try）、`mix_chapter`（module `bgm-mix`；06 旁白 `.mp3`/`.wav` 兜底、缺 → 「未找到旁白音频…请先完成音频合并」；`music=None` → **`shutil.copy2` 原声**（成品，不走 ffmpeg）；音乐库文件缺 → 「音乐库中找不到…」；`probe_duration` 双路；**`merge_gate` 全程持槽**（`acquire(stop_check=…)` False → `TaskCancelled` 不 release；`finally` kill+wait+close + release）；rc≠0 → RuntimeError 带 stderr 尾 300 字、输出 <1KiB → RuntimeError；**部分产物留盘 `-y` 重跑自愈**）；`match_stems`（排序顺序：locked 章**整条保留** + `skipped_locked` 计数、`matched_at` 不动；prev = 上一章**本次新鲜结果**（在批内，含 locked 的现有值）/ 否则其**现有 assignments 值**（单章重匹配边界——不触碰邻章）；next 恒下一章现有值；tags 快照；`data["mode"]` 持久化；assignments **单次**重写） |

## 流水线逻辑链（需求 → 代码 精确映射）

> 每个阶段给出：页面 → 端点 → 引擎链 → 产物 → 不变量/测试。改需求时先在这里定位链路，再进具体函数。
> 前端页面 = `src/views/<X>.vue`；发起任务的端点一律返回 `{"task_id"}`，UI 经任务 SSE 跟踪（见「任务系统」）。

### 阶段 0 · 开始 / 设置（Dashboard.vue `/dashboard`，Settings.vue `/settings`）

`GET/PUT /api/workspace` → `core.config.set_workspace_pointer` / `init_workspace_config` / `logging_setup.setup_logging`。PUT 顺序：**先 mkdir 校验（400）→ 种子工作空间配置（不覆盖已有）→ 写根指针 → 重指日志**。PUT 空 path = 清除（重新锁定流水线、日志回控制台；不触碰任何文件）。
`GET/PUT /api/config` → `core.config.get_config` / `update_config`（409 守卫；`working_dir` 强制）。设置页把配置 JSON 深拷贝为 draft 编辑，保存 = 部分深合并；主题即时生效、保存才持久化。
前端门控：`useWorkspaceGate().workspaceSet`（读 `settings.config.paths.working_dir`）——7 个流水线页顶部 `WorkspaceGateAlert`、所有动作按钮 `:disabled`、`DirPicker` 整体不渲染。工作空间目录失效（`exists:false`）时**门控不重新锁定**（指针仍非空），仅 Dashboard 显示琥珀警告要求重选——重选后各页自然恢复。

### 阶段 1 · 排版与分册（TextFormat.vue `/text`）

单合并页：排版 → 自动章节分析 → 智能识别 → 分册（按智能识别结果）。操作区**仅 4 个控件、按序**：**开始排版 / 智能识别 / 分册（智能识别结果）/ 前往下一步**；零章节时提示条内「不处理，按整本继续」直接写单个 `<base> 全书.txt`（唯一不经智能识别的分册路径）。普通「按原识别结构分册」（`分册NN` 命名）与「同时打包」zip 开关已从 UI 与 API 移除（2026-09 用户拍板简化）。所有环节均为**同步 HTTP**（无任务 / SSE）；页面状态全部**页面本地**（不经 project store），换选文件 / 重新排版 / 「重新上传原文」都**重置全部下游状态**（陈旧的分析 / 分册 / 智能识别结果绝不存活）；keep-alive 缓存页面、状态跨导航存活。

**排版** `POST /api/text/format {path, config?}` →（同步）`require_workspace` → `read_decoded_file`（`book.decode_buffer` 编码探测）→ `partial_copy(config.text, req.config)` → `text.format_text` → 写 `01_input/<原stem>_排版<ext>`（`write_bytes`，**原件保留**）→ 响应 `{source, encoding, output_path, stats, preview(前2000字), full_length}`。
引擎流水线：CRLF/CR→`\n` → 逐行 `normalize_line`（`keep_single_space` 决定空白折叠）→ `apply_punct`（`…{3,}`/`。{2,}`→`……`、重复 `,，!！?？` 折叠、`--`→`——`、孤立半角标点**仅当邻接 CJK** 才转全角——保护英文/URL）→ `is_chapter_title`（`CHAPTER_RE`：第N章/节/回/卷/集/部/篇/幕/场/折、卷N、Chapter N、楔子/序章/引子…；行 ≤40 字；`detect_chapters` 门控）→ 段落装配状态机（空行=硬边界；`dialogue_separate` 引号行起新段；`sentence_break` 句末断行；章节标题强制起新段；其余行内拼接）→ `punct_quotes` 直引号转弯引 → `"\n\n".join`。
10 个开关（`config.text`）：`keep_single_space`(F) / `sentence_break`(T) / `dialogue_separate`(T) / `detect_chapters`(T) / `punct_ellipsis`(T) / `punct_repeated`(T) / `punct_lone_ascii`(F) / `punct_quotes`(F) / `punct_dash`(F) / `live`(T，UI 即时重排——整条排版 + 分析链重跑)。

**章节分析**（排版完成后由前端自动发起；只读）`POST /api/book/analyze {path}`（**无 409 守卫**；无章节 → HTTP 200 + `error` 字段，文案引用 `EXPECTED_CHAPTER_FORMAT` 并给出两个选项）。
链：`read_decoded_file` → `analyze_text`（`build_newline_positions` → `detect_chapters`（`BOOK_CHAPTER_RE` 多行，**只认「第N章」**——比排版用宽 `CHAPTER_RE` 窄，排版结果的「章节标题」与分册章节表口径不同 + `filter_spurious_chapters` 仅删「孤立下凹」假章 + 铺满区间）→ 每章 `range_char_count`）→ 响应 `{source, encoding, base, total_chars, chapters, chapter_count, filenames, expected_format, sequence, error}`。

**分册** `POST /api/book/split {path, base?, whole_book?, smart?}`（`require_workspace`）：**章节文件分册只按智能识别结果**（`smart=true`，前端「分册（智能识别结果）」按钮）：重跑确定性 `smart_repair`（同输入 → 同结果），逐章 `chapter_content`（**原文单一字节切片**）`write_bytes` 进 `02_split_text/`，写出与 `/smart-split` **逐字节相同**的 `第 NNN 章 标题.txt` 文件——**不产生第二套命名**；普通「按原识别结构分册」路径已从 UI 与 API **移除**（`smart` 与 `whole_book` 均 false → 400），无章节 → **400**；唯一其余路径是显式 `whole_book=true`——零章节提示条「不处理，按整本继续」，写单个 `<base> 全书.txt`。**无 zip**（`as_zip` 参数已移除；引擎 `build_zip` 保留、测试固化，但 API 不再调用）。
遗留命名 `make_chapter_filenames`（引擎函数保留、测试固化，**API 不再调用**）：`<base> 分册{NN:zfill≥2} 第{XXX:zfill w}章.txt`（NN = 1 基**位置**顺序号，宽 `max(2, 章数位数)`；XXX = **原章号**，宽 `max(3, 最大章号位数)`——两宽独立驱动；不可解析章号保留原 `numStr` 不补零；`sanitize_file_name` 换非法字符）——重号靠 分册NN 天然消歧（两个第5章 → `分册01 第005章` / `分册02 第005章`）。`02_split_text/` 当前产物只有智能识别命名与整本文件；旧工程可能有遗留 `分册NN` 产物（不读取、不删除）。
`check_chapter_sequence` 报告缺号/重号/乱序（**非阻断**）：页面琥珀警告逐条列出具体问题（如「缺号：第3章之后缺 第4章」）+ 预期格式 `EXPECTED_CHAPTER_FORMAT` + 两个选择：「不处理，继续」（仅关闭警告；分册仍须先跑智能识别）/「重新上传原文」（整页重置，重跑排版 + 识别）。

**智能识别**（操作区第 2 个控件、分册按钮的前置，用户手动触发；`sequence.hasIssues` 且未执行时琥珀 ring 高亮，**不自动执行**）`POST /api/book/smart-split {path}`（`require_workspace` 409 → 编码探测 400 → 无章节 400）：纯机械、**无 LLM**、确定性、输入只读副本；语义由用户拍板——**永远输出文件 + 修复报告，从不阻断**（疑似合并章即使扫不到标题行也按长度推断拆分并补号，重复章也删除，每个动作在报告中带置信级别供人工核验）。物理顺序 = 唯一真相；字数只用于定位推断切点（切点必吸附段落边界）；重编号恒为物理序 1..N；**文件内标题行保留原样**（不改正错号——价值在文件结构与命名，报告供追溯）。
链：`read_decoded_file` → `analyze_text` → `smart_repair(text, chapters)`（自检失败 → 400 诊断文案，**绝不带病输出**）→ 逐章 `chapter_content` + `write_bytes` 进 `02_split_text/`（「分册（智能识别结果）」按钮重跑时字节覆盖同一套文件；与旧工程遗留 `分册NN` 产物并存时「以最近一次产物为准」）。
修复机制：
- **缺号** → 只重编号（`renumbered` + 下一章 `gap_absorbed`），不拆；
- **异常长章**（chars ≥ 中位数 × `LONG_CHAPTER_RATIO=2.0`；章数 < `MIN_LENGTH_SAMPLES=5` 或中位数 < `MIN_BASELINE_CHARS=200` → 长度检测禁用 + `length_disabled` 警告）：先 `_internal_title_scan` 章内重扫 `BOOK_CHAPTER_RE`（**不做**全局假章过滤）——发现**自身号的重复标题行** = 被吞并的重复章（第二份被 `filter_spurious_chapters` 删掉后并入本章尾部，`filter_spurious_chapters` 只删孤立下凹 → 该标题行必被扫到）：头/尾指纹（`_normalized_fingerprint`）相同 → **截除**重复份（`removed` kind=`truncated`、`duplicate_truncated`，置信 medium）；不同 → **拆分双留**（`duplicate_split`，置信 medium，后段取重复份标题）；否则章号后有缺号区间 → **按长度推断拆分**（`_inferred_cut_points`：基线字长逐段切 k 刀，切点吸附段落边界 ±`INFER_SNAP_TOLERANCE=0.5`×段长、严格单调；章内无空行 → 精确位置 + `inferred_split_mid_paragraph` 警告；置信 **low**，报告注明请人工核对）；**无缺号区间**（末章 / 下一章编号连续、乱序、重号）→ **静默原样保留、零警告**（无缺号时异常长章只是普通长章——老小说长章正常，用户 2026-09 拍板：长度标志本身不是异常）；章号不可解析的长章 → 不拆 + `long_kept` 警告（该异常在报告别处不可见，唯一保留 `long_kept` 的来源）；
- **重号**（顶层）：指纹相同 → **删除后一份**（kind=`dropped`、`duplicate_kept`，置信 medium）；不同 → 全保留、重编号消解（`duplicate_kept`）；**跨号**内容相同 → 仅 `content_collision` 警告不删（顶层标题含章号，实际几乎不可达，防御性保留）；
- **自检**（全过才写文件）：保留章 + 已删 span **铺满 [0, len]**（往返 = 原文逆序挖除已删 span）、段非空、最终号 1..N 无重复。
`status`：全部动作 `kept` 且零警告才 `clean`，否则 `ok`；**对修复后结构再跑 → `clean`（幂等）**。响应 `{status, output_dir, file_count, files, chapters: [{seq, orig_num, orig_numStr, final_num, title, chars, actions, confidence}], report: {actions, warnings, removed: [{seq, num, numStr, title, kind}]}, baseline_chars, original_count, expected_format}`；actions ∈ `inferred_split`/`duplicate_kept`/`duplicate_truncated`/`renumbered`/`gap_absorbed`/`kept`，confidence = high/medium/low（逐章取最低）。
文件名 `make_smart_filenames`：`第 {NNN} 章 {title}.txt`（NNN = 总章数 ≤1000 → 3 位、>1000 → 4 位，001/0001 起；title 空 → `第 {NNN} 章.txt`；`sanitize_file_name` 后判重、冲突追加 ` 2`/` 3`…——**与分册 `分册NN 第XXX章`（无空格、靠 分册NN 消歧）是两套独立命名**，智能识别无 分册NN 前缀、必须自带消歧）。
前端（`TextFormat.vue`，纯页面本地状态）：操作区**仅 4 个控件、按序**——**开始排版 → 智能识别**（lucide `Sparkles`；输入同 analyze/split = 排版产物路径）**→ 分册（智能识别结果）**（标签固定；**智能识别完成前禁用**——`smartEnabled` 的镜像 + `smartResult` 非空）**→ 前往下一步**。「同时打包」zip 开关（`asZip` 状态、两个 API 调用的 `asZip` 参数、结果卡 zip 下载按钮）与普通分册 / 「分册（整本）」armed 状态**已整体移除**。零章节提示条：「不处理，按整本继续」**直接执行** `whole_book` 分册（无 armed 中间态；完成后提示条切换「已按整本处理」info 注记 + 分册结果卡显单个整本文件）/「重新上传原文」整页重置。智能识别结果卡（状态/原章节数/最终章节数/基线 统计条；`report.warnings` 逐条琥珀 Alert；`report.removed` 逐条 info Alert；修复记录表 #/原号/新号/标题/操作/置信 Badge（`ScrollArea h-64`）；输出文件表；脚注「文件内标题行保留原样（未改写正文）……推断拆分（低置信）与已删除章节请对照上方报告人工核对」）。**智能识别成功后覆盖原始排版识别**：`analysis` 状态整体替换为修复后的 1..N 结构（chapters/filenames/sequence 全按修复结果重建，`sequence.hasIssues=false` → 序号警告自然消失；章节表「分册文件」列显示智能识别文件名；章节分析卡头部注记「已按智能识别修复结果覆盖（原 N 章 → M 章）」）——「分册（智能识别结果）」按钮启用（带 `smart=true` 调 `/split`），分册产物 = 与智能识别同一套 `第 NNN 章 标题.txt` 文件。`goNext()` guard = **分册或智能识别任一完成**（产物同落 `02_split_text/`，下游解析页直接列目录）。
不变量与测试：排版不变量 → `test_text.py`；分册与智能识别不变量见核心约定 #8 → `test_book.py`。「前往下一步（文本解析）」→ `/script`（guard：分册或智能识别已完成）。

### 阶段 2 · 文本解析（ScriptParse.vue `/script`）

**解析** `POST /api/script/generate-files {files: [02_split_text 裸文件名]}` → **每文件一个独立 Task**（module `script`）：`create(..., start=False)` 建 **PENDING 壳**（已注册 = 可见于任务列表 / SSE 保流判定 / F5 恢复，计入非终态；不起线程）→ 批次注册（模块级 `_BATCHES`：`ParseBatch{id, ordered = 勾选序 task_ids, stop}` + 反向映射 `_TASK_BATCH`）→ 每批起一个协调者线程（`_run_coordinator`，daemon，0.2s 轮询；`set_concurrency(generation.max_concurrency)` 共享闸门不变）→ 响应 `{task_ids, files:[{file, task_id}]}`（形状不变，前端绑定零改动）。
协调者按**勾选顺序投放**：每轮算 `waiting = max(0, no_slot − gate().active)`（no_slot = 批内 RUNNING/PAUSED 且 `phase != "check"` 的任务数）→ `while waiting < PREFETCH_DEPTH=4` 按序 `start` 下一个 PENDING 壳（吞 `ValueError/KeyError` = start/cancel 竞态）→ **不变量 = 「等待槽的任务数 ≤ 4」（稳态 = C 个持槽解析 + 4 个预取）**，而非「在跑 ≤ C+4」（检查期任务既不持槽、也不占预取名额；槽位释放瞬间即有预取补位，槽不空转）；终止 = 批次注销 / `stop` 置位（→ 就地 cancel 剩余 PENDING 壳）/ 全部任务终态 → `finally` 注销批次与反向映射（注册表为进程内存态、重启即失——见「已知陷阱」）。
**批量取消** `POST /api/script/cancel-batch {task_ids}`（同步、非任务）：对入参任务涉及的每个唯一批次置 `stop`（停止投放）+ 就地 cancel 该批剩余 PENDING 壳（不等协调者 0.2s）→ 再逐个 `control(tid, "cancel")`（仅非终态；检查期任务照常协作取消）；幂等（终态 / 未知 id 忽略、不抛）→ 响应 `{cancelled, batches_stopped}`。
`script.generate_file`（Task worker）链（**槽位作用域 = 仅 LLM 分段解析阶段**）：`llm.model_name` 空 → 快速失败 → **预备阶段（无槽）**：读文件 `decode_buffer` → `fix_mojibake`（CP1252-as-UTF8 乱码替换表）→ `split_into_chunks(chunk_size=3000)`（**2026-09 新算法**：坐标系 = 段落 strip 后 `"\n\n"` 拼接串 S（chunk 恒为 S 切片、切后 strip；无损口径 = 去空白后拼接相等）→ **段数** `ceil(total/size)`（过短尾段 `total−(count−1)·size < size/2` → 减一重新平均；再钳到 内部边界数+1，目标切点找不到边界时）→ **逐目标位** `total·k/count` 在统一边界表（一个极大空白游程 = 一个边界：段落 `\n\s*\n` > 换行 `\n` > 句末〔CJK `(?<=[。！？!?…])` 零宽——旧正则对中文从不触发、本次补齐；ASCII `(?<=[.!?])` 须后随空白——保护 `3.14`/`e.g.`〕）取**最近合法边界**（目标的前驱/后继各至多 1 个、严格单调、平手取较小偏移；**悬题改判**：会让前块以章标题结尾的候选让位；完全没有合法边界 → 不切、允许 chunk 超 `chunk_size`——**绝不拦腰截断句子/对话**）→ **收尾过短块消除**（<`size/2` 的块与较短邻边合并 = **只删已有切点**，永不新增/重切、不跨章标题行；两侧都不可并 → 保留短块（允许，非异常）；只删不增 → 必然终止、无震荡）；`chunk_size≤0` 钳 1；**章标题防丢** = 搜索期切点改判 + `_evict_trailing_title`（尾部 100 字窗）兜底 + carry 移交（标题与它统领的章节正文同段，不孤悬在失败/截断 chunk 的尾部；书末独占块标题独立成块）→ `handle.check()`（排队前到达的取消）→ **LLM 阶段（持槽）**：`handle.progress(0.0, "排队中（等待并发槽位）")`（文案不可改——前端 `/排队/` 正则依赖）→ `if not gate().acquire(stop_check=lambda: handle.cancelled): raise TaskCancelled()`（0.2s 协作等待，排队中被取消一个轮询周期内中止、未取槽）→ `handle.phase("parse")` → 逐 chunk `process_chunk`（progress = `0.9·(i−1)/N`）：
- context = 段位置标记（`Beginning of text` / `Part n of m` / `End of text`）+ 跨 chunk 角色名册（sorted 去重、剔除 NARRATOR）+ 上段末 3 条；user 模板 `{context}`/`{chunk}` 占位（`str.format`）；
- LLM = `urllib` OpenAI 兼容调用（`llm.stream` 决定流式与否；流式走双缓冲：`reasoning_content`+`content` 进 UI「流式反馈」，返回值只含 `content` —— 与非流式**字节级一致**；合流节流 0.12s/256 字符 → `handle.llm_chunk` + `handle.llm_rate`；**每 SSE 帧后查取消**）；
- 最多 3 次尝试（`max_retries=2`）：调用异常 / JSON 不可解析 → 重试；`clean_json_string`（剥围栏/思维标签/括号计数截取/控制字符转义）→ `repair_json_array`（补逗号/去尾逗号/丢非对象条目）→ 失败再 `salvage_json_entries` 正则抢救；**`TaskCancelled` 永不重试直接上抛**；
- **逐 chunk 忠实性校验 + 恢复**（拿到可解析响应之后）：`check_chunk_fidelity` —— 源 chunk 中所有 ≥4 词字符的引语段的**骨架**（只留词字符，去空白/标点/引号）必须能在输出条目 text 的拼接骨架中作子串找到：对提示词允许的引号剥离/标签并入等编辑天然免疫误报，而整块截断（JSON 尾部被截）与零星丢行都必然被抓住；任一缺失 → **阶梯第 1 步：把 max_tokens 临时翻倍再跑一次**（截断是最常见根因），通过则采纳（仍缺失但内容更全则以翻倍结果为准）；仍缺失 → **阶梯第 2 步：`split_chunk_balanced`（段落边界 > 换行 > 句末标点，尽量近中点、绝不拦腰切断）把 chunk 对半切开，两半各再跑一次**（右半继承左半条目作前文上下文；**半段不再升级——阶梯到此为止，无递归切分**）——阶梯恒为这两级，成本有界、不循环失控；不可切分 / 仍救不回 → 保留最好结果 + WARNING，**绝不静默丢弃**；
- 单 chunk 全败 → 返回 `[]`（**不抛异常**，仅日志）；**所有** chunk 皆空才文件级 `RuntimeError("未生成任何脚本条目。")`。
chunk 循环过 `if not all_entries` 守卫后：`gate().release()` + `slot_released = True` → `handle.phase("check")` + `handle.progress(0.9, "机械检查（角色匹配检查 / 断句校验 / 标签删除 / 超长段落检查 / 归属抽样 / 纯标点吸收）")` → **检查阶段（无槽）**：解析内各检查/后处理阶段 + 写基文件 + 历史全部在槽外执行——检查阶段的 LLM 调用（边界重判 / 断句重判 / 超长重切 / 归属抽样）**不计入并发上限**（有意语义）；阶段顺序 = 角色匹配检查 → 断句校验 → 标签删除 → 超长段落检查（LLM 重切）→ 归属抽样 → 纯标点吸收 → **同人段落合并** → 超长段落机械分段（**硬保证终态**）；开关读自 `generation`：`check_boundary_speakers` / `revalidate_splits` / `delete_saying_tags` / `check_long_paragraphs` / `spot_check_rate` / `absorb_punct_entries` / `merge_same_speaker`——任一关闭 = 整体跳过该阶段 + 一行「…已关闭（配置）」日志，结果字段保持 0。槽位配平：`finally: if not slot_released: gate().release()`（每条退出路径恰一次 release；`stop_check` 弃权路径从未取槽、不 release）。**progress 重标度**：100% = 仅完成态；解析阶段 [0, 0.9)；角色匹配检查 [0.9, 0.96)（`0.9 + 0.06·seq/len`）；断句校验与超长重切**共用** [0.96, 0.98)（`0.96 + 0.02·seq/len(flagged)`，两阶段先后执行、时间互斥）；归属抽样 [0.98, 0.998)（`0.98 + 0.018·seq/len`）；阶段被跳过（配置）→ progress 保持前值。
**角色匹配检查**（检查段最前——断句校验 1→2 拆条 / 标签删除删条都会移动下标，本阶段窗口必须按 **pristine**（chunk 循环刚结束的原始）条目列表预建；受 `generation.check_boundary_speakers` 门控，见上开关说明）：chunk 切割把跨段上下文切断，边界两侧条目解析时看不到另一侧 → 每个**内部** chunk 边界两侧各 `check_context_window` 条（`select_boundary_targets` 全局去重排序；文件头/尾不查——首/末段解析时 LLM 明确知道 Beginning/End of text，无被切掉的上下文）→ **复用捆绑重判提示词与共享重判批协议**（`_run_rejudge_groups`，与归属抽样同一执行器；几何 `check_batch_size` / `check_context_window`；目标按 `group_retry_indices` 分组、组内非目标条经 `skip` 作未标记上下文）：窗口 = [前置上下文]+[目标]+[后置上下文]，**严格区分 target 与 context**——context 仅提供判断依据、**永不被修改**，只有 target 可被改写（提示词明令 + 应用代码只写 target 下标 + 窗口恒按原始列表预建，三层保证）；首判 → 分歧条目动态多数投票 [原值, 首判] + 至多 3 次同窗口重试、严格多数一出即停、无共识保留原值（**从不猜**）；**只改 `speaker`** + 台词「仅去外层引号」的机械值严格采纳（NARRATOR 终值不剥）；**取消上抛、不落盘任何文件**；**单 chunk 文件 → 静默跳过；零目标 → 一行日志「角色匹配检查：0 条边界目标（无重判，零 LLM 调用）」**（防误读为阶段缺失）；结果字段 `boundary_checked` / `boundary_fixed`。
**断句失败校验**（机械合并之前——拆出的旁白段随后照常合并；受 `generation.revalidate_splits` 门控，见上开关说明）：条目 text 被外层双引号（弯/直任意形式）整体包裹、且引号跨度内出现「…道 + **冒号**」语气标签 → 疑似断句失败（标签被包进台词，下游 TTS 会照原样念出、再无环节纠正，只有解析阶段能就地重推）→ 逐条**重跑解析 LLM 做校验**：条目 text 进解析提示词的 `{chunk}` 槽、`build_batch_window` ±`generation.check_context_window` 条的上下文窗口进 `{context}`（user 提示注明 re-check + 注入全书角色花名册）——**复用解析提示词，无新提示词文件**；每条按**共享重判批协议的多者胜规则**裁决（基础 1 次 + 重试 2 次 = 合计 3 次，严格多数一出立即停（2:0 / 2:1），3 次无共识才再跑第 4 次）；每个回复先过**忠实性门**才计票——各段词字符骨架拼接 ∈ {原文骨架, 剥掉**开头**「…道：」标签后的骨架}（对提示词允许的引号/冒号编辑天然免疫，增/删/乱序词字符必被抓）/ 各段 speaker ∈ `NARRATOR` ∪ 全书花名册 / 多段需 ≥2 个不同 speaker——调用失败 / 响应不可解析 / 未过门均**无票**；严格多数胜出 → 条目被**整体替换**为重新推导的条目（1 段胜出 = 干净重写（剥外层包裹 / 开头标签），多段 = 重新拆分），4 次仍无共识 → 条目保持原样（**从不猜**）。花名册与全部上下文窗口按**原始**条目列表预建、胜出者按**降序下标**应用（拆分不会移动尚未处理条目的窗口下标）；取消立即上抛（文件在此步返回后才写）。LLM 调用复用解析传输（`_llm_call` = `script.py` 模块内的重判 LLM 调用封装，流式照常进 UI 面板）；**0 条命中 → 打一行日志「断句校验：0 条疑似断句失败条目（无重判，零 LLM 调用）」后直接返回**（静默快速路径会让用户误以为阶段缺失，故留痕）。
**纯归属标签条清理**（断句校验之后、归属抽样之前——抽样重判可能把标签条改判成角色 speaker，使本规则不再适用，且角色会用本人声音念第三人称标签；确定性、**零 LLM 成本**；受 `generation.delete_saying_tags` 门控（见上开关说明））：整条内容就是纯归属标签的条目（老道瞪眼怒道。/ 杜尘暗喜，急道。/ 史蒂夫解释道。——无引号、无冒号，断句校验的机械触发器不可见；若保留会成 TTS 的独立旁白行，多一次同人停顿 + 换人停顿，悬在它引入的台词前/后）→ **删除**。删除 = 六条件合取：① `NARRATOR` ② 无引号（模块内 10 字符引号表 `_QUOTE_CHARS`，随重判批协议搬入）③ `len(text.strip()) ≤ 10`（`PURE_SAY_TAG_MAX_LEN`；阈值 = 纯标签与「叙述 + 标签」混合条的分界）④ 剥掉末尾标点后末字 ∈ 五归属动词（与归属抽样同一 `_SAY_VERBS`；「道」前一字符 ∈ 知/难 → 非标签，与归属抽样同一形态守卫）⑤ 非章标题（与机械合并同一 `is_chapter_title`）⑥ **紧邻**（前或后一条）对白条目（非空 speaker ≠ `NARRATOR`）。**非级联**单遍（邻接按删除前列表判定：隔一条的标签保留——代码无法区分纯标签与恰好以动词收尾的短叙述句，保守留）；删除**不会**制造新的 NARRATOR/NARRATOR 相邻对（邻接条件保证一侧是台词条）→ 下游机械合并不受影响。命中 >0 任务日志一行（含 ≤3 个被删例），0 命中也留一行（与断句校验同——静默退出会被误读成阶段缺失）；结果新增 `tags_deleted` 字段。
**超长段落检查（LLM 重切，阶段 A 前半）**（标签删除之后、归属抽样之前——重切可拆出带新 speaker 的条目，需被归属抽样审计；受 `generation.check_long_paragraphs` 门控，几何取 `check_context_window`，见上开关说明）：解析产物里 strip 后 > `generation.max_paragraph_chars`（默认 **200** 字）的条目（`long_entry_indices`）= 疑似文本切割失败（旁白与对白、或数段正文被并进一条）→ **逐条带上下文窗口重跑解析 LLM 重切**（与断句校验同形同协议：条目 text 进解析提示词 `{chunk}` 槽、context 首行注记「the SOURCE TEXT below is a single entry's stored text that is **LONGER than N characters**」+ 全书花名册 + ±`check_context_window` 条窗口；复用解析提示词，**无新提示词文件**）；每条按共享重判协议多者胜裁决（`revalidate_entry(stage="超长段落重切")`，1+2 次早停、无共识第 4 次）；忠实性门 = 各段词字符骨架拼接 ∈ {原条目骨架, 剥开头「…道：」标签后骨架} + speaker ∈ 花名册∪NARRATOR + **多段需 ≥2 个不同 speaker**（同人长独白的「拆分」无票 → 条目原样保留，交给机械分段兜底——**LLM 管语义边界、机械管长度上界**的分工）；胜出 → 条目**整体替换**为重新推导的条目（降序应用）；取消上抛、不落盘。零命中 → 一行日志「超长段落检查：0 条超过 N 字条目（无 LLM 重切，零 LLM 调用）」；progress 带 [0.96, 0.98)（与断句校验时间互斥）；结果字段 `long_checked`（目标条数）/ `long_fixed`（经投票改写条数）——开关关闭或零命中保持 0。
**归属抽样**（断句校验之后、机械合并之前——重判可把 NARRATOR 条改成角色条，合并必须看到改后 speaker；`generation.spot_check_rate` 默认 0.05，0 = 关闭）：所有 chunk 解析完后，从**全部**条目按两桶**不相交**抽样重判 speaker——总预算 `max(1, round(rate·N))` 封顶 N，其中 ~1/3 **纯随机**（全条目均匀，= 整书错误率的无偏「仪表」，唯一可据以判断「采样率能不能降」的读数）+ ~2/3 **风险加权**（按命中风险特征数 3→2→1→0 **级联**抽取、tier 内均匀随机，tier 0 = 未抽中剩余、预算恒用满）。风险特征（代码可判）：① 无显式归属标签——条目自身及紧邻前一条（仅当其为 NARRATOR）无「2~3 字 CJK 人名 + 说/道/问/喊/答」或「他/她 + 五动词」（`道` 前一字符为 知/难 = 知道/难道 的**形态守卫**）；② 极短 `len(text.strip()) ≤ 10`；③ 多角色场景——±10 条窗口内 ≥4 个不同非 NARRATOR 说话人。重判**复用捆绑重判提示词与共享批协议**（`check_prompts` 捆绑默认，**不再用户可配**；几何取 `generation.check_batch_size` / `check_context_window`；目标按 `group_retry_indices` 分组 = 每次调用一个窗口、组内非目标条作未标记上下文；首判 → 分歧条目动态多数投票 [原值, 首判] + 至多 3 次同窗口重试、严格多数一出即停、无共识保留原值；**只改 `speaker`** + 台词「仅去外层引号」的机械值严格采纳，NARRATOR 终值不剥）——**无新提示词文件**；高置信改判在内存中生效、随解析任务自己的基文件写出（不违反不变量 #9——基文件本就是解析任务自己的产物）；**取消上抛、不落盘任何文件**（基文件与历史都只在阶段整体返回后写）。每本的**纯随机桶**首判分歧率 = 错误读数 → 任务日志一行 + 基文件落盘成功后追加 `<workspace>/config/spot_check_history.json`（`{"runs":[{file, ts, rate, random:{n,errors,rate}, risk:{n,errors}}]}`，模块锁读-改-写、保留最近 50 本、`write_bytes` 全量重写、写失败仅 WARNING）——**采样率永不自动降**，由用户在设置页按读数手动调（风险桶读数天然偏高，**不用于**判断能否降率）。
**纯标点条目吸收（阶段 B）**（归属抽样之后、机械分段之前——重判可把条目改成 NARRATOR，吸收必须看到终态 speaker；确定性、**零 LLM 成本**；受 `generation.absorb_punct_entries` 门控，见上开关说明）：`_skeleton`（只留词字符）为空的条目（独立「……」/「？」/空文本——报告里 32 段纯标点 NARRATOR 的根因：保留会成 TTS 独立旁白行、多两次停顿）→ **并入相邻 NARRATOR 条目**：目标 = (前邻, 后邻) 中首个**非章标题**的 NARRATOR 条（前邻优先，标题守卫与机械合并同一 `is_chapter_title`）；并入前邻 = 追加到其尾、并入后邻 = 置于其开头；**无 NARRATOR 邻接 → 删除**（零内容条目，TTS 无意义）；`consumed` 集防链式丢文本（目标本身是纯标点条时留盘承载合并文本、跳过其自身处理）；非级联单遍、输入列表不动。零命中 → 一行日志「纯标点条目吸收：0 条无内容条目（无吸收 / 无删除，零 LLM 调用）」；结果字段 `punct_absorbed` / `punct_deleted`（开关关闭保持 0）。吸收可把邻接 NARRATOR 推过 200 字 → 由下阶段机械分段兜底（**顺序有意**：B 在前、A-mech 在后）。
**超长段落机械分段（阶段 A 后半，硬保证终态）**（纯标点吸收与**同人段落合并**之后——speaker 已定稿、且同人合并的 ≤10 强制合并可能造出 > 上限的块，由本阶段切回；与 LLM 重切同一 `check_long_paragraphs` 开关——关闭 = 两半都跳过）：仍 > `max_paragraph_chars` 字的条目（LLM 重切失败/无票/未改短的 / 同人合并造出的超限块）→ `split_long_entries` 确定性切开：`split_long_text` 三级边界逐级放宽——①句末（CJK 零宽 / ASCII 须后随空白，与 `split_into_chunks` 同口径）②子句界（`[，、；：,;:]`）③定宽硬切（**硬约束「绝不出现超长条目」高于「不拦腰截句」**——切点先查 ±20 字窗引号安全位、找不到才裸切）；①② 切点须引号安全（`_quote_parity` 前缀开−闭引号奇偶与起点一致，不安全让位下一级）；tier 内取**最接近目标宽度** `start+max_chars` 的候选（平手取较小偏移）；不变量 = 各段去空白拼接骨架 == 原文骨架 + 每段 ≤ 上限 + 切点严格前进必然终止；切段**继承父条目 speaker**、`instruct` 只给首段、非级联单遍；`max_chars ≤ 0` 钳 10。**硬保证**：本阶段是检查段最后一步——其前的同人段落合并（≤10 强制合并可造出 > 上限块）已定稿，切出的段**其后无合并**、绝不被回粘，开关开时基文件绝无 > 上限的条目；命中日志注明「硬保证：最终无 N 字以上条目」，零命中也留一行（「0 条仍超 N 字（无分段；硬保证已满足）」）。结果字段 `long_split`（机械切开的条数）。
chunk 循环后**机械后处理** `merge_adjacent_same_speaker`（确定性，不经 LLM；受 `generation.merge_same_speaker` 门控，关 = 跳过 + 一行「同人段落合并已关闭（配置）」日志、`merged_same_speaker` 保持 0）：连续**同 speaker** 条目合并为一条（**NARRATOR 与角色同规则**——旧版仅 NARRATOR 的 `merge_adjacent_narrator` 已替换；去掉 TTS 会在相邻同人段间多插的停顿与段边界）。**字数口径 = 词字符数**（`len(_skeleton(text))`，只留词字符、不计标点/空格/换行——与 200 字硬保证的「原文长度」口径**不同，勿统一**）。**合并条件**（吸收 `nxt` 进运行块）：同人 speaker **且** `nxt.text` 非空 **且** 块尾与 `nxt` 均非章标题（`title_test`）**且**〔块词字符 + `nxt` 词字符 ≤ **100**（`max_chars`）或**较短一方 ≤ 10**（`short_cap`，**强制合并**——即使合并后 >100）〕。**≥3 段同人 = 左→右贪心**：维护运行块，后续段满足任一条件即并入、否则封块以该段开新块。**合并产物**：speaker 不变；text 首尾相接、**边界若前段尾（`.rstrip()` 后）无收尾标点（`ends_sentence`）则补一个「。」**（逐边界判定，除此之外不改任何字符）；instruct 取**块内词字符最多**的成员（tie → 最左；该成员无 instruct 键则删键、不凭空造值）。**章标题守卫恒判无豁免**：每次吸收前都对运行块文本重跑 `title_test`（旧「已合并块免判」豁免作废——反例 `"楔"+"子"` 各自非标题、强制合并成章标题 `"楔子"`）；`is_chapter_title` >40 字短路、成本可忽略。**顺序承重**：本阶段在超长机械分段**之前**——≤10 强制合并可造出 > `max_paragraph_chars` 的块，由其后机械分段切回（200 字硬保证不变）；顺序反了会回粘机械分段自己切出的同人短段。**返回** `(新列表, 合并对数=吸收步数)`；输入列表不动（浅拷贝）。
产物：`03_parsed_json/<源文件 stem>.json`（一源一文件）= JSON 数组 `[{speaker, text, instruct}]`——`speaker` = 全角大写角色名或 `NARRATOR`（非对话）；`text` = TTS 实际朗读文本；`instruct` = 1–2 句声音指导。返回 `{entries, output_path, count, merged_same_speaker, boundary_checked, boundary_fixed, suspicious, suspicious_fixed, tags_deleted, long_checked, long_fixed, long_split, punct_absorbed, punct_deleted, speakers, input_chars, spot_checked, spot_fixed, spot_rate, spot_random_n, spot_random_errors, spot_random_rate}`（`merged_same_speaker` = 同人段落合并的连续同 speaker 条目对数（吸收步数；`merge_same_speaker` 关时为 0）；`boundary_checked` / `boundary_fixed` = 角色匹配检查的目标条数 / 经投票改写条数——开关关闭或单 chunk 时保持 0；`suspicious` = 检出疑似断句失败条数、`suspicious_fixed` = 经投票改写条数、`tags_deleted` = 确定性删除的独立纯归属标签条数——对应开关关闭时保持 0；`long_checked` / `long_fixed` = 超长段落检查的目标条数 / 经 LLM 重切投票改写条数、`long_split` = 机械分段切开的条数（`check_long_paragraphs` 关时全 0）、`punct_absorbed` / `punct_deleted` = 并入相邻 NARRATOR 的纯标点条数 / 无邻接删除条数（`absorb_punct_entries` 关时全 0）；`spot_*` = 归属抽样的抽查数 / 更正数 / 采样率 / 纯随机桶 n / 错误数 / 错误率（空桶 → null））。

遥测（`TaskHandle` → SSE）：`llm_chunk`（流式原文，Task 侧 128KB 字符上限、丢最旧，重连可回放）；`llm_rate`（`cps` 瞬时 + `cps10` = 后端按真实流式字符算的 **10 秒窗均值**（`RATE_WINDOW=10.0`，`(c末-c首)/(t末-t首)`，无偏）→ 吞吐量卡）；`llm_chars`/`llm_secs`（每完成一个 chunk 上报累计原文字数 + 冻结处理时长 → 处理速度表，chunk 之间不衰减）。
测试：`test_script.py`（切分〔2026-09 新算法：无损拼接/段数公式四例〔3100@1500→2 段均分、3800→3、4500→3×1500、4600→3 段均分〕/边界钳制/过短尾段减一/CJK 零宽句边界 + ASCII 词内点号保护/短块合并只删切点/标题恒随正文同段/书末标题独立块保留〕/修复/流式字节一致/取消穿透/解析内六阶段开关门与 e2e（含关闭语义：零 LLM 调用 + 「已关闭（配置）」日志 + 结果字段 0）/**角色匹配检查**（`select_boundary_targets` 单元〔n=0/单段/两侧各 n 全局去重/边界 clamp〕、单 chunk 静默零日志零调用、零目标留痕、开关关 e2e（零 LLM 调用 + 「已关闭（配置）」日志 + 字段 0 + 基文件原样）、**跨 chunk 窗口实锤**（窗口跨边界、首尾为另一侧 chunk 上下文条、context 条无 target 键、基文件 == 原始条目 = context 逐字节未改）、2:1 多数采纳随基文件写出、4 异无共识保留原对象 + 「重试 3 次仍无共识」日志、取消零落盘、**pristine 不变式**（第 1 组修正不改变第 2 组窗口、组内 target/skip 语义）、**顺序 e2e**（边界窗口取自 pristine 列表、断句窗口取自边界修正后列表；调用数钉死 6））/**重判批协议**（自已退役的独立检查模块整体搬入：`build_batch_window` 窗口/边界 clamp/`skip` 语义、`parse_speaker` 多形态、`parse_speaker_map_full` text 捕获、`strip_outer_quotes` 6 种引号对、`_pick_majority` 严格多数、`group_retry_indices` 间距 + 批上限〕/**超长段落检查 + 纯标点吸收**（`split_long_text` 单元〔五级行为：短文本原样 / 句末取最接近目标宽度 / 子句界 / 定宽硬切 / 唯一边界远离目标仍取之 / ASCII `3.14` 保护 / 引号 parity 让位〔句末切点在引号内不安全 → 让位定宽 ±20 窗安全位 / 整条引号包裹 → 裸定宽〕/ `max_chars≤0` 钳 10 / 骨架无损 + ≤ 上限不变量〕、`_quote_parity` / `long_entry_indices` / `split_long_entries` 单元〔speaker 继承 + instruct 只首段 / 零命中原对象 / 输入不动〕、`absorb_punct_entries` 单元〔前邻 / 后邻 / 删除 / 标题守卫双向 / 链 / 双向累加 / 零命中原对象〕、A-LLM〔零命中精确日志 + 零调用、2:0 多数替换 + 窗口注记 `LONGER than 200 characters` + 花名册、4 次未过门（花名册外角色）保留原对象 + 「超长段落重切4 次仍无共识」日志、取消上抛零调用〕、**A 段 e2e 双路径**（LLM 重切：1 解析 + 2 重切 = 3 调用、`long_split==0`、基文件全 ≤200 字；机械兜底：重切回复 = 原样单条 → 投票采纳后仍超长 → `long_split==1`、speaker 继承、骨架无损、`merged_same_speaker==0`〔合并已在切段之前，切段是末段产物、合并看不到 → 不回粘〕；`check_long_paragraphs=False` e2e → 零重切调用 + 超长条原样落盘 + 字段 0 + 「已关闭（配置）」日志）、**B 段 e2e**（解析产物含独立「……」NARRATOR 条 → `punct_absorbed==1` 拼入前邻；`absorb_punct_entries=False` → 原样保留〔对照开关开 = `punct_deleted==1`〕）、**同人段落合并**（`merge_adjacent_same_speaker` 单元：连续同人并 1 条 / 链式合并取最左 instruct / 章标题两侧不并 / 词字符口径超上限不并 / ≤10 强制合并造 >100 块 / 多成员贪心（块+段≤100 并入 / 较短≤10 并入 / 否则封块）/ 章标题反例「楔。子」重跑 `title_test` 拦截 / instruct 取词字符多者；**顺序 e2e**：≤10 强制合并（5≤10、215>100 仅强制路径）造 216 字同人块 → 末段机械分段切回 [200,16]、`merged_same_speaker==1` + `long_split==1` + 全 ≤200 + 骨架无损——钉死「合并在切段前」）〕；槽作用域 / 排队取消，见「测试」段）、`test_script_batch.py`（批投放：PENDING 壳按序 / 预取上限与严格顺序 / 检查期释放预取余量 / 取消全部停投放 / 幂等）、`test_llm_rate_window.py`。

### 阶段 3 · 角色配音（Voices.vue `/voices`）

**两阶段、互斥**（前端禁用另一阶段按钮——LLM 与 TTS 不抢显存）；无独立创建/删除端点，档案由两阶段 Task 整体重写。

**阶段 1（纯 LLM）** `POST /api/tts/prepare-foundations {speakers?, new_only, overrides?, script?}` → `voices.prepare_foundations`（module `voices-foundation`）：`_load_script`（`__all__` → `resolve_parsed_json_all()` **直读基文件**逐文件拼接，单文件损坏仅 WARNING 跳过）→ 证据采样（`_select_target_bands`：≤24 句全进 front；否则 front 8 + back 8 + 中段均匀 8，每条带 `±4` 邻句窗口、目标行标 `★`）→ `ThreadPoolExecutor(max_concurrency)` 并行 `_llm_persona`（复用 LLM 传输；`temperature=0.3`、`max_tokens=1024`；提示词 `config.persona_prompts.*` 空则回退 `persona_prompts.py` 内置；`str.replace` 填 `{speaker}`/`{line_windows}` 防大括号炸裂）→ `extract_json_object`（手写括号配对扫描）→ `{description, ref_text(40-60字)}` → 全败 → `_fallback_persona`（固定描述 + `pick_ref_text`）→ `_fold_aliases`（归一化精确 → 子串 → Jaccard≥阈值，写 `alias_of`）→ **每完成一个角色就整体重写** `voice_config.json`（页面实时刷新）。本阶段**不启动任何 TTS 子进程**。

**阶段 2（纯 TTS）** `POST /api/tts/make-clones {speakers?, new_only, concurrency?, script?, candidate_count?}` → `voices.make_clones`（module `voices-clone`）：选有 foundation 且非 alias 的角色 → 每角色目标候选数 = 固定 `candidate_count`（None=自动 / 2 / 4 / 6 / 8，请求校验）或 `auto_candidate_count(有效台词数)`（**绝对对数分档，无工程相对分母**——旁白可达主角 10 倍量级，相对比例必把主角压成 2~3 条；改按角色**自身**台词量级分档：`<20` 龙套=1，`20+`≥2，≈100→3、≈200→4、≈500→5、≈1000→6、≈2000→7、≈2300+→8，即 `int(-2.1 + 3·log10(台词数))` 钳 1..8）（**别名台词归并进 canonical**（`_effective_line_counts`，沿 `alias_of` 链 ≤8 跳防环）——`speakers`/`new_only` 过滤不移位，单角色重做与整批同预算）→ `new_only` 只重做「已有候选数 < 目标」的角色 → **每 (角色, k) 一个作业**（`jobs` 池；产物路径 `04_voice_profiles/designed_voices/<_sanitize(speaker)>_<ns>_c{k}.wav` 运行前定死——每角色一个 `time.time_ns()` ns 防 `_sanitize` 文件名碰撞，**跨重启稳定**）→ **断点采纳**：盘上已有候选（WAV ≥1024B，坏头守卫）直接采纳（seed 记 −1，不重渲染）；**全部已采纳 → 零作业短路**（结算 + 保存 + progress 1.0，不启动引擎）→ `base_seed = secrets.randbelow(2**31)` **整个运行只算一次**（跨重启稳定）→ `rows_cap = clamp_concurrency(concurrency or config.tts.batch_concurrency)`（缺省 4）→ **一个长驻 worker 子进程**（`run_worker` + `--mode design-batch` + `--disabled-checks`（五道规划检查与音频合成共用 `config.tts.planner_*` 开关，全开时省略该参数，见阶段 4）；模型只加载一次；候选行**跨角色统一队列**组 tensor 子批（全部候选一条长度升序队列、不同角色的同长度候选一起凑满批内行数上限）+ `VramGovernor`——长度分档 + 总字符上限 + 实测显存反馈，逐行 instruct；`base_seed + 子批序号` 播种，**同子批候选共享 seed 但仍互异**（各行独立采样），重跑互异靠 run 级 `base_seed`）→ 作业清单写 `00_temp/design_jobs_*.json`（绝对路径，用完删）→ `remaining` = 未排除、未 ok **且属未结算角色**的作业（看门狗重渲染不会在已结算 entry 下留孤儿文件；未结算角色的失败作业重启时重做）→ **角色级原子结算**：某角色全部候选作业落定才整体写一次 entry（在途角色保持运行前状态；**取消时未结算角色文件不动**）→ 成功（≥1 候选，按 k 排序重编号 1..m）：entry = `{type: clone, ref_audio(工作空间相对), candidates: [{id, ref_audio, seed}], selected_audio_id: None, ref_text, description, character_style, clone_status: done}`；全失败：`{type: design, candidates: [], selected_audio_id: None, clone_status: failed}` 但**顶层 `ref_audio` 保留**（最后已知良好的音频仍可试听；**单角色失败不中断整批**）。**不变量：顶层 `ref_audio` ≡ 当前生效候选**（已选 = 选中项，未选 = 第 1 条）——worker batch 只读顶层 `ref_audio`/`ref_text`，状态推断与路径迁移也都只认它 → 保持同步则 worker / tts_batch / merge / pathio **零改动**。**看门狗重启循环**（退出码 124，与音频合成同形）：`rows_cap>1` → 缩批一半重启；`rows_cap==1` → 对 `[watchdog]` 行里的 (角色,k) 行记罚，**两次超时 → 隔离**（不再渲染，记 `超时（已隔离）`，其余继续）；`attempt>8` → RuntimeError（已完成进度已保住，提示调小「批内行数」）。进度由 worker 的 `[progress]` 行驱动（`(offset+done)/total`，`--done-offset` = 已落定作业数 → **重启后不回跳**；后端只在终末发「完成」）。取消：`run_worker` 主循环每 0.15s `handle.check()` + finally 杀**进程树**（Windows `taskkill /F /T`），已生成 WAV 留盘成孤儿（与旧实现一致）。排障：全量 stdout/stderr 镜像 `workspace/logs/tts_clone_<ts>.log`（每 attempt 一个 `=== attempt ===` 段）。`concurrency` 缺省 `tts.batch_concurrency=4`（前端读作 Voices 页「批内行数（上限）」初值后作为 `req.concurrency` 传入）。

**只读** `GET /api/tts/voices?script=`（409 守卫豁免，无工作空间降级空）：读脚本（`__all__` 折叠整书：按 speaker 名去重、累加台词数、保首见顺序）+ `voice_config.json`（`migrate_entries_in` 惰性迁移 `ref_audio`）→ 每角色 `{name, line_count, status(ready|pending), foundation_status, clone_status, type, alias_of, description, preview, candidates: [{id, preview, seed}], selected_audio_id}`。列表按 `line_count` **降序**（稳定——同数保持首见顺序；无脚本时计数全 0 = voice_config 键序不变），主角恒在列表顶部。`ready` = 有 `alias_of` 或 `_voice_usable`（clone 需 `ref_audio`；design 需非空 `description`；custom 恒真）。`preview` 是**相对 `04_voice_profiles/`** 的路径，前端经 `GET /api/files/download/04_voice_profiles/{name}` 播放（`MiniAudioPlayer`）；`candidates` 经 `V.effective_candidates` 派生（旧格式单候选合成 1 项、无克隆 `[]`），`selected_audio_id` = 用户选择（`null` = 默认第 1 条）。
`PUT /api/tts/voices/select {speaker, audio_id?}`（**同步写，非任务**）：记录某角色的克隆候选选择——`selected_audio_id` 与**顶层 `ref_audio`（生效参考）一次文件重写同步更新**，后续音频合成恒用选中条；`audio_id` 空/None = 清除选择回默认第 1 条。守卫链：工作空间 409 → **voices-foundation/voices-clone 任务在途 409**（两阶段都整体重写 `voice_config.json`，防并发写互踩）→ 文件/角色缺失 404 → 无候选或 `audio_id` 非法 400。试听走既有 `GET /api/files/download/04_voice_profiles/{name}`（零新路由）。前端「选择音色」弹层（页内 `fixed inset-0 z-50` overlay）：候选行 = radio + `MiniAudioPlayer`（`useAudioBus` 全局单播）+ 生效项「当前」Badge；候选 <2 的角色（自动模式龙套、旧格式单候选）按钮禁用。
`voice_config.json` 条目字段：`type`（foundation/clone/design/custom）、`description`、`ref_text`、`ref_audio`（工作空间相对，**≡ 生效候选**）、`candidates`（`[{id, ref_audio, seed}]`，写入即工作空间相对）、`selected_audio_id`（`null` = 默认第 1 条）、`alias_of`、`foundation_status`/`clone_status`（done|failed）、`character_style`。
测试：`test_voices.py`（JSON 抽取/别名解析/采样带/状态推断/**自动备选数分档/候选簿记/别名台词归并**/**make_clones 全 e2e**（fake-worker 真子进程经真 `run_worker`：固定/自动计数、部分失败重编号、全失败保留旧 ref_audio、取消角色级原子、new_only 差额、**进度单调（worker 驱动、重启不回跳）**、看门狗缩批重启（二轮 `--concurrency` 减半）、size-1 两次超时隔离毒行、8 次上限 RuntimeError、断点采纳零作业短路（seed=−1、不启动引擎）、批内多行共享 seed（候选仍互异））/select 端点（选择/清除/400/404/409 工作空间/409 任务在途）/list_voices（候选字段 + 列表台词数降序））。完成后 `project.recordVoices` → 「前往音频合成」。

### 阶段 4 · 音频合成（BatchTTS.vue `/batch`）

`POST /api/tts/batch {indices?, script?, scripts?, concurrency?, seed?}` → module `tts-batch`；**`script`/`scripts` 含 `"__all__"` → 400**（"全部"只用于角色配音）；`indices` 且多文件 → 400。分发：0/1 文件 → `tts_batch.synthesize`（旧单文件路径，行为逐字节不变）；**>1 文件 → `tts_batch.synthesize_multi`**（一个任务内**池化**：全章节待合成段进同一任务池、单一引擎子进程统一调度）。`POST /api/tts/batch-reset {scripts}`（**同步写，非任务**——「重新全部合成」第一步）：删除选中文件的 `05_audio_chunk/<包>/`（mp3 + manifest；`package_for` 与合成包名同一函数——`_checked` 名映射到同一包；包名 = 文件名 stem，构造上不可能越出目录）→ 前端随后发**与一键合成完全相同**的请求（默认 resume：删后全段未完成 → 全部重做）；旧的 `force_all` 分支（请求字段 / `plan_to_synthesize` 全量分支 / 独立任务标签）已移除。守卫链：工作空间 409 → `__all__`/空列表 400 → **tts-batch 任务在途 409**（引擎正在写包目录）。`GET /api/tts/batch-status?script=` → `{total, completed, remaining}`（单文件 / 缺省最近）；`GET /api/tts/batch-status?scripts=…`（重复参数，多文件）→ `{files: [{name, total, completed, remaining, complete, speakers, ready, missing}]}`（按请求顺序；`complete` = total>0 且 completed==total = 行的【已合成】；ready/missing 与角色配音页同一规则（`alias_of` 或 `_voice_usable`）；completed = manifest 里 `ok` 且**文件仍在磁盘**的段；增量写 manifest 所以运行中刷新真实）。
`_synthesize_one` 链（`synthesize` / `synthesize_multi` 共享的每文件主体）：`resolve_parsed_json` + `_load_script`（缺/坏/空 → RuntimeError）→ 读 `voice_config.json`（**缺失仅 WARNING**；先 `migrate_entries_in` 迁移再 spawn，worker 按 `--workspace` 解析相对 `ref_audio`）→ `out_dir = 05_audio_chunk/<包名>`（`package_for` = 源 JSON stem 剥 `_checked`，兜底 `"batch"`）→ `_build_segments`（`index` = 全脚本行位置；`speaker` 取 `entry.speaker or entry.type`；跳空文本；带 `instruct`/`pause_after`）→ `load_manifest` + `plan_to_synthesize`（显式 indices 交集 > 默认 **resume** 只合未完成的；「重新全部合成」= 先 `batch-reset` 删包目录再走本路径，无独立分支）→ **零段短路**（重写完整 manifest、progress 1.0、**不启动引擎**省模型加载）→ 段表写 `00_temp/batch_segments_*.json`（绝对路径，用完删）→ `run_worker`（`--mode batch`，`--concurrency` 钳 [1,64] 缺省 `config.tts.batch_concurrency=4`，`--seed` 缺省 `batch_seed=-1` 随机，`--workspace`，三个模型 id，`--ffmpeg`，`--disabled-checks`（关闭的规划检查逗号清单，全开省略），`--restore-stack`（待恢复的降档记录 `<字数>:<批内上限>,…` 最旧在前，空省略））→ **看门狗重启循环**：`WorkerWatchdogTimeout`（退出码 124）→ workers>1 则**先记录再减半**（`(超时子批总字数, 降档前上限)` 压入运行内存的降档栈 `restore_stack`——chars = `in_flight` 段的 strip 后 text 长度之和，与 worker 行 `chars` 恒等；in_flight 空/全未知索引 → 不记录）后 `workers//2` 缩批重启；workers==1 则对 `[watchdog]` 行解析出的段记罚（**两次超时 → 隔离**：`excluded` + manifest `ok:false reason="超时（已隔离）"`；**记罚不推降档记录**——挡位未变）；累计 8 次未愈 → RuntimeError（已完成进度已保住）；降档栈每次重启经 `encode_restore_stack` 编码交 worker（`--restore-stack`），worker 逐规划子批恢复（见下 worker 段），后端经 `[restore]` 行同步（弹栈顶 + `workers = N`；漂移 = 解析失败/无对应记录 → WARNING，仍按 worker 上报同步）→ 收尾：最终 manifest、统计 + **进度指标强制终推**（`_report_stats(force=True)`——终态快照/result 事件携带同一组最终计数器）；**`completed==0` → RuntimeError**（任务 FAILED，"本次 N 段全部合成失败"）。
`synthesize_multi` 链（多文件，待合成卡的多选；**池化**：所有章节的待合成段读入**同一 TTS 任务池**、**单一引擎子进程**全池统一排序/分组/批调度——**章节是数据组织单位，不是 GPU 调度单位**）：① `voice_config` 读 + `migrate_entries_in` 池级一次 → ② 逐文件准备（请求序；每文件前 `handle.check()`——`TaskCancelled` **永不隔离**，直接上抛，取消是任务级结局）：`resolve_parsed_json` → `_load_script`（缺/坏/空 → **fatal 隔离**：`error` + ERROR 日志 + 继续其余文件）→ `package_for`/`out_dir`/`manifest_path` → `_build_segments` → `load_manifest` → `plan_to_synthesize`（恒 resume）→ **包冲突守卫**（`x.json` + `x_checked.json` 经 `package_for` 映射到同一包 → 后者准备阶段 `error`，API 层兜底，UI 已过滤 `_checked`）；**零段文件**（全完成/无可合成段）= 重写完整 manifest + **不进池**（不占模型）→ ③ 建池（`_build_pool_rows`：池行 = 段字段 + `out_dir`（章包目录绝对路径）+ `file_index`（章内局部行 index）；`index` = **池全局** 0 基下标连续编号；`pool_map` = 池 index → (文件, 局部 index) 的**唯一映射源**）；**零池** → 不启动引擎直接 `_settle_pool` → ④ 引擎准备一次（`clamp_concurrency` 池级单值、seed、`seg_file`、`run_log`；**后端不自发进度**——进度由 worker `[progress]` 行全池 0→1 直驱）→ ⑤ 多 manifest 共享 2s 节流 `flush_manifests`（per-file dirty；force flush 覆盖**全部**进池文件——取消不丢任何章节的已完成工作）→ ⑥ `on_line`：`[segment]` → `_handle_segment_pool`（经 `pool_map` 路由到对应文件 `seg_results` + 置 dirty；**池 index 绝不作章节内 index**）→ **进度指标上报**（全池累计，节流 1s，经 `handle.segment_stats` → SSE `segments` 事件；与 worker `[progress]` 行驱动的进度条是两条独立通道）→ ⑦ 单 worker 看门狗循环（形态同单文件：rc=0 结束；`WorkerWatchdogTimeout` → 缩批 //2（降档记录同单文件：chars = **池行** text 求和，经 in_flight 池 index → 池行）/ workers==1 按**池 index** 记罚（两次 → 隔离，经 `pool_map` 落**对应章** manifest `ok:false reason="超时（已隔离）"`；记罚不推记录）；`on_line` 的 `[restore]` 分支同步池循环的 `workers`（与单文件同一协议）；重启时 remaining 行**沿用原池 index 不重编号**直接写回 `seg_file`；`attempt>8` → RuntimeError；`except Exception`（含 `TaskCancelled`）→ `flush_manifests(force=True)` + 上抛）→ ⑧ 最终强制 flush + `_settle_pool`。结果形状与旧多文件版逐字段一致：顶层 `{total, completed, failed（带 `script`，index = **章内局部**）, output_dir:"", manifest_path:"", done_count, all_count, files: [请求序，每条目 9 字段]}`；per-file `error` **只承载准备阶段 fatal**——引擎级「某文件全段失败」不写 `error`（由 `failed` 列表 + 该包 manifest `ok:false` 表达；一章全败 + 他章成功 → 整池成功）；成败判定 = `attempted（进池或有 error 的文件）且 completed==0 → RuntimeError`（全文件 fatal → 空池 → 同路径）。收益：模型集合只加载一次、短章尾部不再饿死 batch（原逐文件形态的「每文件一个子进程、模型逐文件加载」代价已消除；前端卡片描述与页面文案注明池化语义）。
worker（`tts-engine/tts_worker.py --mode batch`）：按段所需 type **只加载用到的模型**（custom→`model` / clone→`base_model` / design→`design_model`；`Qwen3TTSModel.from_pretrained`，优先 HF 本地缓存；cuda→bf16+device_map；import 噪音吞进 devnull）；无配置的角色 → 段级 error「缺少角色声音配置（X）——请先在「角色声音」页生成」（**不中断整批**）；**按 type 分跨角色统一队列**：**行数最多的队列先跑**（平手按首见序；队列内按字数升序——长度分档 / 比值 ≤3 约束的是统一队列，不同角色的同长度行可同批凑满批内上限，长短行仍绝不混批：解码上限随批内最长行，长短行混批会让短行按长行的上限跑）；clone 每角色建一次 `voice_clone_prompt`（跨角色子批按行取各自角色的 prompt 条目——模型 list API 逐行匹配 reference）；**惰性子批规划**每轮 `plan_next_sub_batch_with_restore`（`plan_row_tokens` = 字符×1.2+overhead 估 token；`VramGovernor` 纯算术反馈环按实测显存压力**减半**/宽松**增长** `vram_scale` 重标定；`LENGTH_BANDS` 长度分档 cap；单批总字符 ≤ `--max-batch-chars`(12000)；超长行独批；长度比 ≤3 防混批（自 5 收紧——解码上限随批内最长行，混批短行会跑长行全程）；以上五道检查（分档/总字符/超长独批/长度比/显存静态估算）可经 `config.tts.planner_*` 逐项关闭（**设置页开关**，默认全开 = 行为不变；关闭项经共用 `--disabled-checks` 逗号清单传给 worker〔全开省略〕，各留一行「…已关闭（配置）」运行日志；实测显存的 VramGovernor 不受开关影响、恒生效）；**每行必入恰好一个批**；**降档恢复栈** `--restore-stack`（`<字数>:<上限>,…` 最旧在前，`parse_restore_stack` 解析，畸形 → 退出码 2）：先按当前（降档）挡位规划，本轮批总字数 < **栈顶**记录阈值（**LIFO 只查栈顶** = 逐级回退）→ 按恢复挡位**试规划**（五道检查与 VramGovernor 照作用于试批，`vram_scale` 保留累积值）——试批也 < 阈值才**确认恢复**（`gov.cap`/manual cap 抬回 = 该挡位「全新进程」态，弹栈 + 发 `[restore] cap=N` 行）；试批仍超阈值 → 回退试规划 cap、按小批跑（记录保持待命，后续轮次再试）；**解码上限随批内最长行缩放** `min(2048, max(128, 字数×6))`（批量模式模型不按行提前停 EOS，固定 2048 上限会让短行批跑满全程——2026-09-17 一键合成卡死的根因）；子批超时预算 `sub_batch_timeout_seconds`（GPU〔cuda/mps〕= `chars/15` 秒〔实测 ~15 字/秒，clone/custom/design 同一率〕，floor 30s / cap 3600s；cpu `600+4·chars`∈[600,10800]）+ 心跳（10s 起每 20s）→ 超时先 flush `[watchdog]` 行再 `os._exit(124)`（进程+CUDA 上下文同死）；非超时故障 → size-1 也崩则 `os._exit(124)` 交后端隔离，否则 `observe_fault` 缩容 + 清显存 + **对半递归重试**；`seed>=0` 时每子批 `torch.manual_seed(seed + 子批序号)`（可复现）。
产物：`05_audio_chunk/<包>/<index+1 零填充>.mp3`（soundfile WAV → pydub/libmp3lame；<1024B 视为坏头删）+ **`manifest.json`**（list `[{index, speaker, text, pause_after, path(工作空间相对), ok, reason}]`，**每见一条 `[segment]` 行更新内存态，整份重写落盘节流至最多每 2 秒一次**（`MANIFEST_FLUSH_INTERVAL`；首行必落盘，取消 / 引擎失败 / 看门狗重启 / 收尾强制落盘）——取消不丢已完成工作，后端被强杀最多丢 ~2s 进度（其 mp3 留盘，续合只补 manifest 缺的段）；加载时惰性迁移旧绝对 path）。
worker 退出码：`0` = 成功（**个别段失败也 0**，段级失败走 `[segment] i error <reason>` 行）/ `1` = 通用失败 / `2` = setup 错误（stderr `TTS_WORKER_ERROR:`）/ `124` = 看门狗。
排障：全量 stdout/stderr 镜像 `workspace/logs/tts_batch_<ts>.log`（`[out]`/`[err]` + `=== attempt started/ended rc=N ===`）——任务日志是 SSE-only 的，此文件是失败运行的磁盘证据。
测试：`test_tts_batch.py`（钳制/看门狗缩批/隔离/resume/增量 manifest/相对路径/零启动/**多文件池化**（`synthesize_multi`：池行 per-row `out_dir`/`file_index`、零池不启动引擎、已完成文件零池行、跨文件 resume 保留原 path、看门狗池 index 映射（隔离只落对应章包）、取消 flush 全部 manifest、引擎级「一章全败」进 `failed` 列表不写 `error`、全池全败 → RuntimeError、结果形状/文件序钉死、cmd 形状不变 + 包冲突守卫、准备阶段 fatal 隔离、取消 → `TaskCancelled` 穿透）/**batch-status 多文件**（计数/complete/ready/missing、缺失名零字典、`__all__` 400、无工作空间降级）/**run_batch 分发**（多 → `synthesize_multi` + 「N 个文件」标签、单 → 旧路径逐字节不变、`indices`+多 → 400）/**进度指标**（基线全量累计〔resume 含运行前 done〕/ 逐段节流推送（`STATS_FLUSH_INTERVAL` monkeypatch 0 时逐段推）/ 失败段不计入 / **全池口径**跨章边界累计 / prep-fatal 文件排除出总数 / 池化 resume 基线））、`test_tts_worker.py`（规划/显存/governor/超时预算、**`_row_output_paths` 池化落盘（缺省回退逐字节一致 / per-row 目录与局部号 / 超宽零填充）**）。

### 阶段 5 · 音频合并（Merge.vue `/merge`）

**批量合并（2026-09 改造：单选 → 复选 + 并行任务）** `POST /api/tts/merge {m4b=false, packages: [...]}` → **每包一个独立 Task**（module `merge`，label `合并音频（Merge）：{包}`（m4b → `合并 M4B：{包}`）——label 格式是 F5 重挂与在途守卫的承重契约，见「已知陷阱」）：守卫链 `require_workspace`（409）→ 旧字段探测（`package` 非 null → 400「前端版本过旧……请重新构建前端（npm run build）后刷新」）→ 空选择 → 400（**无「最近包」回退**）→ 包名校验（`p != Path(p).name` 穿越 → 400）→ **同包在途 409**（`_inflight_merge_packages()`：非终态 `merge` 任务 label 尾部 `re.search(r"：(.+)$")` 提取包名——允许追加不冲突的包，仅同包冲突被拒，防两个任务写同一 `06` 输出文件）→ `set_merge_concurrency(merge.concurrency_limit())` → 逐包 `create(start=False)` 建 PENDING 壳 → 起 `_run_merge_coordinator`（daemon 线程，0.2s 轮询；**无注册表/无 stop 标志**——与解析页的差异：合并任务**全程持槽**、无「检查期」，`no_slot` 直接数 RUNNING/PAUSED，无需 `phase` 判定；取消 = 前端逐任务 `control`（无 cancel-batch 端点），被取消的 PENDING 壳就地终结后自然掉出投放搜索；不变量同为「等待槽 ≤ 预取深度 4」（`MERGE_PREFETCH_DEPTH = 4`），按提交序投放）→ 响应 `{task_ids, packages: [{package, task_id}]}`。并行度 = `concurrency_limit()` = `max(1, min(4, (os.cpu_count() or 4) // 2))`（**常量策略，无配置项**——每任务 ≈ pydub 1 核 + ffmpeg 1~2 核 + 磁盘 I/O，整书 WAV 全量入 RAM 是内存约束点，封顶 4；8 核 → 4 / 4 核 → 2 / 2 核 → 1）。`m4b=true` 目前仅 WARNING（"M4B 后续阶段支持；本次生成 MP3"），输出恒 MP3。
`run` 链：`_find_manifest`（`05_audio_chunk/<package>/manifest.json` → 缺省取 mtime 最新包 → 再退遗留顶层 `05_audio_chunk/manifest.json`；无 → RuntimeError「未找到合成结果清单」）→ 解析/空 → RuntimeError → `migrate_entries_in` → `collect_segments`（只留 `ok` 且文件真实存在，**按 index 重排**；segs 空 →「没有可合并的音频」；missing>0 → WARNING「N 段…缺失，将跳过」）→ 停顿 `pause_ms = tts.pause_between_speakers_ms or 500` / `same_ms = tts.pause_same_speaker_ms or 250` → 批数 `ceil(N/100)`（>1 时日志「两阶段合并：N 段 → M 批（每批 100 段）→ 整书」）→ **`merge_gate().acquire(stop_check=…)`**（排队中取消 → `TaskCancelled` 零文件残留——acquire 在全部快速失败校验之后、任何暂存文件写出之前；返回 False = 未取槽，**不得 release**）→ 暂存 `00_temp/merge_segments_*.json` + `00_temp/merge_tmp_*/`（uuid 名 → 多任务并行天然无冲突）→ `run_worker`（`--mode merge`，`--pause-ms/--same-same-ms/--tmp-dir/--merge-batch-size 100`，无 watchdog）→ `[result]` 捕获产物 → **finally（所有退出路径）**：`if acquired: merge_gate().release()` → `[result]` 指向 tmp 内 WAV（编码失败兜底）则搬进 `06_audio_merge/`；`rmtree(tmp_dir)` → 返回 `{file, path, segments, size}`。
worker（`_run_merge`）：**Stage 1** 每批 pydub `combine_audio_with_pauses`（`pause_after` override > 同人 `same_ms` > 换人 `pause_ms`；末段 override 被忽略——存入 part 元数据 `last_pause_after` 供批间用）→ `part_NNN.wav`；进度带 `merge_stage1_frac` 0.05→0.80。**Stage 2** 单 part → **`os.replace` 原子改名**即整书 WAV（免重解码快路径）；多 part → 批间间隙全由 `boundary_gap_ms`（与单遍合并同一规则）显式 override → 整书 WAV；`merge_stage2_frac` 0.80→0.95。**编码** `_encode_mp3_streaming`：`ffmpeg -y -i <整书WAV> -c:a libmp3lame <out.mp3>`（**MP3 直出** `06_audio_merge/`，全链路唯一一次编码；整书 WAV 只存在于 `00_temp` 暂存目录内、随 `rmtree` 即毁——成功路径**无大体积 WAV 落盘**；Windows 绑 KILL_ON_JOB_CLOSE Job Object 防孤儿 ffmpeg）；进度双源（stderr `time=` 行 + 每 ~2s ffprobe 测增长中输出文件真实时长——Windows stderr 块缓冲兜底）；`merge_encode_frac` 0.95→1.00；失败（无 ffmpeg/非零退出/输出 <1KiB）→ 保留 WAV 经 `[result]` 上报 → 后端搬走（**唯一保留 WAV 的情形**）。
产物：`06_audio_merge/<包名>.mp3`（包名清洗 Windows 非法字符；遗留顶层 manifest → `cloned_audiobook.mp3`；兜底 `.wav`——**页面不计为「已合并」**）。**取消/失败残留的部分 MP3 不删除**（「绝不删除用户数据」；重合并 `ffmpeg -y` 覆盖自愈）。
行状态源 `GET /api/tts/merge-status?packages=…`（重复查询参数；`Annotated[list[str], Query()]`——裸 list 参数会被当 JSON body 静默忽略，见「已知陷阱」）→ `{packages: [{name, total, completed, remaining, complete}]}`：**`total` 恒取源解析 JSON 口径**（`03_parsed_json/<包名>.json` 经 `Batch._build_segments` + `count_completion`，与 `/batch-status` 同口径）——按 manifest 长度算会把合成中途取消的包（manifest 只有已上报段）**误判「已就绪」**、静默合并出半本书；源 JSON 缺失/损坏/空才降级 manifest 长度（`completed` = `is_done` 计数）；`completed` 口径 = `is_done`（ok 且文件在盘）；无工作空间 → 全零值行降级（不 409）；穿越名 → 400。
前端（`Merge.vue`，纯页面本地状态 + task store 派生）：行 = `listDir('05_audio_chunk')` 的目录（弃 DirPicker，自绘复选行）；行任务**派生式重挂**——store 中 `module === 'merge'` 任务按 label 尾部 `：{包}` 归位（在途取 `seq` 升序首个、失败取最新），F5 后 `refresh()` 即自动重挂、无本地 job 列表。行状态**优先级**：合并中（有在途任务）> 已合并（SSE 标记或 `06` 下有 `<包名>.mp3`——**WAV 兜底产物不算**）> 已就绪（`stat.complete`）> 已合成 C/T 段 > 未合成。工具栏：**全选 = 清空后只勾「已就绪」行**（用户拍板的语义；按钮态随切换「已选/全选」）· **全量全选 = 无视状态全勾**（含已合并/未就绪）· 清空 · 刷新 + 计数「已选 X/N · 已就绪 Y · 含已合并 K」；**在途行 checkbox 禁用**（防同包重复提交，409 兜底）。主按钮「合并为 MP3（N 个包）」；**取消全部 = 前端逐任务 `control(id, 'cancel')`**（无专用端点）；行内：合并中 → `Progress` + % +【取消】；已合并 →【下载】+ `MiniAudioPlayer`（替换原生 `<audio>`，`useAudioBus` 全局单播）；失败 →【重试】+ 错误行；行内 `LiveLogPanel` **仅 RUNNING/PAUSED 渲染**（防 N 个 PENDING 壳渲染 N 个空面板）。**无轮询**：磁盘口径（行列表/段数/MP3 存在性）在 onMounted/onActivated/手动刷新时拉取；「合并中 → 已合并」实时性由 SSE getter 式 watch 驱动（终态 merge 任务：succeeded + `.mp3` → 置行标记 + `project.recordMerge`（单值、最后一个完成者生效）+ toast；succeeded + `.wav` / failed / cancelled → 清标记行回落；`processed` 集合防快照重放重复弹 toast、任务转回非终态（重试）即释放）。「前往音频分集」按钮门控 = `project.mergeResult && ui.show_audio_split`。
测试：`test_merge.py`（命令/两阶段日志/tmp 成败双清/WAV 兜底/错误路径 + **merge 闸作用域**：快速失败不占槽〔他方持闸时立即失败、闸不动〕/持槽时 spawn 前零调用〔release 后才 spawn〕/成功与引擎失败路径 `active` 配平/排队中取消 ≤1s `TaskCancelled` 未取槽零残留/成功路径 `06` 下无 WAV〔MP3 直出需求固化〕/`concurrency_limit` 公式）+ `test_merge_batch.py`（**批量投放**：PENDING 壳按提交序/首轮恰投预取深度 4/label 钉死/预取上限与顺序〔gate ≤C、在跑 ≤C+4〕/CPU 定闸上限/空选择 400（无最近包回退）/旧字段 400/穿越 400/去重保序/同包在途 409〔追加不冲突包放行〕/逐任务取消全部收敛/merge-status 各口径）+ `test_tts_worker.py` 合并纯函数（批覆盖/`boundary_gap` == 单遍/进度带单调连续）。

### 阶段 6 · 音频分集（AudioSplit.vue `/audio`）

同步：`POST /api/audio/probe {path}`（ffprobe 时长/大小/扩展名/MIME）；`POST /api/audio/plan {path, target_duration?}`（均分方案预览，`build_plan`）。
任务：`POST /api/audio/silences {path, target_duration?, align_tolerance?}` → `_silences_worker`（probe → `detect_silences` → `build_aligned_plan`；结果含 `pauses`、`snapped`、`fallbacks`、`shifts`——「智能对齐」预览）；`POST /api/audio/cut {path, target_duration?, smart_align?, align_tolerance?, naming_format?, start_number?, segments?}` → `_cut_worker`（**传了 `segments` 就直接用**（与页面预览一致）；否则按 `smart_align`（缺省 `config.audio.smart_align=True`）重探测停顿并建方案）→ `cut_segments` → 产物进 **`07_output/<源文件 stem>/`**（包名 = 源 stem））。
引擎细节：`probe_duration`（`ffprobe -show_entries format=duration`，timeout 120）；`detect_silences`（`ffmpeg -af silencedetect=noise=-30dB:d=0.5 -f null -`，reader 线程流读 stderr 折算进度，主循环 0.1s 轮询取消 → `proc.kill()`）；`parse_silence_log`（正则兼容 ffmpeg 4/5/6 与 `[silencedetect @ 0x…]` 前缀；负 `silence_start`（编码器延迟）忽略；`end≤start` 丢；**尾部未闭合静默封顶到总时长**；按 start 排序）；`build_plan`（`count = max(1, ceil(total/target − 1e-6))`，段平铺 `[0,total]` 末段钉住终点；`parse_duration_to_seconds` 支持 `MM:SS`/`HH:MM:SS`/裸秒）；`snap_boundaries`（只动内部边界；窗口 `W = min(tolerance, 段宽/2 − 0.5)`；窗口 `[max(ideal−W, prev+0.5), min(ideal+W, 下一ideal−W−0.5)]` **单调棘轮** → 永不交叉；取窗口内离 ideal 最近的**停顿中点**再钳回；无候选 → 留原位计 fallback）；`build_aligned_plan` = 均分 + snap（**段数不变**；`count<2` 时 `aligned=False`）；`cut_segments`（每段 `ffmpeg -y -ss S -i in -t D -c copy out`；stderr **按字节**读（Windows GBK locale 会炸 UTF-8 日志）；段间查取消；非零退出 → RuntimeError 带 stderr 尾 300 字）。
**分集命名**（`output_name`，commit f7db438）：**`naming_format` 即完整文件名**（默认 `第 {} 集`），`{}` → `(start+index)`、按 `start_number` 输入位数零填充；**不再拼源文件名前缀**（`"重活了 第 {} 集"` + start `"1"` → `重活了 第 1 集.mp3`）；无 `{}` → 追加 `_<n>` 防碰撞；`start` 非数字 → 1（`"10x"` → 10）。
常量：`SILENCE_NOISE_DB=-30`、`SILENCE_MIN_DURATION=0.5`、`DEFAULT_TOLERANCE=15`（钳 5..30）、`MAX_SEGMENTS=1000`（`/cut` 超限 → RuntimeError）。
切割后同步端点：`POST /api/audio/zip {base?, files[]}` → `07_output/<base>/<base>.zip`（`ZIP_STORED`）；`POST /api/audio/export {source_path, files[]}` → **源音频旁建 `分集/` 文件夹** `copy2` 拷入（构造文件名越界 `Path(name).name != name` → 400）。
测试：`test_audio.py`（均分/棘轮/段数不变/命名/停顿日志 + 真实 ffmpeg 集成测试，缺 ffmpeg 自动 skip）。参数（目标时长/命名/起始号/智能对齐/容差）由**前端** fire-and-forget 存回 `config.audio`（后端只按「请求字段 or 配置缺省」取值，自己不写配置）。

### 阶段 7 · 背景音乐（BGM.vue `/bgm` + MusicLibrary.vue `/music`）

**音乐库**（`MusicLibrary.vue`，全局资源、**无** WorkspaceGateAlert——音乐库在**工作空间外**的项目根 `music_library/`，跨工程共享；所有 `/api/music` 端点**无 `require_workspace`**）：行 = `music_index.json` 曲目（文件名 = 跨页面通用引用键）+ 试听（`GET /music/preview/{name}`）+ 四类标签 Badge（scene=sky/mood=violet/emotion=rose/custom=amber）+ enabled Switch；批量上传（同名 **409 不覆盖**）/ 批量加删标签 / 启用禁用 / 删除（被 **locked 且 `music==name`** 的章引用 → skip + reason；未锁定引用不阻挡——删除后 BGM 行标「⚠ 已删除」降级、重匹配自愈）；AI 标签推荐 `POST /music/suggest-tags`（**同步 LLM 不经任务系统**，prompt = 文件名 + 用户描述 + 词表、LLM 不读音频；结果只留词表内名字 + 钳上限，**候选**供勾选预填、以用户确认为准；全败 502）；**批量 AI 识别（2026-09，每曲一任务）** `POST /music/suggest-tags-batch {names}`（module `music-ai-tags` / label `AI 推荐标签：{name}`〔承重契约〕；守卫 = 逐名 穿越 400 / 缺失 404 → 空 400 → model 空 400 → **同曲在途 409**；`set_concurrency(max_concurrency)` + 复用 `_run_bgm_coordinator`〔真共享 LLM 闸、预取深度 4〕；worker `suggest_track_tags` = 2 次 LLM（temperature 0.2 / max_tokens 300），**2 败 → 任务 failed**（UI【重试】）、成功**只写本曲候选条目**进 `music_tag_suggestions.json`、索引不动）——候选缓存在 `GET /library` 的 `suggestions` 只列现存曲目（孤儿隐藏不删）、用户确认（`PUT /tracks/{name}` 带 tags）才进标签并消费候选；**AI 推荐采用（2026-09）** `POST /music/tracks/apply-suggestions {names}` = 第二条确认路径：只作用于「**有候选 且 四类全空**」的曲目，已手动打标曲目**绝不覆盖**（`skipped_manual` + 候选保留——可稍后经弹层确认），采用后消费候选（词表外陈旧候选名同 PUT 折叠进 custom）；批量条【AI 推荐采用（N 首）】（N = 选中内可采纳数 = 有候选且未打标）；**行状态实时**：上传逐文件完成后即刷新列表、批量 AI 发起后 `taskStore.refresh()` 即刻重挂 PENDING 壳（行即显「识别中…」、SSE 驱动完成态）、【刷新】同步库 + 任务两口径（不依赖 F5）；标签管理（新增/改名重名 409；改名/删除**波及注册表 + 全部 track + `08_bgm` 分析缓存**（自有缓存安全重写），**assignments 快照不改写**（历史口径）；删除标签**不删音乐文件**）。

**BGM 页**（`BGM.vue`，WorkspaceGateAlert + 按钮门控；行/派生/SSE/F5 全复刻 Merge 页模式）：行 = `GET /api/bgm/chapters`（**只读无 409**，无工作空间空行降级；行基准 = 02 stem 排序，孤儿 assignments 不显示）+ taskStore 派生（`stemOfLabel` 取**第一个** `：` 后全部 × module 归位：在途 seq 升序首 / 失败最新）。行状态优先级钉死：`混音中 > 分析中 > 未合并（06 缺失）> 已混音（music=null 章 copy2 后 = 「已混音 / 无 BGM」）> ⚠ 已删除（music 被删：**禁止直接混音**、放行重匹配）> 已匹配 > 无 BGM > 已分析 > 未分析`。工具栏【全选】= 只勾「已匹配 ∧ 未混音 ∧ 可混音」行、全量全选、清空、刷新、计数、【分析所选（N 章）】、【混音所选（N 章）】、【取消全部】（逐任务 `control`，无 cancel-batch 端点）。

- **章节气氛分析** `POST /api/bgm/analyze {chapters}` → 每章一 Task（module `bgm-analysis`，**label `章节气氛分析：{stem}`**）→ 协调者（`BGM_PREFETCH_DEPTH=4`、无注册表/无 stop 标志、共享 LLM 闸 `gate()`，端点 `set_concurrency(generation.max_concurrency)`；分析任务槽作用域 = **全任务**（无检查期）→ `engines/bgm.py::analyze_chapter`：采样 头/中/尾三窗 × `bgm.analysis_chars`（默认 6000）→ LLM 3 次（temperature 0.2 / max_tokens 512；复用 `_llm_chat_completion`）→ 3 败 → 空标签记录 + WARNING（**绝不卡章**，可手动编辑/重分析）→ 成功 → **只改本 stem** 全量重写 `08_bgm/chapter_music_analysis.json`（LLM 只出标签**不选音乐**；词表外标签保留、自由可编辑）。
- **匹配** `POST /api/bgm/match {chapters?, mode}` **同步**（`chapters` 缺省 = 全部现存 02 章）：`mode="segment"` → **400**「段落级匹配为后续版本功能」（UI 占位：radio 禁用 + 「后续版本」Badge）；未知模式 400；**同章分析在途 409**（防读半成品分析；混音在途**不阻挡**）→ `match_stems`：排序顺序逐章 `match_chapter`（评分 + 平分随机 + 相邻 prev/next 去重逐级放宽 + 通用池兜底 + random 模式）；**locked 章整条保留**（`matched_at` 不动）+ `skipped_locked` 计数 → `08_bgm/bgm_assignments.json` **单次**重写（`data["mode"]` 持久化——模式切换 = 前端立即全量重匹配）。响应 `{mode, matched, no_bgm, skipped_locked, assignments}`。
- **手动干预** `PUT /api/bgm/chapters/{stem} {tags?, music?, locked?}` **同步写**：`tags` → 分析条目（`edited: true` + `edited_at`，**`analyzed_at` 保留**）+ assignments 标签快照（词表外自动进 custom）；`music` **键存在** = 设置（`null` = 清除；库内不存在 400；穿越 400）→ `manual: true` / `score: null` / reason「手动指定」 / `matched_at` 刷新；`locked` → 锁定旗标（任何重匹配不动其 music）；未匹配章按需创建 entry（reason「未匹配」）。
- **混音** `POST /api/bgm/mix {chapters}` → 每章一 Task（module `bgm-mix`，**label `背景音乐混音：{stem}`**）→ 协调者（同 `merge_gate()`——ffmpeg/CPU/磁盘同型负载，端点 `set_merge_concurrency(Merge.concurrency_limit())`）→ `mix_chapter`：守卫（该章从未匹配 400 / 06 旁白缺失 400 / 音乐已删除 400；**music=null 放行**）→ `music=None` → **`shutil.copy2` 06 原声进 08**（成品已生成，`mix_exists=true`）→ 否则 `probe_duration` 双路 → `build_mix_cmd`（`-stream_loop -1|-0` 由 `bgm.loop`；双侧 `aresample=44100`；`atrim` 到旁白时长；`afade` fade 双侧钳 `min(fade, 时长/2)` 防短章节重叠/越界；`volume = bgm.volume`（0.18）；`amix duration=first` 旁白为准）→ `merge_gate` 全程持槽、0.2s 轮询 + 输出文件增长进度 0.05→0.95 → 产物 `08_bgm/<stem>.mp3`（rc≠0 / <1KiB → failed；**部分产物留盘 `-y` 重跑自愈**）。

**label 契约（承重，三处钉死：后端正则 ↔ 前端派生 ↔ 测试）**：分析 module `bgm-analysis` / `章节气氛分析：{stem}`；混音 module `bgm-mix` / `背景音乐混音：{stem}`。后端 `_inflight_bgm_stems` 用 `re.search(r"：(.+)$")` 提取 stem 做同章在途 409；前端 `stemOfLabel` 取**第一个** `：` 后全部（stem 可含全角冒号，两侧同口径；与 Merge 页 `pkgOfLabel` 同形）——**改 label 必须前端 + 后端 + 测试三处同步**。

测试：`test_bgm.py`（纯函数全矩阵 + 两 JSON 缓存 + `match_stems` 边界〔locked 跳过 / 单章 prev 现有值 / 顺序 prev 新鲜值〕+ analyze e2e〔fake LLM：成功只改本 stem / 3 败空标签 / 排队取消 ≤1s 零落盘 / model 空快速失败 / gate 配平〕+ mix e2e〔fake Popen：成功 / rc≠0 / <1KiB / 排队取消 / 旁白缺失 / 音乐缺失 / **music=None copy2 字节一致** / merge_gate 配平〕）、`test_bgm_batch.py`（投放：PENDING 壳按序 + **label 两式钉死** / 预取上限与严格顺序〔双闸各一〕/ 端点定闸 / 空选择·穿越·前置 400〔从未匹配 / 06 缺失 / 音乐已删除 / **music=null 放行**〕/ 同章在途 409〔追加放行〕/ match 同步落盘〔locked 保留 + 计数 / segment 400 / 分析在途 409 而混音不挡〕/ 逐任务取消收敛 / `GET /chapters` 口径〔孤儿隐藏 / music_missing / wav 兜底 / 无工作空间降级〕/ PUT〔edited + analyzed_at 保留 / music 清除 / 守卫 400〕）、`test_music.py`（音乐库引擎 + API，见「测试」表）。

### 横切 · 任务系统（`core/tasks.py` + `api/tasks.py`）

发起：`TaskManager.create(module, label, func, *args, start=True, **kwargs)` → 立即返回 `{task_id}`（HTTP 200；**错误全部发生在任务内**）：缺省 `start=True` → daemon 线程跑 `func(handle, …)`；`start=False` → **PENDING 壳**（注册进 `_tasks`：可见于 `GET /api/tasks` / `snapshot_all` / F5 恢复，计入非终态 → SSE 保流；不起线程、`started` 保持 0、无 RUNNING 事件）→ 由 `start(task_id)` 在同一把锁内把壳 PENDING→RUNNING 起线程（双重检查：非 PENDING → `ValueError`、未知 id → `KeyError`；批次协调者等调用方吞掉二者 = 与 cancel 的良性竞态）。engine 代码用 `TaskHandle`：`progress(frac, current)`、`log(msg, level)`、`check()`（**每段工作之间调**——cancel 置位 → `raise TaskCancelled`；pause 置位 → 0.1s 忙等；暂停不作用于进行中的 HTTP 流读）、`phase(name)`（引擎上报所处阶段，如 `parse`/`check`；`set_phase` 发 `{"type":"phase"}` 事件——进程内消费者 = 解析批协调者（预取补位信号），前端 store 对未知 type 安全忽略，零改动）、`llm_chunk/llm_rate/llm_chars`（纯展示遥测）、`segment_stats(done, total, chars_done, chars_total)`（音频合成进度指标：已合成/总段数 · 已合成/总字数的**全量累计值**，发 `segments` 事件；快照/终态事件随快照携带 4 个计数器，`retry` 归零）。
状态机：`pending → running → {paused ⇄ running} → 终态 {cancelled, succeeded, failed}`（PENDING = 延迟启动的壳态——壳没有线程，没有 worker 观察 `cancel_event`）；**PENDING 上 cancel = 就地终结**（CANCELLED + `finished` + 带快照终态 status 事件 + `final` 事件——不就地终结壳就永远悬挂、SSE 流永远保持）；`retry` 仅终态（清空 logs/result/LLM 状态 **并 `phase = ""`** 后起新线程重跑——清陈旧 `phase` 必须：否则协调者把重试后的任务误判为检查期、白占预取名额）；`seq` = 单调创建序（`TaskManager` 内 `itertools.count` 于 `create` 时分配、入快照）——F5 恢复按 seq 升序重建勾选序（`time.time()` 同值会乱序）。
**SSE 协议**：前端只用**多路复用端点** `GET /api/tasks/stream`——**每标签页恰好一条连接**推所有任务（浏览器对单源的 HTTP/1.1 并发连接上限 ≈6，本控制台常开多个窗口/标签页，旧的「每任务一条 EventSource」在几个并发解析后耗尽连接上限：超限的 EventSource 永不连上，对应窗口一条日志都不显示而后台正常跑）。连接时**先取快照再订阅**（回放与直播不重叠）→ 先发 `snapshot_all{tasks:[快照…]}`（全部任务，重连即全量自愈）→ 此后**永不自行结束**（任务随时会新建），每事件带 `task_id`，15s 无事件发 `ping` 保活。`TaskManager` 持有进程级总线（`subscribe_all` 收 `(task, event)` 元组；`Task._broadcast` 钩子在 `Task._emit` 里把每个事件同时转发给总线；总线队列 maxsize 3000，**满时只丢纯展示事件** `llm_chunk`/`llm_rate`/`segments`（快照/final 回放自愈），关键事件（`log`/`progress`/`status`/`final`）先驱逐最旧展示事件再入队）。事件 `type`：`snapshot_all` / `snapshot`（仅单任务端点）/ `progress{progress,current}` / `log{level,msg,t}`（时序追加，新行在下）/ `llm_chunk{data}` / `llm_rate{cps,cps10}` / `llm_chars{chars,secs}` / `segments{done,total,chars_done,chars_total}`（音频合成进度指标——**纯展示、全量累计值**：每次事件都带完整四计数器，前端直接覆盖即收敛；快照/终态回放携带同一组计数器，断线重连自愈）/ `phase{phase}`（引擎阶段上报——进程内协调者消费，前端忽略）/ `status{status[,task]}`（**终态事件带完整快照**——客户端据此原子读取 result/error）/ `final{task}` / `ping`——多路复用流里一律额外带 `task_id`。前端 `task` store 用原生 `EventSource` 连复用端点（裸 `data:` 行统一 `onmessage`），`applyEvent(task_id, e)` 按 type 分发（`snapshot_all` → 整表替换）；**流仅在「至少一个任务非终态」时保持**（最后一个任务终态事件即关流，空闲标签页不占连接；`refresh()`/`control()`/建 store 在有活动任务时重开）；断流 → `refresh()` 对齐列表并按需重开。**刷新恢复（F5）**：任务内存常驻（进程生命周期内），运行中任务的快照含完整 `progress/current/logs/llm_stream`——刷新后 store 的 `refresh()` 全量对齐并按需重开流，发起任务的页面在 `onMounted`（`await taskStore.refresh()` 后）把**非终态**任务按 module/label 重新挂回本地 id 引用（经 `activeTasks(module)`：`fileJobs`/`taskId`/`busy` 等），进度条 / 日志 / 流式反馈 / 取消钮 / 完成 watcher 随之恢复（合并页例外：行任务由 store **派生**——按 label 尾部 `：{pkg}` 归位，无本地 job 列表，`refresh()` 即自动重挂）；终态任务不恢复（磁盘产物 + 页面既有徽章已表达完成态）；label 格式依赖见「已知陷阱」。单任务端点 `GET /api/tasks/{id}/stream` 保留（快照 `snapshot` 回放、`final` 即止，供一次性消费者）。
控制：`POST /api/tasks/{id}/{cancel|pause|resume|retry}`（404 任务不存在；400 未知 action）。任务内存常驻（进程生命周期内），`GET /api/tasks` 全量快照。
当前使用任务的阶段：文本解析（每文件一任务，内含角色匹配检查/断句校验/标签删除/超长段落检查/归属抽样/纯标点吸收/同人段落合并/超长机械分段）、角色配音两阶段、音频合成、音频合并、音频停顿检测/切割、**背景音乐**（章节气氛分析每章一任务〔共享 LLM 闸〕+ 混音每章一任务〔merge 闸〕）、**音乐库批量 AI 标签识别**（每曲一任务 module `music-ai-tags`〔共享 LLM 闸；候选写 `music_tag_suggestions.json`，用户确认才进标签〕）。

### 横切 · TTS 子进程编排（`engines/tts.py`）

`resolve_engine()` → `(python, worker)`：`PROJECT_ROOT/.venv-tts/{Scripts/python.exe | bin/python}` + `tts-engine/tts_worker.py`（env 覆盖 `AUDIOTTS_PYTHON`/`AUDIOTTS_WORKER`，测试桩用）；缺失 → 可操作的 RuntimeError（"请先运行 install_tts_env.ps1…"）。
`run_worker(cmd, handle, on_line, *, temp_files, fail_prefix, watchdog_code, log_file)`：`Popen(cwd=PROJECT_ROOT, env={PYTHONUTF8=1, PYTHONIOENCODING=utf-8})` → 双 daemon reader 线程泵 stdout/stderr 进队列 → 主循环 0.15s：`handle.check()`（协作取消/暂停）→ 抽干队列（**每行再 `handle.check()`**——满载机器上行处理慢、队列积成大积压，取消绝不能等整个积压抽干才被看到；延迟 = 一行处理时间）：`[progress] frac label` → `handle.progress`；其余 stdout → `on_line`（stage 解析 `[result]`/`[segment]`/`[watchdog]`）；stderr → WARNING 级日志 + `stderr_tail(40)` → `log_file` 时全量镜像（**块缓冲 64KB**、attempt 结束统一 flush——行缓冲 = 每行一次落盘 + AV 扫描，本身就在热路径上放大积压）→ 进程退出且双 EOF 后收工 → **finally**：存活则 `_kill_worker_tree`（Windows `taskkill /F /T /PID`，POSIX 普通 kill）→ wait → 关管道 → 删 `temp_files` → 非零退出：`RuntimeError(f"{fail_prefix}失败（退出码 N）：{stderr尾500}")`，**恰为 `watchdog_code` 时改抛 `WorkerWatchdogTimeout`**（供 batch 缩批重启）。
worker（`tts-engine/tts_worker.py`）6 种 `--mode`：`custom` / `design` / `clone`（前三者**不被后端调用**——后端只用 design-batch（角色配音·克隆）、batch、merge；前三者保留供手动/历史）、`design-batch`、`batch`、`merge`。stdout 协议行：`[progress] <frac> <label>`（所有 mode 驱动任务进度）、`[result] <abs path>`（恰好一次 = 最终文件；merge/单条 mode）、`[segment] <i> ok|error <…>`（batch）、`[design] <i> ok <seed> <abs path>` / `[design] <i> error <reason>`（design-batch）、`[watchdog] timeout batch=<label> indices=[…] elapsed=<s>`（先 flush 再 `os._exit(124)`）、`[restore] cap=<N>`（batch：规划子批总字数低于**最新**降档记录阈值、恢复挡位试规划也低于阈值 → 后端把批内上限恢复为 N 并弹该记录、同步 `workers`——恢复后后端账目的唯一同步源）。`[segment]`/`[watchdog]` 的 index = 段表原始下标——**池化多文件运行时 = 池全局 index**（池行另带 `out_dir`/`file_index` 决定落盘目录与章内文件号；池 index 从不写入 manifest/文件名，章内 `file_index` 从不进协议行/记罚）。stderr 错误前缀 `TTS_WORKER_ERROR:`。规划检查开关协议：`--disabled-checks <逗号清单>` 为 `batch` / `design-batch` 专用参数，值域 = 五个规范名 `length_bands,batch_chars,seq_chars,length_ratio,vram`（对应 `config.tts.planner_*`，后端经 `tts_batch.disabled_planner_checks` 映射）；全开时**整体省略**（默认配置下 worker 命令逐字节不变）；未知名 → `TTS_WORKER_ERROR` 退出码 2（fail-fast，防拼错静默关门）；该参数恒为 `--flag value` 成对出现（见「已知陷阱」）。降档恢复栈协议：`--restore-stack <字数:上限,…>` 为 **`batch` 专属**参数（后端交出的降档记录，最旧在前，经 `tts_batch.encode_restore_stack` 编码）；**空/省略 = 无恢复**（默认命令逐字节不变）；条目 = 两个正整数 `<超时批总字数>:<降档前批内上限>`，任何畸形（缺冒号/多冒号/非整数/非正）→ `TTS_WORKER_ERROR` 退出码 2（fail-fast，防拼错静默丢一条待恢复记录）；恒 `--flag value` 成对。模型常量 `DEFAULT_MODEL/BASE_MODEL/DESIGN_MODEL` = Qwen3-TTS-12Hz-1.7B-{CustomVoice,Base,VoiceDesign}；`DEFAULT_SPEAKER="serena"`、`DEFAULT_LANGUAGE="chinese"`。

## 配置项速查（`<workspace>/config/app.json`；根 `app.json` 为只读模板）

| 配置段.字段 | 默认 | 消费处 |
|---|---|---|
| `paths.working_dir` | `""` | 工作空间指针（**仅根文件可写**；工作空间配置里恒被强制为当前工作空间） |
| `text.*` | 见阶段 1 | `POST /api/text/format`（`partial_copy` 合并请求覆盖） |
| `audio.target_duration` | `"10:00"` | 分集 probe/plan/silences/cut 缺省 |
| `audio.naming_format` / `start_number` | `"第 {} 集"` / `"1"` | 分集 `output_name`（完整文件名语义，见阶段 6） |
| `audio.smart_align` / `align_tolerance` | `True` / `15` | 停顿对齐（`snap_boundaries` 窗口，钳 5..30） |
| `tts.model` / `base_model` / `design_model` | Qwen3-TTS-12Hz-1.7B-{CustomVoice,Base,VoiceDesign} | batch `--model/--base-model/--design-model`（空 → worker 同名默认） |
| `tts.speaker` / `language` / `device` | `serena` / `chinese` / `auto` | worker 缺省（auto\|cuda\|cpu\|mps） |
| `tts.pause_between_speakers_ms` / `pause_same_speaker_ms` | `500` / `250` | merge `--pause-ms/--same-same-ms`（0 → 回退该默认） |
| `tts.parallel_workers` | `1` | **遗留**（无读取方，仅为旧配置 round-trip 保留） |
| `tts.batch_concurrency` | `4` | 音频合成与角色配音·克隆共用的「批内行数」上限（请求 `concurrency` 缺省时用；钳 [1,64]） |
| `tts.batch_seed` | `-1` | 合成可复现 seed（`seed + 子批序号` 播种；-1 = 随机） |
| `tts.planner_length_bands` | `True` | 子批规划开关·段长分档（**设置页开关**；关闭 = 规划跳过该约束 + 运行日志一行「…已关闭（配置）」，经 `--disabled-checks` 传给 worker） |
| `tts.planner_batch_chars` | `True` | 子批规划开关·单批总字符上限（同上） |
| `tts.planner_seq_chars` | `True` | 子批规划开关·超长行（>2500 字）独批（同上） |
| `tts.planner_length_ratio` | `True` | 子批规划开关·批内最长/最短 ≤3（同上） |
| `tts.planner_vram` | `True` | 子批规划开关·静态 L² 显存估算门（**只关静态估算**；实测显存的 VramGovernor 动态调节恒生效、不可关） |
| `tts.api_base/api_key/voice/concurrency` | 空/1 | **遗留** API 字段，本地引擎不读（保证旧配置 round-trip） |
| `llm.base_url` / `api_key` | `http://localhost:11434/v1` / `"local"` | OpenAI 兼容端点（默认本机 Ollama）；远程 API 需真 key |
| `llm.model_name` | `""` | **必须用户设置**（空 → 解析/阶段 1 各自快速失败或兜底） |
| `llm.stream` | `True` | 流式（「流式反馈」面板）；服务器拒 `stream:true` 时置 false 回退非流式 |
| `prompts.system_prompt/user_prompt` | `""` | 解析提示词（空 → `resources/default_prompts.txt`；user 模板含 `{context}`/`{chunk}` 占位） |
| `persona_prompts.system/user/advanced_prompt` | `""` | 角色配音·阶段 1（空 → `persona_prompts.py` 内置；user 含 `{speaker}`/`{line_windows}`，`str.replace` 填充） |
| `generation.chunk_size` / `max_tokens` | `3000` / `4096` | 解析切块 / 单次完成上限 |
| `generation.temperature/top_p/top_k/min_p/presence_penalty/banned_tokens` | `0.6/0.8/0/0.0/0.0/[]` | LLM 采样（0 值的 top_k/min_p 不发；`banned_tokens` 非空才发） |
| `generation.max_concurrency` | `3` | 进程级 LLM 闸门（解析共享；`set_concurrency` 每批设置；另被阶段 1 `ThreadPoolExecutor` 用作 worker 数） |
| `generation.spot_check_rate` | `0.05` | 解析后归属抽样率（0 = 关闭）：全条目重判 speaker 的抽样比例，1/3 纯随机（整书错误率仪表）+ 2/3 风险加权（无标签/≤10 字/多角色场景，特征数级联）；每本纯随机桶读数记入任务日志与 `config/spot_check_history.json`，**降率由用户手动**（不自动降） |
| `generation.revalidate_splits` | `True` | 断句失败校验开关（**设置页开关**；关闭 = 解析时跳过该阶段并记录日志，`suspicious`/`suspicious_fixed` 保持 0） |
| `generation.delete_saying_tags` | `True` | 纯归属标签删除开关（**设置页开关**；关闭 = 解析时跳过该阶段并记录日志，`tags_deleted` 保持 0） |
| `generation.check_boundary_speakers` | `True` | 角色匹配检查开关（**设置页开关**；关闭 = 解析时跳过该阶段并记录日志，`boundary_checked`/`boundary_fixed` 保持 0） |
| `generation.check_batch_size` | `20` | 解析内重判批大小（角色匹配检查 / 断句失败校验 / 归属抽样共用；**UI 不露出**） |
| `generation.check_context_window` | `4` | 解析内重判上下文窗口（角色匹配检查 / 断句失败校验 / 超长段落重切 / 归属抽样共用；**UI 不露出**） |
| `generation.check_long_paragraphs` | `True` | 超长段落检查开关（**设置页开关**；覆盖 LLM 重切 + 机械分段兜底两半；关闭 = 解析时跳过该阶段并记录日志，`long_checked`/`long_fixed`/`long_split` 保持 0，最终不保证长度上界） |
| `generation.max_paragraph_chars` | `200` | 超长段落字数上限（**设置页可调**，strip 后 Unicode 码点口径）；`check_long_paragraphs` 开时**硬保证**：基文件绝无 strip 后超此值的条目 |
| `generation.absorb_punct_entries` | `True` | 纯标点条目吸收开关（**设置页开关**；无词字符条目并入相邻 NARRATOR / 无邻接删除；关闭 = 跳过 + 日志，`punct_absorbed`/`punct_deleted` 保持 0） |
| `generation.merge_same_speaker` | `True` | 同人段落合并开关（**设置页开关**；连续同 speaker 条目按词字符数合并〔块+段 ≤100 / 较短一方 ≤10 强制〕、边界无收尾标点补「。」、instruct 取词字符多者、章标题两侧不合并〔恒判无豁免〕；置于超长机械分段**之前**，其 ≤10 强制合并造出的 >`max_paragraph_chars` 块由机械分段切回；关闭 = 跳过 + 日志，`merged_same_speaker` 保持 0） |
| `ffmpeg.ffmpeg_path/ffprobe_path` | `""` | 空 = 从 PATH 解析（**绝对路径，永不相对化**） |
| `log.level` | `"INFO"` | 根 logger 级别（运行时可改） |
| `ui.theme` | `"system"` | 前端 `applyTheme`（system 挂 matchMedia 监听） |
| `ui.show_parse_logs` | `False` | 解析页日志区显隐（**设置页开关**；关 = 整个「解析进度」Card〔实时日志 + 流式反馈〕隐藏、三指标移到「开始处理」按钮下方；开 = 日志区显示且三指标在日志区） |
| `bgm.volume` | `0.18` | BGM 增益（`build_mix_cmd` 的 `volume=`；**设置页开关**） |
| `bgm.fade_in` / `bgm.fade_out` | `1.5` / `3.0` | 混音淡入/淡出秒数（**混音时钳 `min(fade, 章节时长/2)`**——短章节防 afade 重叠/`st` 越界；设置页开关） |
| `bgm.loop` | `True` | 循环策略：True = `-stream_loop -1` 循环铺满 / False = `-stream_loop -0` 只播一遍其余静音 |
| `bgm.min_match_score` | `1` | 匹配最低分（低于此分不进候选；钳 ≥1；设置页开关） |
| `bgm.analysis_chars` | `6000` | 章节气氛 LLM 采样字数（头/中/尾三窗；设置页开关） |

## 前端要点

- **`src/api/client.ts` 是唯一 HTTP 客户端**：`API_BASE = import.meta.env.VITE_API_BASE || 'http://127.0.0.1:8642'`（绝对源直连，CORS 后端全开）；fetch + JSON（响应先取 text 再试解析）；`!res.ok` 抛 `ApiError`。SSE 不在 client 里：`api/tasks.ts::streamAllTasks` 用**原生 `EventSource`** 连**多路复用端点** `/api/tasks/stream`（每标签页一条连接覆盖所有任务，事件带 `task_id`；永不 `final` 终止，由 store 主动 abort；`CLOSED` 回调；断开自动重连 → 后端重发 `snapshot_all` 全量自愈）——浏览器单源 HTTP/1.1 连接上限 ≈6，多窗口 × 每任务一条流的旧设计会超限致窗口无日志（见「任务系统」SSE 协议）；`streamTask`（单任务端点）保留共用同一 `openSse` 管道。唯一例外：`uploadFile` 走 FormData。
- **路由 = hash history**（静态托管下可工作），全挂持久 `MainLayout`（侧边栏固定 + `keep-alive`）：`/dashboard` 开始 · `/text` 排版与分册 · `/script` 解析 · `/voices` 角色配音 · `/batch` 合成 · `/merge` 合并 · `/audio` 分集 · `/bgm` 背景音乐（`/audio` 之后）· `/settings` 设置 · `/music` 音乐库（**导航最下方**，全局资源不受工作空间门控）（`/` → redirect dashboard；`/bgm` 与 `/music` 不受 `show_audio_split` 过滤、恒显示）。
- **stores**（Pinia setup 风格）：`app`（`backendUp` 状态灯，3s 轮询 health）；`project`（流水线交接：`activeScript/voiceResult/batchResult/mergeResult/audioOutputs` + `record*` 动作 + computed `audioInput`——「前往下一步」按钮的数据源；排版与分册页状态是**页面本地**的，交接在磁盘 `02_split_text/`（解析页直接列目录）；`activeScript` 被角色配音与音频合成**共享**（合成页的多选是**本地**状态，仅把**首个选中**文件同步到这里——`lastSynced` 守卫防多选被自身同步坍缩；外部页面改它则选择坍缩为该文件））；`settings`（`config` 全量 + `load/save/applyTheme`）；`task`（**应用级单例**：`refresh()` 全量对齐 + **一条多路复用 SSE 流覆盖所有任务**（`streamAllTasks` 连 `/api/tasks/stream`；仅在「至少一个任务非终态」时保持，最后一个任务终态即关流，空闲标签页不占浏览器 ≈6 条/源的连接额度；`refresh()`/`control()` 有活动任务时重开）；`applyEvent(task_id, e)` 按 type 分发（`snapshot_all` → 整表替换；日志超 1000 条裁最旧、`llm_stream` 客户端 128KB 上限与后端对齐）；`control`/`stopAll`/`startStream`（遗留别名 → 确保应用流）；`activeTasks(module?)` 返回非终态任务（可选按 module 过滤），供五个发起任务的页面在 `onMounted`（`await taskStore.refresh()` 后）把刷新后丢失的本地 id 引用重新挂回在途任务——进度/日志/取消随之恢复，label 格式依赖见「已知陷阱」；store 创建时即 `refresh()`）。
- **文件选择 = 隐藏 `<input type=file>` → `uploadFile` → 绝对路径**（`utils/fileops.pickFile`）；目录/文件浏览用 `DirPicker`（扫 `GET /api/files/list/{目录名}`，支持 default/`__all__` 行、pick-dirs、后缀过滤，`@scanned` 回传绝对路径）；下载 = 导航 `GET /api/files/download/{目录名}/{文件名}`。
- **组件**：`LiveLogPanel`（进度条 + 当前步骤 + 分级着色日志 + 自动跟随 + actions 插槽放取消钮）、`LiveStreamPanel`（原始 LLM 流回看）、`MiniAudioPlayer`（`useAudioBus` 全局单例保证同屏只播一个试听）、`WorkspaceGateAlert`、`Toaster`。`utils/log-follow.useLogAutoFollow`：仅当用户本就在近底（≤48px）时才自动滚底——翻看历史不被拽回。
- 页面任务展示惯例：解析页**文件行 = 进度行**（文件选择行即进度展示：复选框 + 文件名 + 状态 Badge（排队 N/总 / 解析中 / 已完成 / 失败 / 已取消 / 已暂停）+ 速度（`llm_cps_10s` 字/s）+ 行内细进度条（复用 `Progress`，`h-1.5 w-24 sm:w-32`，indicator 按状态着色：解析中 = primary / 已完成 = emerald / 失败 = destructive〔保留已跑到 frac〕/ 已取消 = 灰、排队 = 0 值纯轨道）+ 百分比 + 行内按钮（在途 → 【取消】；失败 → 【重试】`control(id,'retry')`；已完成 → 【下载 JSON】）；失败行下方 `text-xs text-destructive` 错误文案；**文件大小显示已删除**；排队位置 = `fileJobs` 中 1 基下标 / 总数（F5 恢复按 `seq` 升序重建勾选序）；「解析进度」日志 Card（每文件一行：`LiveLogPanel` + 完成后 `LiveStreamPanel` 可回看）显隐受设置 `ui.show_parse_logs`（默认关 = 整个 Card 隐藏）；三指标（并发数 / 吞吐量 Σ`llm_cps_10s` / 处理速度 Σ`llm_chars`÷Σ`llm_secs`，**纯 SSE 驱动无定时器**）**双位置**：开 = 日志区顶部、关 = 「开始处理」按钮行下方紧凑小卡；「开始处理」按钮配【取消全部】（`cancelAll()` → `cancelParseBatch` = `POST /api/script/cancel-batch`，失败回退逐个 `control(id,'cancel')`）；工具栏【全选】只勾**未完成**条目、【全量全选】无视状态全勾（含已完成），计数附「含已完成 N」——执行按**实际勾选原样提交**（前后端均无状态过滤，后端只去重）；文件行徽章：「已完成」= `03_parsed_json/` 已有该文件基文件（可再次勾选重新解析）；角色配音 / 音频合成页保留 `_checked.json` 选择过滤，作为旧工程遗留孤儿的守卫（防旧孤儿被选为源）；角色配音/合成页各自 `LiveLogPanel`（角色配音 watch 任务 progress 变化即重拉角色表）；分集页用行内 Alert（`task.current` + 进度条 + 取消）；合成页每行「已合成/总段落 · 角色 · 已就绪声音」用 `batchStatusFiles` **3s 轮询**（读增量 manifest 真实计数，不走 SSE；文件全部完成瞬间行上出现【已合成】徽章；轮询只在运行中任务非终态时进行）；合成页「开始音频合成」按钮下方**进度指标卡**（「已合成 / 总段数」·「已合成 / 总字数」，`task.seg_done`/`seg_total`/`seg_chars_done`/`seg_chars_total`，**纯 SSE 驱动**——后端每段完成经 `segments` 事件推**全量累计值**、1s 节流，无定时器；仅 `seg_total > 0` 时显示，任务终态 `taskId` 清空即隐藏、由结果卡接管；F5 / 断线重连经快照回放自愈，与行级 3s 轮询是两条独立通道）。本页是 keep-alive 缓存页：**`onUnmounted` 在导航时不触发**，行刷新与轮询定时器都挂在 `onActivated`/`onDeactivated` 上（隐藏时定时器停发、不产生请求；重新进入时刷新行并在任务仍在跑时恢复轮询）。合并页行 = 进度行（复选〔在途行禁用〕+ 包名 + 四态 Badge：合并中 / 已合并 / 已就绪 / 已合成 C/T 段 + 按状态追加：行内 `Progress` + % +【取消】、或【下载】+ `MiniAudioPlayer`、或【重试】+ 错误行；行内 `LiveLogPanel` 仅 RUNNING/PAUSED 渲染，防 N 个 PENDING 壳渲染 N 个空面板；**无轮询**——磁盘口径（行列表/段数/MP3 存在性）onMounted/onActivated/手动刷新拉取，「合并中 → 已合并」实时性由 SSE getter 式 watch 驱动（终态任务：succeeded + `.mp3` → 行标记 + `project.recordMerge`〔单值、最后完成者生效〕+ toast，`.wav`/failed/cancelled → 清标记回落；`processed` 集合防快照重放重复 toast）；行任务按 label 尾部**派生式重挂**（无本地 job 列表，F5 后 store `refresh()` 即自动重挂）；工具栏【全选】只勾**已就绪**行、【全量全选】无视状态全勾、计数附「已就绪 Y · 含已合并 K」；【取消全部】= 逐任务 `control(id, 'cancel')`（无专用端点））。

## 测试（827 个，`backend/tests/`）

| 文件 | 固化的行为 |
|---|---|
| `test_text.py` | 排版：内容保持、幂等、空白/省略号/重复标点、孤立 ASCII 保护（URL）、对话/叙述分段、句断开关、章节隔离与正负样例、10 开关逐一、端到端样例 |
| `test_book.py` | 分册（每章一册）：章节铺满/真实章头/无章节即停（空文件名表）、**无损往返拼接 == 原文**（逐章切片）、字数排除换行（非 BMP 计 1）、命名格式（`分册NN 第XXX章`，NN 宽随章数、章号宽随最大章号，双宽独立）/补零/缺口不重编号/重号靠 NN 消歧/全书文件名（含清洗）/`EXPECTED_CHAPTER_FORMAT` 文案、编码探测（BOM/UTF-8/GB18030）、中文数字解析、序号体检、zip STORED 往返；**智能识别（`smart_repair`）**：干净书 → `clean`/全 `kept`/往返 == 原文、缺号只重编号不拆（`gap_absorbed`+`renumbered`）、被吞并重复章同内容 → 截除（kind=`truncated`、往返 = 原文挖除被删尾）、异内容 → 拆分双留（`duplicate_kept` medium、后段取重复份标题）、顶层重号 → 删除（kind=`dropped`）、异常长章推断拆分（切点落 `\n\n` 段落边界、段号 numStr 推断阿拉伯、置信 low、往返 == 原文）、**对修复后结构再跑 → `clean`（幂等）**、长章无空行 → 精确切 + `inferred_split_mid_paragraph`、末章/编号连续异常长 → 不拆**静默保留（零警告、status `clean`**——无缺号 = 普通长章）、章号不可解析（第〇章）长章 → 不拆 + `long_kept`、<5 章/中位数过低 → `length_disabled`（baseline None）、中文数字（一/二/五…十）推断段、文件名双宽（1000 → `第 001 章`、1001 → `第 0001 章`/末 `第 1001 章`/空 title → `第 001 章.txt`）/sanitize 与判重唯一、指纹忽略空白、跨号同内容 → 仅 `content_collision` 警告不删、**保留章 + 已删 span 铺满 [0,len] + 逆序挖除往返**（四种 fixture 形状通用） |
| `test_audio.py` | 分集：时长解析、均分铺满且每段≤目标、只动内部边界/严格单调/窗口回退、**段数不变**、分集命名（`{}`/无 `{}`/非数字 start）、停顿日志解析（负 start/尾部封顶/排序）、真实 ffmpeg 集成（缺则 skip） |
| `test_merge.py` | 合并编排：命令含 `--tmp-dir`/`--merge-batch-size 100`、两阶段日志、成功/失败路径都清暂存、WAV 兜底搬迁、manifest 缺失/为空/全缺文件错误、缺文件跳过 + 警告、遗留输出名、**merge 闸作用域**（快速失败不占槽〔他方持闸时立即失败、闸不动〕/持槽时 spawn 前零调用〔release 后才 spawn〕/成功与引擎失败路径 `active` 配平/排队中取消 ≤1s `TaskCancelled` 未取槽零残留/成功路径 `06` 下无 `*.wav`〔MP3 直出需求固化〕/`concurrency_limit` 公式〔cpu 2/3/4/8/16/64/None → 1/1/2/4/4/4/2〕） |
| `test_merge_batch.py` | 批量合并投放（`api/tts.run_merge` 直调、`merge.run` monkeypatch 为用**真 `merge_gate()`** 的可控桩；直调不走 HTTP 层）：**PENDING 壳按提交序创建**（首轮恰投预取深度 4、其余 PENDING、seq = 请求序、label 钉死 `合并音频（Merge）：{pkg}`、响应 `{task_ids, packages}` 顺序）、**预取上限与严格顺序**（监控采样全程 gate ≤C、在跑 ≤C+4、投放恒为提交序、释放后补投至全终态、协调者退出）、**CPU 定闸上限**（端点每次请求按 `concurrency_limit()` 重置）、空选择 400（两字段皆缺——无最近包回退）、旧 `package` 字段 400（「前端版本过旧」文案）、穿越名/空串 400、去重保序、**同包在途 409**（含冲突清单；追加不冲突包放行；混合 → 整批拒绝）、**逐任务取消全部收敛**（未投放壳就地终结、在跑协作取消、gate 归零）、**merge-status 口径**（total = 源 JSON 可合成段数 / **JSON 100 段 + manifest 60 全 ok → total=100 不判就绪** / 源缺失降级 manifest 长度 / 段文件缺失计未完成 / 零行 / 穿越 400 / 无工作空间零行） |
| `test_script.py` | 解析：切分无损/打包/分句、**章标题防丢**（尾部标题 → 下一 chunk 开头 / 独占标题独立成块）、JSON 清理/修复/正则抢救、乱码修复、默认提示词加载、**流式与非流式字节级一致**、思维链不进返回值、流中取消穿透不重试、**忠实性校验**（忠实输出通过 / 丢行必抓 / 引号标签剥离免疫误报 / 短引语忽略）、**对半切**（段落/换行/句末回退、无边界 → 保留整块）、**忠实性恢复 e2e**（丢行 → 翻倍 max_tokens 挽回、请求体钉死 100→200 共 2 次调用 / 两轮皆败 → 一级对半切、调用数钉死 4 / 不可再切 → 保留最好结果共 2 次调用）、**同人段落合并**（`merge_adjacent_same_speaker`：连续同人并 1 条 / 链式合并取最左 instruct / 章标题两侧不并 / 词字符口径超上限不并〔≤100 挡 120、放 max_chars=4000〕/ **≤10 强制合并**造 >100 块 / 多成员贪心（块+段≤100 并入 / 较短≤10 并入 / 否则封块）/ 章标题反例「楔。子」重跑 `title_test` 拦截 / instruct 取词字符多者 / 输入不动）、**断句失败校验**（检测器正负钉死：包裹 + 引号内「…道 + 冒号」触发 / 无包裹 / 无冒号 / 包裹外标签不触发 / 空包裹 / 非 dict 跳过、开头标签剥离（含「知道：」形态 / 中段不剥 / 未包裹不动）、忠实性门（标签丢弃单段 / 标签保留双段 / 原样包裹 通过；丢词 / 增词 / 花名册外角色 / 多段同主体 / 段序颠倒 / 空段 / 缺 text / 空 / 非 dict 拒绝）、投票协议（2:0 早停共 2 调 / 2:1 第 3 调定 / 1:1:1 → 第 4 调 / 4 异不猜 / 不可解析与未过门无票 / 取消零调用上抛）、无标记零 LLM 调用、**降序应用钉死**（1→2 拆分不移位后续窗口 / 输入列表保持原样）、无共识原对象原样返回、**纯归属标签条清理**（谓词正/负钉死：五动词×标点形态命中（。/，/：/！/……/无标点）/ 知道·难道 形态守卫 / 混合长条（>10 字）/ 带引号 / 章标题（空格形态）/ 角色条 / 空·纯标点·非 dict 不中；邻接：台词前后均删 / 孤立留 / 双旁白夹留 / 隔一条留 / 非级联（双标签只删紧邻台词者）/ 空 speaker 非对白、**e2e 删除**（[旁白, 标签, 台词] → 2 条、解析外零 LLM 调用、无合并）/ **e2e 先删后抽样**（rate=1.0 全量抽样窗口里无标签条、零分歧 1 调零改动））、**generate_file 全 e2e**（1 解析 + 2×2 校验 = 5 调用、拆出的旁白段 = 独立纯标签被确定性删除（`tags_deleted=1` / `merged_same_speaker=0`）、直引号形态单条重写、「知道」负例原样、结果含 suspicious / suspicious_fixed / tags_deleted；**必须传 `spot_check_rate=0.0`**——默认 0.05 会跑归属抽样打破调用数断言）、**归属抽样**（预算表钉死 @0.05 各 N + rate=0/负/空关闭 + rate≥1 全量、标签检测正例〔人名×五动词 / 他她×五动词〕与负例钉死〔他知道 / 她知道 / 不知道 / 谁知道 / 他难道 / 这道理 / 林某知道 / 纯旁白 → False〕、前一条规则〔标签可在前一条 NARRATOR；前一条是角色台词不算〕、tier 组合 0/1/2/3 钉死 + 短边界 10 中 11 不中 + 多角色 4 中 3 不中 + i=0 窗口 clamp、两桶不相交/同 seed 同选择/换 seed 不同（N=200 固定 seed）、级联高 tier 先抽 + tier 0 补足用满预算 + 级联不变量、历史文件 55 追加留最近 50 + schema 钉死 + 损坏/缺失 → `[]`、关闭零 LLM 调用、协议〔零分歧 1 调零改动原对象 / 2:1 首判+1 重试即停只改 speaker / 4 调无共识保留原对象 / 去引号三规则〔严格一致采纳 / 不符 WARNING 忽略 / NARRATOR 终值不剥〕/ 取消上抛输入不动〕、**spot 全 e2e**（1 解析 + 2 spot = 3 调用、纯随机桶目标 2:1 改判随基文件写出、六 `spot_*` 字段、历史文件读数 + 03_parsed_json 不被历史文件污染）、**取消 e2e**（取消落 spot 阶段 → 基文件与历史都不落盘）、**开关关闭语义**（`revalidate_splits=False` → 校验 LLM 零调用 + `suspicious=0` + 日志「已关闭（配置）」；`delete_saying_tags=False` → 标签条保留（与邻接旁白合并）+ `tags_deleted=0` + 同一日志）、**重判批协议**（自已退役的独立检查模块整体搬入：`build_batch_window`〔target 标记/边界 clamp/零 n 只含目标/`skip` 条不标 target/原 speaker+text 透传〕、`parse_speaker`〔对象/围栏/思维标签/单元素数组/裸 token/垃圾→None〕、`parse_speaker_map_full`〔text 捕获/非 target 与空白丢弃/speaker 投影〕、`strip_outer_quotes`〔6 种引号对/内层引号保留/未包裹或空内部→None〕、`_pick_majority`〔严格多数/平票/孤票/异值→None〕、`group_retry_indices`〔间距 + 批上限分组〕）、**超长段落检查 + 纯标点吸收**（`split_long_text` 单元〔短文本原样 / 句末取最接近目标宽度 / 子句界 / 定宽硬切 / 唯一边界远离目标仍取之 / ASCII `3.14` 保护 / 引号 parity 让位〔切点在引号内不安全 → 让位定宽 ±20 窗安全位〕/ 整条引号包裹裸定宽 / `max_chars≤0` 钳 10 / 骨架无损 + ≤ 上限不变量〕、`_quote_parity` / `long_entry_indices` / `split_long_entries`〔speaker 继承 + instruct 只首段 / 零命中原对象 / 输入不动〕、`absorb_punct_entries`〔前邻 / 后邻 / 无邻接删除 / 标题守卫双向 / 纯标点链 / 双向累加 / 零命中原对象 / 输入不动〕、A-LLM 单元〔零命中精确日志 + 零调用 / 2:0 多数替换 + 窗口注记 `LONGER than 200 characters` + 花名册 + 逐条日志 / 4 次未过门（花名册外角色）保留原对象 + 「超长段落重切4 次仍无共识」/ 取消上抛零调用〕、**A 段 e2e 双路径**（LLM 重切：1 解析 + 2 重切 = 3 调用、`long_split==0`、基文件全 ≤200 字 + 重切 instruct 随基文件；机械兜底：重切回复 = 原样单条 → 投票采纳后仍超长 → `long_split==1`、骨架无损、speaker 继承、`merged_same_speaker==0`〔合并在切段**前**运行、切段是末段产物合并看不到 → 不回粘〕；**同人在切段前顺序 e2e**（≤10 强制合并〔5≤10、215>100 仅强制路径〕造 216 字同人块 → 末段机械分段切回 [200,16]、`merged_same_speaker==1` + `long_split==1` + 全 ≤200 + 骨架无损）；`check_long_paragraphs=False` → 零重切调用 + 超长条原样落盘 + 字段 0 + 「已关闭（配置）」日志）、**B 段 e2e**（解析产物含独立「……」NARRATOR 条 → `punct_absorbed==1` 拼入前邻；`absorb_punct_entries=False` → 原样保留，对照开关开 = `punct_deleted==1`）、**槽作用域与排队取消**（`test_generate_file_slot_scope`：`acquire` 恰 1 次（返回 True）/ `release` 恰 1 次且先于检查阶段入口〔phase("check") 与 progress 0.9 文案〕、`finally` 配平；`test_generate_file_cancel_while_queued`：C=1 他方持槽 → 排队中取消 ≤1s CANCELLED、未取槽、零 LLM 调用、零产出；`test_generate_file_cancel_mid_chunk_releases_once`：chunk 期取消 → acquire1/release1 配平、零产出） |
| `test_script_batch.py` | 解析批投放（`api/script.py` 直调、`generate_file` monkeypatch 为用**真共享闸门**的可控桩）：**PENDING 壳按勾选序创建**（seq = 请求序 / 协调者首轮投满预取深度 4 / 其余保持 PENDING / 批次注册表 + 反向映射 / 释放后按序补投至全完成 + 批次注销）、**预取上限与严格顺序**（稳态 = C 持槽 + 等待 ≤4〔监控采样全程钉死 gate ≤C、在跑 ≤C+4〕/ 启动次序恒为勾选序 / **释放条件按「未释放的持槽者」判定**——防陈旧波次误满足尺寸条件〔历史竞态根因〕）、**检查期释放预取余量**（全部任务 phase="check" + 零完成时协调者仍投完全部——核心不变量「等待 ≤4」而非「在跑 ≤C+4」）、**取消全部停止投放**（未投放 PENDING 壳就地终结 / 在跑经 stop_check 与协作取消全 CANCELLED / gate 归零 / 批次注销）、**幂等**（终态 + 未知 id 忽略不抛 / 新批次只取消自身） |
| `test_llm_rate_window.py` | 吞吐量 10 秒窗：样本数/零跨度防除零、真实均值、年轻任务部分跨度、累计单调、样本逐出、SSE 事件 |
| `test_task_bus.py` | 多路复用 SSE 总线：manager 创建的任务事件全量转发为 `(task, event)` 元组、退订即停、独立 Task 无广播钩子、**队列满时丢纯展示事件（`llm_chunk`/`llm_rate`/`segments`）保关键事件**（驱逐最旧展示事件重试 / 无可驱逐则丢弃新事件而非打乱既有关键事件）、`/stream` 路由（`snapshot_all` 回放含新建任务 + 直播事件带 `task_id`） |
| `test_tasks.py` | TaskManager 延迟启动（PENDING 壳）生命周期：`create(start=False)` = 已注册（get/list 可见、计入非终态）但无线程/无 RUNNING 事件/`started==0`、默认 `start=True` 行为不变、`start(task_id)` 起线程跑完（held worker 钉死 RUNNING 窗口）、start 非 PENDING → `ValueError` / 未知 id → `KeyError`、**PENDING 上 cancel = 就地终结**（CANCELLED + `finished` + 带快照终态 status + `final` 事件、迟到的 start → `ValueError` 状态不变、**retry 复活**壳 → SUCCEEDED）、**start/cancel 竞态 25 轮**（终态必达、无悬挂 PENDING、无线程死锁） |
| `test_voices.py` | 角色配音：JSON 对象抽取、说话人名归一化（CJK 保留）、Jaccard、采样带/窗口、`pick_ref_text`/fallback、`_sanitize`、foundation/clone 状态推断、**自动备选数对数分档**（龙套<20=1/量级锚点 100→3·200→4·500→5·1000→6·2000→7·2300+→8/单调有界/真实规模 e2e：旁白 2 万条不压主角）、**候选簿记**（新格式清洗/旧格式合成 1/无克隆空）、**别名台词归并**（链防环/出域丢弃）、**make_clones 全 e2e**（fake-worker 真子进程经真 `run_worker`：固定/自动计数、部分失败重编号、全失败保留旧 ref_audio、取消角色级原子、new_only 差额、进度单调（worker 驱动、重启不回跳）、看门狗缩批重启、size-1 两次超时隔离、8 次上限、断点采纳零作业短路、批内共享 seed、**规划开关 `--disabled-checks`**（缺省省略 / 关一项 → `vram` / e2e 带 flag 经真 `run_worker` + fake worker 跑完全 ok、磁盘 run 日志证明 flag 到达子进程命令行）、select 端点（选择/清除/400/404/409 工作空间/409 任务在途）、list_voices（候选字段 + 列表台词数降序） |
| `test_tts_batch.py` | 合成编排：段构建/`_voice_usable`、concurrency 钳制（含 0 与负值）、看门狗缩批重启成功、workers=1 两次超时隔离（manifest 记 ok:false）、8 次上限、运行日志镜像、resume/显式 indices、**增量写 manifest（逐条内存态 + 节流落盘、收尾强制落盘）+ 工作空间相对路径**、零段不启动引擎、**`--disabled-checks`**（缺省省略 / 关闭项按规范序入值 / `disabled_planner_checks` 助手：全开 `""`·单项·全关五名规范序）、`batch-status`、**多文件池化**（池行 per-row `out_dir`/`file_index`〔纯函数 + seg JSON 钉死〕/零池不启动引擎/已完成文件零池行/跨文件 resume 保留原 path/看门狗池 index 映射隔离只落对应章包/取消 flush 全部 manifest/引擎级一章全败进 failed 列表不写 error/全池全败 RuntimeError/结果形状+文件序/cmd 形状不变+包冲突守卫/准备阶段 fatal 隔离/取消穿透）、**batch-status 多文件**（计数/complete/ready/missing/零字典/`__all__` 400/无工作空间降级）、**run_batch 分发**（多→`synthesize_multi`/单→旧路径/`indices`+多 400/`__all__` 400）、**reset_batch**（删包目录/无包空操作/`_checked`→基包/`__all__` 400/无工作空间 409/任务在途 409/**e2e：重置后同一条普通调用重做全部**）、**超时降档恢复 `--restore-stack`**（默认运行 cmd 无该 flag〔省略模式〕/超时降档记录本批总字数 `3000:4`（1000+2000 字段钉 chars 口径）+ 减半 `--concurrency "2"` /`[restore]` 行弹栈后再压——次条降档记录用**恢复后**挡位 `1000:4` / 连续降档 4→2→1 栈编码 `3000:4,3000:2` 最旧在前 / workers==1 记罚**不推记录**（栈跨两次记罚不变）/ 未知索引 `indices=[99]` 零字数不推 / `[restore]` 无对应记录（漂移）→ WARNING 仍按上报同步、运行不失败 / **池化版**：in_flight 池 index → 池行 text 求和进栈 + `[restore]` 同步池循环 `workers`）、**进度指标**（`handle.segment_stats` 全量累计推送：单文件基线 + 节流终推〔默认节流下恰 2 推〕/ 节流 0 时逐段推〔钉死每段累计值〕/ resume 基线含运行前 done〔单调不回跳〕/ 失败段不计入 / **池化全池口径**〔基线两文件全表、逐段累计跨章边界严格递增、强制终推 = 末段值〕/ prep-fatal 文件排除出总数且仍按文件隔离 / 池化 resume 基线） |
| `test_tts_worker.py` | worker 纯函数（importlib 加载，无 torch）：子批规划（行必入恰一批/长度分档/字符上限/超长独批/比值拆批/**长度比 3 边界**（恰 3x 同批 / 超 3x 拆）/design 行缺省独行、`force_rows_cap` 才共享）、**跨角色统一队列**（`order_speaker_groups` key 通用：队列行数降序 / 平手首见序 / 队列内长度升序 / 不同 key 绝不混 / 跨角色同长度行补满批〔A2+B3+C1=6〕/ 跨角色长度差 >3x 仍拆批）、**长度比例解码上限**（`max_new_tokens_for_chars` 短行 floor / 中段比例 / 长行顶格 / 单调 / `_generate_rows` 三 vtype 透传钉死〔clone 含逐行 prompt 条目、不同角色各拿各的 reference〕）、显存估算（`plan_row_tokens` 标量 / 逐行 overhead）、`band_cap_for_chars` 钉死值、**规划开关 `--disabled-checks`**（`parse_disabled_checks` 空白容错/去重/未知名抛错含合法名列表；vram 关 = 用户真实 32×50 字场景开时首批 2 段·关时单批 32 段 / 分档关 / 总字符关 / 超长独批关的诚实交互〔3000 字行仍被分档+比值强制独批，三项同关才合批〕/ 比值关 / 五项全关 = 纯手动上限）、`VramGovernor`（减半/增长/floor/故障/标定）、超时预算（cpu/clone/gpu 曲线）、看门狗、design-batch 纯函数（`_generate_rows` 逐行 instruct + `force_do_sample` 透传、`_save_and_report_design` 协议行 + 逐行容错）、合并纯函数（批覆盖/`boundary_gap`==单遍/进度带单调）、**顶层 stdlib-only + 旧调度器已删除**的模块约束、**降档恢复 `--restore-stack`**（`parse_restore_stack`：空/None/空白 → `[]`、空段跳过、空白容错、顺序钉死〔最旧在前〕、畸形〔缺冒号/多冒号/非整数/`0:4`/`-5:4`〕→ ValueError 且信息含肇事条目；`plan_next_sub_batch_with_restore` 真规划 + 真 VramGovernor：空栈 = 与纯规划逐字段相同 / 确认〔降档批与试批均 < 阈值 → 试规划行成批、`new_max_batch`/`restored_cap` = 恢复值、`gov.cap` 已抬、`manual_cap` 留给 driver〕/ 拒绝〔试批超阈值 → 小批照跑、`gov.cap` 回退、`restored_cap=None`、记录留待下轮〕/ `cap ≤ max_batch` 记录跳过 / **LIFO 只查栈顶**（底记录可确认而栈顶拒绝 → 不恢复）且不改栈〔driver 弹栈〕） |
| `test_tts_stress.py` | 压测引擎（临时测试入口，纯函数；Task worker 经页面手工覆盖）：自动生成文本（精确长度 / 零与负 → 空 / 仅 CJK+句读 / 行偏移错开 / 超大偏移可切片 / 2500 上限）、轮次判定（限时 = 字数 ÷ 10 字/秒 / 限时内与恰界通过 / 超时带「10 字/秒」/ None → 无法测得即失败）、克隆音色选择（自动取首个可用克隆 / 显式非克隆或缺 ref 报错 / 无克隆报错） |
| `test_pathio.py` | 路径模型：相对/绝对/外部资源、`..` 越界拒绝、工程搬家两级恢复、幂等迁移、`migrate_entries_in` list/dict；voice_config 含 candidates 时只迁顶层 `ref_audio`（嵌套候选相对路径不动） |
| `test_paths.py` | Layout 惰性/ensure、指针解析（相对→项目根）、`resolve_parsed_json(_all)`（只读基文件/显式 `_checked` 名直读/孤儿惰性〔指名基名忽略孤儿、自动选取取最新基文件、`__all__` 排除孤儿、仅孤儿目录 → 遗留名兜底/空列表〕/`__all__` mtime 序/遗留名兜底）、**08_bgm**（`Layout.bgm` 惰性/ensure、`dirs()`/`WORKSPACE_MARKERS` 含 `08_bgm`）、**`MUSIC_LIBRARY_DIR` 恒为项目根级**（`PROJECT_ROOT/music_library`，不随工作空间指针移动） |
| `test_config.py` | 配置 round-trip、两文件模型（根只写指针/工作空间独立/不覆盖已有）、TTS 字段缺省（三模型 id、500/250、parallel_workers=1 ≠ batch_concurrency=4、**五个子批规划开关 planner_*=True**）、GenerationConfig 解析内检查缺省（revalidate_splits/delete_saying_tags/check_boundary_speakers = True、check_batch_size=20、check_context_window=4、**超长/吸收三开关** check_long_paragraphs=True / max_paragraph_chars=200 / absorb_punct_entries=True + **同人合并开关** merge_same_speaker=True + round-trip）、UIConfig 缺省与 round-trip（**show_parse_logs=False** / 旧配置缺字段 → 默认填充 / `update_config` 落工作空间配置）、**BGMConfig 缺省与 round-trip**（`volume=0.18` / `fade_in=1.5` / `fade_out=3.0` / `loop=True` / `min_match_score=1` / `analysis_chars=6000`、旧配置缺 `bgm` 段 → 全默认填充、部分 round-trip 深合并不丢邻段）、遗留字段保留 |
| `test_concurrency.py` | 闸门：clamp≥1、增/减上限、acquire/release 配平、唤醒等待者、**`acquire(stop_check)` 协作取消**（limit 被持 → stop_check 触发 ≤0.5s 返回 False、active 不变〔未取槽〕/ stop_check 未触发 → 照常阻塞至 release / `None` = 旧阻塞语义） |
| `test_music.py` | 音乐库（`engines/music.py` + `api/music.py`，沙箱 monkeypatch `core_paths.MUSIC_LIBRARY_DIR`）：纯函数（`normalize_track_tags` 四类分桶 + 词表外归 custom、`is_generic_track` 四类全空、标签重名跨四类 409、改名/删除**波及**注册表 + 全部 track.tags + `chapter_music_analysis.json`〔assignments 快照**不改写**〕、`validate_music_name` 穿越/扩展名守卫）；索引 IO（`update_index` 模块锁原子事务、缺失不写盘、损坏降级空索引、`write_bytes` 无 CRLF、并发读改写不丢更新）；API（upload 同名 409 不覆盖 / 非 mp3·wav 400 / 空文件 400 / probe 时长 nan→0 不阻断、preview 穿越 400、locked 引用删除 skip + 文案、批量 enable/tags/delete 空 400 缺失静默 skip、suggest-tags 同步 LLM：model 空 400 / 全败 502 / **词表外名字过滤** + 钳上限〔fake LLM monkeypatch〕）；**AI 推荐提示词/解析**（`build_suggestion_prompts` 钉死〔stem/描述/词表行、空描述「（无）」〕、`parse_suggestion_reply` 词表过滤 + strip/去重 + 钳上限 scene2/mood3/emotion2、非对象 → None）；**候选缓存 IO**（缺失不写盘 / 损坏降级 / `_coerce_suggestions` 坏形态降级 / `write_bytes` 无 CRLF / 中止事务不写、`clear_suggestion` 缺失 no-op 不写盘 / 只删指名条目）；**`/library` 候选过滤**（孤儿候选隐藏不删、候选**不并入** track.tags）；**标签决定消费候选**（PUT tags → 候选清除 / 非 tags 补丁 → 候选保留）；**`suggest_track_tags` worker e2e**（fake LLM 真 TaskManager + 真共享 LLM 闸：成功只写本曲候选条目 + 索引不动 + 闸配平 / 2 败 → failed「AI 推荐失败」零落盘 / 不可解析 2 调 failed「回复不可解析」/ model 空快速失败 / 缺文件「音乐库中找不到」/ **排队中取消 ≤2.5s CANCELLED 未取槽零落盘**）；**apply-suggestions AI 推荐采用**（untagged-only 采用 + 消费候选 / **手动标签绝不覆盖**〔skipped_manual + 候选保留〕/ 陈旧候选名折叠进 custom〔同 PUT 口径〕/ 守卫 空 400 · 穿越 400 / 缺失报告 + 去重） |
| `test_music_batch.py` | 批量 AI 标签投放（`api/music.py` 直调，真共享 LLM 闸 + 可控桩，`test_bgm_batch` 同款）：**PENDING 壳按序 + label 钉死**（module `music-ai-tags` / label `AI 推荐标签：{name}`、首轮恰投预取深度 4、其余 PENDING、seq = 请求序、全终态严格顺序）、**预取上限与严格顺序**（监控采样 `gate().active ≤ C`、在跑 ≤ C+`BGM_PREFETCH_DEPTH`、投放恒提交序）、**端点定闸**（每次请求 `set_concurrency(max_concurrency)` → `gate().limit`）、守卫（空选择 400 / 穿越·扩展名 400 / 缺失 404 / model 空 400）、去重保序（`[a,b,a]` → 2 任务保序）、**同曲在途 409**（含冲突清单；追加不冲突曲目放行；混合 → 整批 409）、**逐任务取消全部收敛**（未投放 PENDING 壳就地终结、在跑协作取消、gate 归零） |
| `test_bgm.py` | BGM 引擎（`engines/bgm.py`，沙箱 2 章 + 2 曲〔battle 带标签 / calm 通用〕）：`sample_chapter_text`（len≤n 原文 / 三窗 `\n……\n` 连接 / 零预算 / 重叠窗首见去重）、`parse_analysis_reply`（每类钳上限 scene2/mood3/emotion2/custom2 + 去重、围栏剥离、垃圾/数组/null → None、缺桶 → 空）、`score_track`（权重 mood3/scene2/emotion1/**custom1**、reason 钉死 `"mood 命中 紧张, 热血(+6)；scene 命中 战斗(+2)"`、跨类不命中、垃圾 → (0,"")）、`match_chapter` 全矩阵（seed 钉死：评分 + 平分随机、min_score → 通用兜底、enabled-only、相邻去重逐级放宽〔含通用池放宽 +「（相邻章去重放宽）」〕、无候选 → music=None、**random 模式**忽略标签 / score 0 /「全章节随机」/ 仍相邻去重）、`build_mix_cmd` 逐元素钉死（`-stream_loop -1`/`-0`、双侧 `aresample=44100`、filter_complex 串逐字节、`-map [out] -c:a libmp3lame`、**短章节 fade 钳 `min(x, 时长/2)`**〔dur4 → `st=2.000:d=2.000` / dur1 → 双侧 0.500〕）、两 JSON 缓存（缺失不写 / 损坏降级 / round-trip 无 CRLF、`list_chapter_stems` 忽略子目录与非 txt）、`match_stems`（**prev = 批内新鲜结果 / 批外现有值**〔单章边界邻章逐字节不动〕/ next 恒现有 / locked 整条原样保留 + `skipped_locked` 计数 / no_bgm 与 random 模式含重启用）、`analyze_chapter` e2e（fake `_llm_chat_completion`：成功只改本 stem + `model` + 闸配平、3 败 → 空标签记录仍 succeeded、model 空 → failed「尚未配置 LLM 模型」、缺章 → failed、**排队中取消** C=1 他方持槽 ≤2.5s CANCELLED 未取槽零落盘）、`mix_chapter` e2e（fake Popen：成功 / rc≠0「退出码」/ 输出 <1KiB「输出异常」/ **排队中取消** 无输出 / 旁白缺失 / 音乐缺失「音乐库中找不到」/ **music=None copy2 字节一致** / 从未匹配「从未匹配」/ merge_gate 配平） |
| `test_bgm_batch.py` | BGM 投放（`api/bgm.py` 直调，真 `gate()`/`merge_gate()` + 可控桩，`test_merge_batch` 同款）：**PENDING 壳按序 + label 两式钉死**（`章节气氛分析：{stem}` / `背景音乐混音：{stem}`、module `bgm-analysis`/`bgm-mix`、首轮恰投预取深度 4、其余 PENDING、seq 顺序、全终态严格顺序）、**预取上限与严格顺序**（双闸：监控采样 `active ≤ C`、在跑 ≤ C+4、投放恒提交序）、**端点定闸**（analyze → `gate().limit = max_concurrency`；mix → `merge_gate().limit = cpu//2`）、守卫（analyze：空 400 / 穿越 400 / 缺章 400 / model 空 400；mix：空 400 / 从未匹配 400 / 旁白缺失 400 / 音乐已删 400 / **music=None 放行**；match：`segment` 400「段落级匹配为后续版本功能」/ 未知模式 400 / 空 400）、去重保序、**同章在途 409**（双模块，追加不冲突放行、混合 → 整批 409）、match 同步落盘（locked 逐字节保留 + `skipped_locked` / 重匹配「放宽」/ 未选中章 `matched_at` 不动 / mode 持久化）、分析在途 409「分析任务在途」（混音在途**不阻挡** match）、**取消全部收敛**（双模块：PENDING 壳就地终结、在跑协作取消、gate 归零）、`GET /chapters` 口径（行基准 = 02 stem 排序 / 孤儿隐藏 / 全字段 / `.wav` 兜底 narration / music_missing / **无工作空间 → `{"chapters": [], "mode": "llm"}`** 不 409）、`PUT /chapters/{stem}`（tags → `edited:true` + `edited_at` + `analyzed_at` 保留 + 词表外归 custom + 快照；music set → `manual:true` / score None /「手动指定」、null 清除、锁定保留、未匹配章建条目、守卫 400〔穿越 / 音乐穿越「非法音乐文件名」/ 音乐缺失〕失败零改动）、四写端点无工作空间 409 |

## 已知陷阱 / 历史遗留（改动时留意）

> 以下均为**有意的设计**而非缺陷——改动时别"顺手修正"；真正可修的陈旧文案（误导日志 / 测试 docstring / README 测试数）已清理。

- **智能识别（`smart_repair`）的产物语义**：① 文件名规范独立于分册——`第 {NNN} 章 {title}.txt`（**带空格**、无 `分册NN` 前缀、N≤1000 三位宽 / 否则四位宽、sanitize 后判重加 ` 2`/` 3` 后缀自带消歧），与分册的 `<base> 分册NN 第XXX章.txt`（无空格、重号靠 分册NN 消歧）是两套命名，**别统一**；**「分册（智能识别结果）」按钮**走 `/split?smart=true`，写出**与智能识别同一套** `第 NNN 章 标题.txt`（同输入 → 同结果，字节一致）；普通「按原识别结构分册」（`分册NN` 命名）与 `as_zip` **已从 UI 与 API 整体移除**（2026-09）——两套命名并存的唯一情形是**旧工程遗留 `分册NN` 产物**（UI 注明「以最近一次产物为准」），**别清理旧产物**（「绝不删除用户数据」）；别把普通分册路径恢复回来（引擎 `make_chapter_filenames` / `build_zip` 保留仅供测试与遗留语义，勿接回 API）。② `LONG_CHAPTER_RATIO=2.0` 而非 3.0：吞掉**一章**的异常长章 ≈2× 中位数，3.0 会漏掉最主要情形；有缺号区间时误报的代价只是报告里一条低置信推断拆分（用户语义 = 永远拆分并报告、从不阻断）；**无缺号区间时（编号连续/末章/乱序）异常长章静默保留、零警告**——老小说长章正常，长度标志本身不是异常（用户 2026-09 拍板），故阈值**有意取灵敏**；**别给无缺号分支恢复 `long_kept` 警告**（唯一保留来源 = 章号不可解析的长章）。③ 被吞并重复章（重号第二份被 `filter_spurious_chapters` 删掉后并入本章尾部）≈2× 基线——所以**重复章内扫不门控在 `_long` 上**，对每个可解析章号章都跑（`filter_spurious_chapters` 只删「孤立下凹」，被吞并的重复标题行必是顶层可检出 → 内扫只会命中下凹行）。④ `status` 只在**全部动作 `kept` 且零警告**时为 `clean`——仅有 `long_kept`/`length_disabled` 警告的书是 `ok` 不是 `clean`。⑤ **`seq` 字段陷阱**：`smart_repair` 返回的 chapters 里 `seq` 已被重编号为 1..N（与 `final_num` 同值），**原始位置序只存在于 `report.removed` 条目的 `seq`（重编号前记录）**——测试/外部代码要用原始 seq 时从 removed 条目或输入章节表取，别读结果 chapters 的 seq。⑥ 保留章单独**不**铺满 [0, len]（截除型删除在被截章与下一章之间留下被删 span）——无损口径是「保留章 + 已删 span 铺满」/「原文逆序挖除已删 span == 保留章拼接」，别改成裸 `"".join(保留章) == 原文`（有删除时必假）。
- **解析的逐 chunk 忠实性校验 = 引语段骨架比对，而非字数比对**（源中 ≥4 词字符引语段的骨架 = 只留词字符，须在输出 text 拼接骨架中作子串找到）——别改回全文字数/多重集对比：模型对引号剥离/标签并入的合法编辑会令字数对比**系统性误报**，触发无谓重发与调用成本；而截断/丢行在引语段骨架下必被抓住。同理 `merge_adjacent_same_speaker` 的合并字数上限（**≤100 词字符**，与 TTS 行超时预算 `gpu chars/15`〔floor 30s〕联动——100 字行的预算仍宽裕（≈4× 预期时长））是有意取紧：合并行保持短小 → 单行失败代价小、节奏更细（≤10 强制合并是 spec 要求的唯一例外，可造 >100 块、由随后的机械分段切回——见「同人段落合并与 200 硬保证的交互」条）——别去掉 ≤100 上限或放大到数千字量级。忠实性恢复阶梯**有意保持短**（只有两级：翻倍 max_tokens 重跑一次 → 一级对半切各跑一次；无 temp-0 重发、无深层递归切分，半段跑完即止）——截断是最常见的忠实性失败根因，翻倍预算 + 对半缩块已覆盖主要情形且调用成本有界；别再加升级阶段（多轮投票 / 递归切分 / 逐级翻倍），调用成本与行为可预测性都会失控。
- **2026-09 解析切分重构 = 先定段数再找最近合法边界，切点只减不增**（`split_into_chunks` 重写，见阶段 2）：**合法结构边界是最高约束**——段数/均长只是目标：目标位附近无合法边界 → 切点落**最近**合法边界（接受偏离均长、允许个别段超 `chunk_size`）；完全没有 → 不切（整块保留）；**绝不拦腰截断句子/对话**（CJK 句末边界是**零宽**的 `(?<=[。！？!?…])`——旧正则 `(?<=[.!?])\s+` 对中文从不触发、正是超长中文段切不开的根因；ASCII 点号须后随空白——`3.14`/`e.g.` 受保护，别统一成一种）。无损口径维持「**去空白后拼接相等**」（坐标系 = 段落 strip 后 `\n\n` 拼接串——未升级到字节级，超需求）。收尾短块合并**只删已有切点**（与较短邻边合并、永不新增切点/重切）→ 每轮切点集严格收缩、必然终止、**无震荡**；合并不得把**章标题行**切成块尾（标题必须与统领正文同段）；两侧都不可并 → **保留短块**（允许，不是异常——别加告警/重试/强并）。悬题改判（切点搜索期把会让前块以标题结尾的候选换掉）**严于** `_evict_trailing_title` 兜底（100 字窗 + carry）——别把兜底删掉（它覆盖「唯一候选即悬题」与书末标题独立成块两种搜索管不到的情形），也别把搜索期改判降回纯兜底（标题独占块是旧贪心的已知产物，新算法标题恒随正文同段）。`chunk_size≤0` 钳 1（防 0 宽无限切）。**角色匹配检查阶段居检查段最前 = pristine 不变式**：断句校验 1→2 拆条 / 标签删除删条都会移动条目下标，边界窗口必须按 chunk 循环刚结束的**原始**列表预建——别把边界阶段挪到断句校验之后（下标会指错条目）。
- **解析后的断句失败校验 = 启发式触发 + 门控投票，不是规则化重写**：检测 = 外层双引号包裹（弯/直任意形式）且引号跨度内「…道 + **冒号**」（`rfind` 取最宽跨度，嵌套同款引号也可见）——冒号是硬条件，`知道/难道/道理` 的「道」后无冒号不触发；但带冒号的 `知道：` **会**触发，此时由忠实性门 + 多者胜保护内容（重推骨架拼不回就无票），别在检测器里加词表黑名单。忠实性门接受**两种**骨架：原文、以及剥掉**开头**「…道：」标签后的原文（重判复用解析提示词，纯标签整段丢弃是其编辑 (e) 的合法产出）——剥后的形态保留内层开引号（`““…`），无害（骨架忽略引号，该形态只进门、永不落盘）；**别把剥标签扩到中段**（中段「…道：」是另一种混合形态，重判协议不会拆条——保持原样，保守不动）。**1 段胜出必须应用**（干净重写），不能当 keep——否则规范失败形态（其正确重判恰是单条剥标签条目）永远修不好。校验位于 `merge_adjacent_same_speaker`（同人段落合并）**之前**（拆出的旁白段随后照常合并）；**复用解析提示词**（无新提示词文件，`prompts.*` 仍是唯一来源）；窗口/花名册按原始列表预建 + 胜出者**降序下标**应用——别改成「每应用一条重取窗口」（下标会指错条目）。
- **纯归属标签条清理 = 六条件合取的确定性删除，不是启发式重写**（解析任务内、断句校验之后、归属抽样之前，见阶段 2）：**≤10 字阈值是精度的主要滤网**——真实语料里上千条以「…说道。」收尾的 NARRATOR 条目绝大多数是「叙述 + 标签」的长混合条（>10 字，绝不被删）或合法的独立标签行，**别放大阈值**（会吞掉混合叙述）；**无引号条件是台词保护**——带引号的短条（“快说。”）是台词内容，删了 = 整句台词丢失，比重判错归属（后有解析内重判/抽样兜底）严重得多，**别去掉**；末字「道」必过**知道/难道 形态守卫**（他不知道。是真实叙述）与**章标题守卫**（第5回 问道 是标题不是标签——注意 CHAPTER_RE 要求「第N回」与标题之间有分隔符）；**非级联**是有意设计（代码无法区分纯标签与恰好以动词收尾的短叙述句，隔一条的宁可留）；**位置在归属抽样之前**是有意设计（抽样重判可把标签条翻成角色 speaker，规则便不再适用，且角色会用本人声音念第三人称标签）；删除不会制造新的旁白相邻对，机械合并不受影响；受 `generation.delete_saying_tags` 开关（**默认开**，开启时零 LLM 成本；关 = 跳过 + 一行日志 + `tags_deleted=0`）；0 命中也留一行日志（静默退出会被用户误读成阶段缺失）。
- **解析后的归属抽样 = 两桶抽样 + 复用检查协议，不是新检查阶段**：`generation.spot_check_rate` 默认 0.05 是**活的**——任何钉死 `generate_file` LLM 调用数的测试必须显式传 `GenerationConfig(spot_check_rate=0.0)`，否则抽样阶段会多出的调用打破断言。**超长段落检查（`check_long_paragraphs`）与纯标点吸收（`absorb_punct_entries`）默认开**——钉死调用数的既有 e2e fixture 全是「短条目 + 无纯标点条」，两阶段零命中零调用故断言不破；一旦 fixture 出现 >200 字条目或无词字符条目就会多出重切 LLM 调用（吸收零调用但会改写条目）——新增/改 fixture 时要么避开这两种形态、要么显式关开关（`_STAGES_OFF` 已含两者）。归属标签检测的 知道/难道 排除是**形态守卫**（人名组贪婪会把 知/难 吞进名字，故查「道」**前一字符** ∈ {知,难} → 跳过该匹配），**别改成开放词表黑名单**（知道/难道 之外的同形态词会漏，词表会腐）；负例由测试钉死（他知道/她知道/不知道/谁知道/他难道/这道理/林某知道 → False）。**历史文件必须在 `config/`，绝不进 `03_parsed_json/`**——后者被 `resolve_parsed_json_all` 整体 glob 当解析产物，混入一个统计 JSON 就污染角色配音的「全部文件」聚合；`config/` 不经文件列表 API。解析任务把抽样修正**烙进自己的基文件**不违反不变量 #9（不变量 #9 说基文件是解析任务的唯一产物、检查阶段永不改写它——基文件本就是解析任务正在写出的文件）。**只记录读数、永不自动降率**：历史文件与任务日志是用户的「仪表」，降不降由用户在设置页手动决定——别加自动降率逻辑。**解析内抽样修正直接烙进基文件，即最终态**（独立检查阶段已退役，不再有后续全量重判兜底——严格多数投票 + 只改 speaker 的协议就是其精度保证）。
- **超长段落检查 + 纯标点吸收（2026-09，用户拍板 = 「理论上不允许有超过 200 字的段落」）**：① **阶段顺序是承重的**——A-LLM（重切）在归属抽样**前**（重切可拆出带新 speaker 的条目，必须被抽样审计）；B（纯标点吸收）在 A-mech（机械分段）**前**（吸收可把邻接 NARRATOR 推过 200 字，须由机械分段兜底再切）；A-mech 是检查段**最后一步** = 硬保证终态。别把 B 与 A-mech 对调（吸收后超长将无兜底），也别把 A-LLM 挪到抽样后（新 speaker 逃审计）。② **分工 = LLM 管语义边界、机械管长度上界**：重判忠实性门要求多段需 ≥2 个不同 speaker → 同一说话人长篇独白的「拆分」**无票**、条目原样保留、必落机械切分——这是设计行为不是失败（`_reparse_vote` 的多主体门是防 LLM 把独白乱拆的保护）。③ **硬保证高于「不拦腰截句」**：三级边界的最后一级 = 定宽硬切（全流水线唯一可拦腰截句的切法）——用户硬需求（绝无 >200 字段落）压过「不拦腰」的美学偏好；切点先查 ±20 字窗内引号安全位、找不到才裸切；①句末/②子句界的切点须**引号 parity**（切点落在引号跨度内 = 不安全，让位下一级——`_quote_parity` 前缀开−闭奇偶）。别「顺手」去掉定宽兜底以保全拦腰美学（硬保证会变软保证）；也别把 parity 判据换成「引号成对出现」（跨度内单侧切点照样把引号劈进两段）。④ **机械分段 = 检查段最后一步，其后无合并 → 切段绝不被回粘**：同人段落合并在机械分段**之前**运行（其 ≤10 强制合并可造出 >200 块的超限块由本阶段切回），切出的段是末段产物、其后无任何合并阶段 → 结构上不可能被回粘（旧「`merge_adjacent_narrator` <100 上限挡回粘」论证已作废——合并不再在切段之后）。⑤ 口径 = strip 后 Unicode 码点数（全应用一致）；机械切段**继承父条目 speaker**、`instruct` 只给首段、非级联单遍；`max_paragraph_chars ≤ 0` 钳 10（退化值无意义）。⑥ 零命中也留一行日志（与断句校验 / 标签删除同款——静默退出会被用户误读成阶段缺失）。
- **同人段落合并（2026-09）与 200 硬保证的交互**（`merge_adjacent_same_speaker`，`generation.merge_same_speaker` 默认开）：① **合并在机械分段**之前**——≤10 强制合并可造出 > `max_paragraph_chars` 的同人块，由随后的机械分段（检查段末步）切回；顺序反了会把机械分段自己切出的同人短段（如 210 字切 `[200,10]`、同 speaker）用 ≤10 强制合并粘回 210，直接破硬保证。别把合并挪到机械分段之后。② **两档字数口径勿统一**：合并上限（≤100 / ≤10）用**词字符数**（`_skeleton` 骨架长度，不计标点/空格/换行），200 硬保证用**原文长度**（strip 后码点数）——两套不同口径，各自服务不同目的（合并看「实读字数」、硬保证看「段落体量」）。③ **`check_long_paragraphs=False` 时合并不再受 200 上界约束**：机械分段被跳过（两半都跳），同人合并的强制合并可产出 >200 字条目落盘——这是用户显式关闭硬保证的情形（配置自洽），非 bug。④ **章标题守卫恒判无豁免**：每次吸收前对运行块文本重跑 `title_test`（旧「已合并块免判」豁免作废）——反例 `"楔"+"子"` 各自非标题、强制合并成 `"楔子"` = 章标题；`is_chapter_title` >40 字短路、成本可忽略。⑤ **角色台词现纳入合并**（旧仅 NARRATOR）：同角色连续短台词并成一条、消除中间同人停顿（250ms）——叙述更自然，但**刻意留白的对白节奏**（`林某：「……」` 紧接 `林某：「不。」`）会被 ≤10 强制合并并掉——spec 明确要求，`merge_same_speaker` 开关可一键回退（关闭 = 跳过 + 日志、`merged_same_speaker=0`，行为回到旧 NARRATOR-only 的**超集**——注意旧函数已删除，关闭后 NARRATOR 也不合并）。⑥ instruct 取**词字符最多**成员（tie → 最左），该成员无 `instruct` 键则删键、不凭空造值（防 exact-dict 测试断言被 `instruct: ''` 污染）。
- **子批规划五道检查是合取且相互交互的**（段长分档 / 单批总字符 / 超长行独批 / 批内长度比 / 显存静态估算，均可经 `config.tts.planner_*` 单独关闭）：关一项**不保证**出现预期的合并——例如 >2500 字的行同时被长度分档与长度比两道检查强制独批，只关「超长行独批」它仍不与短行同批（须 seq_chars / length_bands / length_ratio 三项同关才合批）。别把这种交互当 bug，也别「顺手」为达预期合并而去削弱其余检查（它们各有独立的显存/时长保护动机）。
- **worker 命令恒为 `--flag value` 成对**：`test_voices.py` 的 fake design worker 用通配「见 `--` 开头即取下一 token 作值」解析，混进一个**无值** flag 会把下一个 flag 名吞作它的值，静默改坏命令（如 `--concurrency` 丢失落回默认 1，测试与实跑行为漂移而无任何报错）。后端拼 worker 命令时永不发无值 flag（`--disabled-checks` / `--restore-stack` 协议同样恒带值；空时整体省略而非发空值）。
- **超时降档恢复 = LIFO 栈 + 双门槛试规划，`[restore]` 行是恢复后 `workers` 账目的唯一同步源**（音频合成，2026-09 用户拍板）：workers>1 的每次降档**先记录再减半**——`(超时子批总字数, 降档前上限)`，chars 口径 = strip 后 text 长度（`_build_segments` 已 strip，与 worker 行 `chars` 恒等；池化 = 池行 text 求和）；**in_flight 空/全未知索引不推记录**（0 字阈值是惰性记录，会堵死 LIFO 栈里它后面的所有记录）；**workers==1 的记罚/隔离不推记录**（挡位未变，`(chars, 1)` 是无操作）。worker 侧每轮**只查栈顶**（LIFO 逐级回退，别改成一次弹多条或查全栈）；恢复确认是**双门槛**——降档挡位规划批 < 阈值 **且** 恢复挡位的**试规划**也 < 阈值（全尺寸批仍会超时的不得按旧挡位跑）；拒绝时试规划的 `gov.cap` 回退、记录保持待命（下轮再试）；确认后 governor 处于「该挡位的全新进程」态，**`vram_scale` 保留累积值**（安全偏离，免重探），五道规划检查照作用于试批——恢复只抬 manual 上限，**绝不推翻任何保护检查**（别给恢复路径加 `disabled` 旁路）。`[restore] cap=N` 行是恢复后后端 `workers` 的唯一同步源（弹栈顶 + 同步；漂移 = 解析失败/栈空 → WARNING 仍按 worker 上报同步，宽容不炸任务）；**`attempt` 不因恢复重置**（防振荡的 8 次上限语义不变）。范围 = 仅音频合成（batch 单文件 + 池化）——design-batch（角色配音·克隆）与 merge 无此机制（用户范围 = 语音合成，别顺手扩散）；降档栈**只存运行内存、无持久化、无新配置项**（与降档/记罚机制一致恒生效）；flag 空省略保证默认命令逐字节不变（`test_tts_batch` 的 cmd 形状断言依赖）。
- worker 的 `--mode custom/design/clone` **不被后端任何端点调用**（主链路只用 `design-batch`（角色配音·克隆）、`batch`、`merge`）——保留供手动/历史用途；改 worker 时别误以为它们有调用方。
- **音频合成进度指标 = 全量累计的 `segments` SSE 事件，不是增量、不是轮询**（合成页「开始音频合成」按钮下方卡，2026-09）：`segments` 事件是**纯展示事件**（总线 `_DISPLAY_ONLY` 一员，队列满时被丢弃——前端在下次推送/快照回放时自动收敛，因为每次推送都带**全量累计值** done/total/chars_done/chars_total 而**不是增量差值**，任何单次丢弃都无害；前端 store 直接覆盖四个 `seg_*` 字段）。累计口径 = **运行前 manifest done 集 ∪ 本次运行 ok 段**（union：显式 indices 重做已完成段不重复计数；**失败段不计入**；resume 运行的基线非零 = 运行前进度立即可见、进度永不回跳）；池化 = **全池口径**，prep fatal 的文件**排除出总数**（没有可合成段，但仍按文件隔离、`error` 语义不变）。1s 节流（`STATS_FLUSH_INTERVAL`）只限推送频率、**不丢信息**（全量值）；基线 / 收尾 / 异常路径各**强制推一次**——终态快照与 result 事件携带同一组最终计数器（任务完成卡隐藏后结果卡接管，计数不缺）。别改成增量差值（队列丢弃 / 重连时序会造成累计漂移）、别改成定时器轮询（解析页三指标 = 纯 SSE 驱动无定时器是既有先例；合成页行级 3s `batchStatusFiles` 轮询是**另一条独立通道**——行级状态 vs 按钮下方总量卡，两者并存各有职责）、别加配置项（恒生效、纯展示）。字数口径与全应用一致 = strip 后 Unicode 码点数。
- `config.tts.parallel_workers` 是**遗留字段（无读取方）**——前端初值改读 `tts.batch_concurrency`（音频合成与角色配音·克隆共用的批内行数上限，单进程 GPU 张量批）；仅为旧配置 round-trip 保留，文档/注释里别再当它生效。
- 音频分集页参数的「自动保存」发生在**前端**（fire-and-forget `PUT /api/config`）；后端只按「请求字段 or 配置缺省」取值，自己从不写配置。
- `merge` 的 `m4b=True` 目前只是 WARNING（输出 MP3）——M4B 是后续阶段。
- `config.py` 的读取**永不失败**：文件缺失/损坏/类型不符 → 静默降级（工作空间配置 → 根模板 → 代码默认）；`_load_config_file` 吞掉一切解析异常。写入才是严格路径。
- 角色配音页的「全部文件」（`__all__`）是**该页局部**选择，不写入共享的 `project.activeScript`；音频合成页的多选同样是**局部状态**（`selected` map，键 = 文件名），只在**用户交互**时把**首个选中**文件同步到 `activeScript`（`lastSynced` 守卫防自身同步坍缩多选；角色配音页改它 → 本缓存页选择坍缩为该文件），且拒绝 `__all__`。别把整个多选写进 `activeScript`（单值字段会碾掉多选）。
- `GET /api/files/list/{module}` 的 `module` 参数是**磁盘目录名**（`02_split_text` 等），不是 Layout 属性名。
- **遗留 `_checked` 孤儿 = 惰性文件，resolver 永不读取**（两独立检查阶段退役后应用内再无代码写 `_checked.json`）：旧工作空间的 `_checked.json` 孤儿在自动选取 / `__all__` 聚合里被排除（glob 排除 `_checked` 名），文件列表可见、可下载，显式指名 `_checked` 名仍可直读；**下游结果陈旧时重跑解析（重写基文件）即恢复**，孤儿继续留盘（「绝不删除用户数据」约定，勿顺手清理）。
- worker 的 `--seed` 现为 **batch / design / design-batch 三处共用**：design / design-batch 按 `base_seed + 子批序号` 播种（**同子批多候选共享一个 seed**——复现条件 = 同输入 + 同 seed + 同批布局；design-batch 另加固 `do_sample=True`，防个别 checkpoint 关采样致候选逐字节相同；断点采纳的候选 seed 记 −1）；batch 为 `seed + 子批序号`。别当 batch 专属。
- **角色配音·克隆的任务进度由 worker 的 `[progress]` 行驱动**（后端运行中不发进度，只在终末发「完成」）；worker 经 `--done-offset`/`--total` 参数上报 `(offset+done)/total`，**看门狗重启后进度不回跳**——别重新引入后端侧的进度驱动。
- **`run_worker` 的取消在排空循环内逐行检查**（`handle.check()`，不是只在主循环顶部）：满载机器（Defender 实时扫描 + 磁盘/GPU 争抢）下行处理慢，worker 全速喷出的行在队列里积成大积压——取消若只在顶部检查，点取消后要等整个积压抽干（分钟级）才杀进程树，用户看到日志还在滚动 = 「取消停不下来」（2026-09 实测事故；`test_tts_batch.py::test_run_worker_cancel_not_stalled_by_backlog` 钉死）。同理 run 镜像 `log_file` **块缓冲**（行缓冲的每行 flush 是积压放大器之一）——别改回「只在主循环顶部 check」或「行缓冲镜像」。
- 旧的每作业子进程编排 `_design_preview` / `_ChildReg` **已删除**（唯一调用方 make_clones；阶段 2 现为单一长驻 design-batch 子进程）——别把「每候选一个子进程」的形态恢复回来。
- **自动备选数 = 角色自身台词的绝对对数分档**（`auto_candidate_count(台词数)`，无工程相对分母）：旁白可达主角 10 倍量级，任何"相对全脚本最大值"的比例都会把主角压成 2~3 条——别改回 ratio，也别把分档改回"随工程规模缩放"；锚点（100→3 / 200→4 / 500→5 / 1000→6 / 2000→7 / 2300+→8）与真实规模 e2e（`test_make_clones_auto_counts_follow_ladder`：旁白 2 万条 → 8 且不压主角）钉死该行为。
- **多候选升级后首次 new_only 重渲染**：旧单候选角色 `have=1 < 目标≥2` → 首次「批量制作（仅新增）」会把它们全部重渲染为多候选（特性升级路径，且收敛：重渲染后 have=目标，之后只碰真新角色）；自动模式龙套（目标=1）恒跳过。
- **切换备选数 = 整组差额重渲染**：重跑整体替换候选组，**旧候选文件留盘成孤儿**——`04_voice_profiles/designed_voices/` 随时间累积，页面只列当前候选；「绝不删除」约定下勿顺手清理。
- `selected_audio_id` 在**重渲染后重置为 None**（旧选择对新候选组无意义）；候选 <2 的角色（自动模式龙套、旧格式单候选）「选择音色」按钮**禁用**——均为有意行为。
- `migrate_entries_in` **非递归**：dict 形态只迁每 entry 顶层 `ref_audio`，嵌套 `candidates[].ref_audio` 不动（候选写入时即工作空间相对，永不需迁移）。
- **路由函数的裸 `list` 参数 ≠ 查询参数**（FastAPI 0.141 行为）：`scripts: list[str] | None = None`（不带 `Query()`）会被当成 **JSON 请求体**——URL 里的 `?scripts=…` 被静默忽略（端点落回单文件分支，前端待合成行全 0）；必须写 `Annotated[list[str] | None, Query()] = None`（Annotated 保住普通默认值 `None`，测试才能继续直调函数——测试套件全部直调、不走 HTTP 层，这类参数绑定 bug 测试抓不到，改路由签名后用真实 HTTP 探测验证）。
- **各页的 F5 刷新恢复依赖任务 label 格式**（见「横切·任务系统」刷新恢复段）：文本解析页按 `文本解析（{文件名}）`（全角括号，`ScriptParse.reattachJobs` 正则）取回文件名；音频分集页按 label **前缀** `停顿检测：` / `音频分集：`（全角冒号）区分两类任务；音频合并页按 label **尾部** `：{package}` 派生行归属（**同一契约同时承载同包在途 409 守卫**——`_inflight_merge_packages` 用同一正则 `：(.+)$`；前端 `pkgOfLabel` 取**第一个** `：`，与后端尾部捕获同口径）；音频合成 / 角色配音只按 module 匹配。**改这些 label（`api/script.py` / `api/audio.py` / `api/tts.py`）会静默破坏对应页面的刷新恢复**（页面无报错，只是刷新后看不到在途任务）——改 label 时同步改前端挂接逻辑。
- **池化多文件合成的文件名 = 章内局部 `file_index+1` 零填充到池宽** `max(4, len(str(池总段数)))`——超宽零填充（如池 120 段时 `00038.mp3`）是**有意且无害**的（manifest 存真实 path、merge 按 manifest 排序，文件名宽度只是美观）；**别把宽度改回按文件段数**（跨文件宽度不一致会在同一池内产生歧义路径）。看门狗重启时 seg 文件重写为 remaining 子集、宽度按子集重算（与既有单文件重启行为一致；`zfill` 只补零不截断，归属恒由 manifest + `file_index` 决定）。
- **`synthesize_multi` 的 per-file `error` 只承载准备阶段 fatal**（JSON 缺/坏/空、包冲突）——引擎级「某文件全段失败」不再写 `error`（由顶层 `failed` 列表〔带 `script`，index = 章内局部〕+ 该包 manifest `ok:false` 表达）；成败判定 = attempted（进池或有 error 的文件）且 completed==0 → RuntimeError。别恢复 per-file 引擎级 fatal（池内单文件失败不该中断全池）。池 index 与章内 `file_index` **严禁混用**：池 index 只作调度身份（协议行/记罚/watchdog，经 `pool_map` 唯一映射回章节），`file_index` 只作章节身份（manifest/文件名/结果归属）；重启沿用原池 index 不重编号（跨运行才重新编号，池 index 从不落盘）。
- **解析 PENDING 壳的 cancel 必须就地终结**：壳没有 worker 线程，没有任何代码会观察它的 `cancel_event`——`control("cancel")` 若只置标志，任务永远悬挂在 PENDING（且 SSE 流因「非终态任务存在」永远保持）。故 PENDING 上 cancel = 就地终态 CANCELLED（`finished` + 带快照终态 status 事件 + `final` 事件）；迟到的 `start(task_id)` 是良性竞态 → `ValueError`（协调者吞掉后继续投下一个）。别「简化」回只置标志。
- **解析槽位作用域 = 仅 LLM 分段解析阶段**：读文件 / 解码 / 分 chunk 在占槽**前**、chunk 循环结束即 `release()`、各机械检查/后处理（含同人段落合并 + 超长机械分段）+ 写基文件全在槽**外**——**检查阶段的 LLM 调用（边界重判 / 断句重判 / 超长重切 / 归属抽样）不计入并发上限**（有意语义，用户拍板）。别把 `acquire`/`release` 移回包住这些阶段的旧形态（检查期持槽 = 槽空转 + 预取不补位，正是本次改造要消灭的行为；`test_generate_file_slot_scope` 钉死「release 先于检查阶段入口」）。
- **批次协调者的不变量是「等待槽的任务数 ≤ PREFETCH_DEPTH=4」，不是「在跑 ≤ C+4」**：`waiting = max(0, no_slot − gate().active)`，no_slot 只数 RUNNING/PAUSED 且 `phase != "check"` 的任务——检查期任务既不持槽、也不占预取名额，槽位释放瞬间即有预取补位（稳态 = C 持槽 + 4 预取）；改成「在跑 ≤ C+4」口径会让检查期任务吃掉预取配额，排队末端的文件永远投不出去、闸门空转（`test_check_phase_releases_prefetch_headroom` 钉死：零完成时全部 6 文件仍须投完）。
- **批次注册表（`_BATCHES` / `_TASK_BATCH`）是进程内存态、重启即失**：F5 刷新恢复**不依赖它**（按 label 正则 + `seq` 升序把任务挂回本地引用，任务本身继续跑到终态）；注册表只服务于发起批次的进程存活期内的「按序投放 / 停止投放」——别尝试持久化（孤儿批次 / 陈旧 stop 标志的复杂度不值得），也别让前端逻辑依赖注册表存在。
- **`retry` 必须清陈旧 `phase`**：曾进入过检查阶段（`phase == "check"`）的任务被重试后，若不复位 `phase = ""`，协调者会把它误判为「检查期（无槽）」而不计入 `no_slot`——它实际重新排队等槽，协调者却看不见 → 预取配额错算 / 投放卡死。清 `phase` 在 `control("retry")` 路径上做，别挪进引擎。
- **`gate().acquire()` 返回值已变 bool**：True = 取到槽 / False = 等待中被取消（**未取槽**）——调用方在 False 时**绝不能 release**（release 有下溢保护会静默吞掉，泄漏不会当场炸，而是把闸门永久占满，此后所有 acquire 永久阻塞——`test_script_batch` 的 teardown 清扫正是防这类泄漏）。当前生产调用方 = 解析引擎（`gate()`）与合并引擎（`merge_gate()`，2026-09 批量合并）（均 False → `raise TaskCancelled`）；新增调用方不带 `stop_check` 时语义与旧版一致（阻塞至取槽），但别在其他调用方上据返回值做分支逻辑（「False = 中止」契约只属于解析排队路径）。
- **2026-09 解析改造不触碰承重不变量**：不变量 #9（基文件只由解析任务写出、检查阶段永不改写）、label 全角括号约定（`文本解析（{文件名}）`）、取消 ≠ 失败（CANCELLED 是独立终态）、绝不删除用户数据——**均不受影响**；改造只动了投放顺序（有序 + 预取）、槽位作用域（收缩到 LLM 分段阶段）、取消粒度（批量 + 排队即时）与 UI 信息布局（进度进文件行 / 日志区开关 / 指标双位置）。
- **2026-09 批量合并改造（Merge.vue 单选 → 复选并行）**：① **label 尾部 `：{pkg}` 是承重契约**——同时承载 F5 派生式重挂（前端 `pkgOfLabel` 取**第一个** `：`，与后端 `re.search(r"：(.+)$")` 尾部捕获同口径——包名含 `：` 时用 `lastIndexOf` 会解析错）与同包在途 409（`_inflight_merge_packages` 同一正则）——改 label 必须前端 + 后端 + 测试钉死三处同步。② **行状态优先级与「已合并」判据**：合并中 > 已合并 > 已就绪 > 已合成 C/T 段；已合并 = SSE 标记或 `06` 下有 **`.mp3`**（编码失败兜底 WAV **不算**——行回落已就绪、可重合并，`-y` 覆盖自愈；别把 WAV 计入「已合并」）。③ **全选 = 清空后只勾「已就绪」行**（用户拍板的语义）；全量全选 = 无视状态全勾（含已合并/未就绪）；在途行 checkbox 禁用（防同包重复提交，409 兜底）。④ **闸 = 硬上限 / 协调者 = 投放节奏**：`merge_gate` 钳制并行（`run()` 内 acquire/release，协调者挂了也不超限），协调者只管按序投放 PENDING 壳（不变量「等待槽 ≤ 4」，**无** `phase` 判定——合并任务全程持槽、无检查期，与解析闸的槽作用域不同；别把「在跑 ≤ C+4」当不变量）。⑤ **并行度公式 `max(1, min(4, cpu//2))` 是常量策略**——别加配置项（「评估并合理设置」= 自动评估；封顶 4 给 CPU/磁盘/内存留余量，约束点是整书 WAV 全量入 RAM）。⑥ **merge-status 的 `total` 恒取源解析 JSON 口径**（`_build_segments` + `count_completion`，与 `/batch-status` 同口径）——manifest 长度口径会把半合成包误判「已就绪」、静默合并出半本书；仅源 JSON 缺失/损坏/空才降级 manifest 长度。⑦ **取消/失败残留的部分 MP3 不删除**（绝不删用户数据；`-y` 覆盖自愈）；`00_temp` 暂存（段表 + `merge_tmp_*`）由 `run` 的 finally 在所有退出路径清理（只清自己暂存物，与约定不冲突）。⑧ **无 cancel-batch 端点**——取消全部 = 前端逐任务 `control(id, 'cancel')`（PENDING 壳就地终结 + RUNNING 协作取消，必然收敛）；合并协调者**无** stop 标志/注册表——被取消的壳自然掉出投放搜索，别把解析页 `_BATCHES` 注册表机制搬过来（它服务于「停止投放」，合并侧不需要）。
- **BGM label 契约三处钉死（2026-09 背景音乐系统）**：分析 module `bgm-analysis` label `章节气氛分析：{stem}`、混音 module `bgm-mix` label `背景音乐混音：{stem}`（全角冒号）——同一契约同时承载 F5 派生式重挂（前端 `stemOfLabel` 取**第一个** `：` 后全部，与后端 `_inflight_bgm_stems` 的 `re.search(r"：(.+)$")` 尾部捕获同口径——stem 含全角冒号时用 `lastIndexOf` 会解析错）与同章在途 409 守卫（`POST /mix` / `POST /match` / `POST /analyze` 三端点同一正则）——改 label 必须前端 + 后端 + 测试（`test_bgm_batch` label 两式钉死）三处同步；BGM 协调者（`api/bgm.py::_run_bgm_coordinator`）与合并协调者同形：**无注册表/无 stop 标志**（取消 = 前端逐任务 `control`，被取消壳自然掉出投放搜索）、0.2s 轮询、`BGM_PREFETCH_DEPTH=4`、不变量「等待槽 ≤ 4」；双模块共用一个协调者函数（`gate_fn` 参数区分 `gate()`/`merge_gate()`）。
- **音乐库在工作空间外（项目根 `music_library/`）= 全局跨工程资源，永不进 pathio 链**：`MUSIC_LIBRARY_DIR = PROJECT_ROOT / "music_library"`（import 时求值常量，**引擎侧一律 `core_paths.MUSIC_LIBRARY_DIR` 模块属性访问**——`from … import` 会把常量绑死进引擎模块、测试 monkeypatch 失效）；所有 `GET/POST/PUT/DELETE /api/music/*` 端点**无 `require_workspace`**（无工作空间也全可用，MusicLibrary 页无 WorkspaceGateAlert、不受 `show_audio_split` 过滤）；`bgm_assignments.json` 的 `music` 字段与 `music_index.json` 的 tracks 键一律存**纯文件名**（不带目录、不经 `pathio.resolve_path`/`to_workspace_relative` 迁移链）——工程搬家对音乐引用**零影响**，别「顺手」把它改成工作空间相对路径（会走进 pathio 的 `..` 越界/搬家恢复语义，而音乐库根本不在工作空间里）；音乐库位置**不可配置**（配置按工作空间存，用按工程的配置项定全局路径自相矛盾——先例 = 根级布局由代码常量管理）。
- **BGM `match_stems` 的 prev/next 边界语义（2026-09 测试发现的引擎修正）**：`prev` = 上一章（02 排序序）在**本次调用批内**时的**新鲜结果**（locked 章 = 其现有 music，locked 邻居同样构成约束），**批外**（含单章重匹配 = 批里唯一一章）→ 上一章的**现有** assignments 值；`next` 恒取下一章**现有**值（`match_chapter` 的相邻去重只排除 prev/next 两个名字于最高分档内、整档被挡才放宽 + reason 追加「（相邻章去重放宽）」）——**邻章条目逐字节不动**（单章重匹配绝不改写邻章 assignment，`test_bgm` 钉死）；实现 = `fresh: dict[stem → music]` + `_prev_stem_key`/`_next_stem_key`（按 `list_chapter_stems` 排序序取 ±1）；旧实现只带「批内上一章的新鲜结果」会把跨缺口（批内不连续）的批内 stem 误当 prev——别改回批内携带形态。
- **BGM `music=null`（无 BGM 章）= 合法成品**：匹配判「无候选」落 `music=null` + `matched_at`（有 `matched_at` = 匹配过判无，无条目 = 从未匹配——混音端点对两者不同 400）；混音走 **copy2 路径**（`shutil.copy2(06 旁白, 08 输出)`，不走 ffmpeg、`duration=None`）→ `mix_exists=true`（08 下有 mp3 即成品）→ 前端行状态「**已混音 / 无 BGM**」、按钮文案「无 BGM·复制原声」——别把 music=null 当错误态（`GET /chapters` 的 `music_missing` 只针对「music 指向的音乐文件已从库删除」：显「⚠ 已删除」、**禁止直接混音**（400「音乐库中找不到」）、**允许重匹配**（重匹配成功后恢复正常）、允许手动选曲改指他曲）。
- **BGM 部分混音产物留盘、`-y` 重跑自愈**（与 merge 同款）：`mix_chapter` 失败/取消残留的 `08_bgm/<stem>.mp3` 部分文件**不删除**（绝不删用户数据）；重混音 `ffmpeg -y` 覆盖自愈；`analyze_chapter` 失败 3 次 → **空标签记录**（`edited:false` + WARNING）而非不写——章不被 LLM 失败卡死，可手动编辑标签/重分析；两个 JSON（analysis/assignments）写盘 = 模块锁 + `mkstemp`（前缀 `.bgm_`）+ `os.replace` 原子换名（分析任务并行、每完成一章整体重写——`voice_config.json` 先例；分析任务只改**本 stem** 条目 + `model` 字段）。
- **BGM 在途守卫的非对称是有意的**：`POST /match` 只挡**分析在途**（读 analysis 半成品会被 match 快照固化，409「以下章节有分析任务在途（避免读取半成品分析）：…」）、**不挡混音在途**（match 只写 assignments，混音读 assignments 的时机在任务启动前的守卫链，读写不同文件无竞争）；`POST /mix` 只挡**同章混音在途**（两任务写同一 08 输出文件，409）；`POST /analyze` 只挡**同章分析在途**——三端点守卫链各自独立、无交叉阻挡，别「顺手」加交叉 409（会挡住并行的合法组合，如分析新章 + 混音已匹配章）。
- **`suggest-tags` = 同步 LLM 不经任务系统（与 BGM 分析 Task 不同）**：`POST /api/music/suggest-tags` 在请求线程内直跑 `_llm_chat_completion` ×2 次（文件名 + 用户描述 + 词表，「只能从词表中选」），model 空 400、全败 **502**「AI 推荐失败，请手动打标」、返回只留**词表内**名字 + 钳上限（防 LLM 造词/越桶）；而 `POST /api/bgm/analyze` 是每章一 Task（`bgm-analysis`，共享 LLM 闸、3 次重试、全败空标签不卡章）——两者 LLM 策略刻意不同（前者是行内即时辅助、后者是批量流水线阶段），别统一成同一形态；LLM 标签推荐**只依据文件名 + 描述 + 词表，从不读取/分析音频内容**（用户拍板），推荐结果仅作候选、以用户确认后的标签为准。
- **批量 AI 标签识别 label 契约 + 候选缓存语义（2026-09 音乐库批量识别）**：① **label 三处钉死**——module `music-ai-tags` / label `AI 推荐标签：{name}`：后端 `_inflight_ai_names()` 用 `re.search(r"：(.+)$")` 提取曲目名做同曲在途 409；前端 `MusicLibrary.vue` 的 `aiTrackOfLabel` 取**第一个** `：` 后全部（文件名可含全角冒号，两侧同口径——与 BGM `stemOfLabel` / Merge `pkgOfLabel` 同形）；`test_music_batch.py` label 钉死。改 label 必须前端 + 后端 + 测试三处同步。② **候选只进 `music_library/music_tag_suggestions.json`，索引 `tracks[].tags` 零变化直到用户确认**（弹层「全部采用/确认并修改」都终止于 `PUT /tracks/{name}` 带 `tags`，该端点随即 `clear_suggestion` 消费候选——非 tags 补丁不动候选）；候选缓存**存 LLM 原始词表内候选**（三桶、不做注册表二次过滤——词表可能事后改名/删除，缓存是历史读数）；`GET /library` 的 `suggestions` **只列现存曲目**（已删曲目的孤儿条目隐藏、**留盘不删**——BGM assignments 孤儿策略同款，别顺手清理）。③ **worker 2 败 = 任务 failed**（UI 给【重试】）——与 `analyze_chapter`「3 败空标签记录、绝不卡章」和同步端点「502」**三种结局语义都不同**：候选失败不卡任何下游（BGM 匹配读的是索引 tags 而非候选缓存），刻意不给兜底空候选（空候选与「识别失败」在 UI 上已可区分，兜底只会掩盖配置/网络故障）。④ 投放复用 `api/bgm.py::_run_bgm_coordinator`（真共享 LLM 闸 `gate()`、预取深度 4、无注册表/无 stop 标志——取消 = 前端逐任务 `control`）；槽位作用域 = 全任务（无检查期，同 BGM 分析）。⑤ **`apply-suggestions`（AI 推荐采用）= 第二条确认路径，不覆盖规则双重检查**：锁外初判 + 索引锁内复检（锁外判定后、写入前的毫秒级手动编辑同样胜出）；被跳过（`skipped_manual`）曲目的候选**保留**（用户可稍后经弹层确认——与「确认即消费」不矛盾：消费只发生在真正写入 tags 时）；陈旧候选名（词表事后改名/删除）**折叠进 custom 而非丢弃**（与 `PUT /tracks/{name}` 带 tags 的 `normalize_track_tags` 口径一致——别改成丢弃，弹层确认路径同口径）；「可采纳」判据 = 有候选（三桶任一非空）**且** 曲目四桶全空（`enabled` 不参与判定——禁用曲目也可采纳标签）。
